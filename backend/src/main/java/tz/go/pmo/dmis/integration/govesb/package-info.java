/**
 * GovESB client — OAuth2 client credentials, request envelope ({@code esbBody}),
 * RSA signature, hybrid RSA+AES payload protection, and outbound calls to institutional services.
 *
 * <p>Use the eGA helper patterns (sign / verify / encrypt); do not invent ad-hoc crypto in feature services.
 */
package tz.go.pmo.dmis.integration.govesb;
