/**
 * eGA layer — application services (interfaces only).
 *
 * <p>Each use-case contract is declared here as {@code XxxService}. Implementations live in
 * {@link tz.go.pmo.dmis.service.impl}. Controllers depend only on these interfaces.
 */
package tz.go.pmo.dmis.service;
