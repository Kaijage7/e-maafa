/**
 * eGA layer — JPA persistence entities ({@code @Entity}).
 *
 * <p>Map to database tables. Prefer JPA/Hibernate for new master data to support
 * eGA database/OS portability requirements. Prefer portable mappings over vendor SQL.
 */
package tz.go.pmo.dmis.entity;
