package tz.go.pmo.dmis.service;

import java.util.Map;

/** eGA service — paths unchanged (/v1/finance/economics). Deterministic formula engine (not ML/AI). */
public interface EconomicsOfDisasterService {
    Map<String, Object> model();
}
