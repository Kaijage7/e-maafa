/**
 * eGA layer — service implementations ({@code XxxServiceImpl}).
 *
 * <p>Business logic, transactions ({@code @Transactional}), and orchestration of repositories
 * and integration clients. One implementation class per {@code service} interface.
 */
package tz.go.pmo.dmis.service.impl;
