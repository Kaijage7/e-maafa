/**
 * eGA layer — Spring configuration (security beans, OpenAPI, ModelMapper, CORS, async).
 *
 * <p>Cross-cutting {@code @Configuration} classes. Prefer this package for new config;
 * existing security under {@code common.security} remains until fully relocated.
 */
package tz.go.pmo.dmis.config;
