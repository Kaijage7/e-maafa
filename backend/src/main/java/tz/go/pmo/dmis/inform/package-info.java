/**
 * INFORM risk model domain + engine (not thin REST).
 *
 * <p>HTTP is under {@code controller.InformController} / {@code PortalInformController}. Entities,
 * repositories and the standardisation engine remain here as a shared hub.
 */
package tz.go.pmo.dmis.inform;
