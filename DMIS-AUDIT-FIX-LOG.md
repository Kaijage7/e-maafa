# DMIS Audit Fix Ledger — resolve ALL, honestly, one by one
Source: DMIS-LINKAGE-AUDIT.md (2026-07-05). Status: OPEN → FIXED (with verification evidence) / WONTFIX (with reason).
**Current authoritative migration counter (2026-07-18): latest V214 (jwt denylist, applied live); next free V215.** The long V171 counter line immediately below is retained only as a historical campaign snapshot and must not be used for new migrations.
Migration counter: next free = V171 (V140 = ICS command roles; V141 = area-name backfill; V142 = area-role authority trim; V143 = stakeholder_portal.view + OH trim; V144 = full area-role least-privilege trim; V145 = budget scope/area consistency; V146 = Emergency Supplies stock-journal reconciliation; V147 = reconciliation actor fallback; V148 = forecast activation warning link; V149 = repository analytics quality; V150 = knowledge repository documents; V151 = relief distribution stock link; V152 = exercise scenario library; V153 = remove unused outbox scaffold; V154 = area roles use issued alerts, not EW workbench; V155 = sector keying and One Health dissemination permissions; V156 = incident advisory comments; V157 = partner planning/analytics scope; V158 = Roles & Permissions control labels; V159 = Stakeholder Portal partner baseline; V160 = Prevention & Mitigation sector controls; V161 = Stakeholder Portal donation controls; V162 = public report i18n coverage; V163 = incident-flow role groups and countrywide officer seats; V164 = settings/hazard/communication label updates; V165 = mainland LGA incident-flow seats; V166 = Monitoring & Evaluation module permissions; V167 = Monitoring & Evaluation indicator registry and workbench; V168 = M&E climate-vulnerability planning and quarterly reporting indicators; V169 = M&E TCVMP indicator wording; V170 = institution registry, policy roles, sector-scoped M&E indicators and glossary). ASSIGN SERIALLY HERE before creating any migration.

Documentation contract:
- Every finding must keep an `Fxx` heading, `Domain`, `Status`, `Evidence`, and `Fix hint` unless it is closed with explicit reason.
- `OPEN` stays open until code/data/docs are changed and verification evidence is recorded. A hidden symptom, backfill, workaround, or conceptual design does not make a finding fixed.
- `FIXED` or `CLOSED` must name the date, the implemented behavior, and the verification used (API, SQL, UI, test, grep, or build evidence).
- Count changes must be mirrored in `improvement.md` and, when operational guidance changes, `CODEX-DMIS.md`.
- Keep raw severity tags (`s1`-`s4`) separate from fix-order buckets (`P1`-`P3`); document promotions such as F93 explicitly.

Whole-ledger reassessment note (2026-07-09):
- Re-parsed all 116 F-headings and all 116 `- Status:` lines after the F34 PHR code tracking browser/API/SQL smoke closure: **54 FIXED/CLOSED**, **62 OPEN**, zero unknown statuses.
- Re-count 2026-07-10 after F23/F25/F26 dual-proof wave + prior campaign progress: see LIVE-ISSUE-TRACKER.md (operator board). Migration counter next free after V171 = **V172**.
- Incident-persona verification 2026-07-15: local-only credential generation produced 1,309 login rows across 14 incident roles and 195 unique council-scoped DDMC seats. Real JWT/API runs passed 1,309/1,309 authentication and permission checks, 195/195 DDMC scope checks, and 195/195 incident lifecycles with exact cleanup to zero controlled incidents/notifications. V209 attaches the 11 generated Zanzibar district-flow seats to their one-to-one active councils; their intentionally unstaffed RDMC stage auto-advanced to RAS and was verified in workflow history. Adviser comments, logistics tasking, public-report conversion, and browser action visibility remain explicitly NOT RUN in the workbook rather than being inferred green.
- Regression-gate verification 2026-07-15: an isolated `RUN_TESTCONTAINERS=true` Java 21/PostgreSQL 16.13 run executed **40 suites / 186 tests with 0 failures, 0 errors, 0 skips**. Flyway validated **197 schema-history entries**: the baseline marker plus **196 versioned SQL files through V212**. Mobile idempotency, incident cursor/tombstone/retention/concurrency/rollback/foreign-gap cases, notification restore/deleted-gap catch-up, GraphQL query/subscription controls, security enforcement and SSE relay/controller cases all executed. `HermeticPostgresSupport` disables scheduled scanner sweeps, scenario injects, and delivery retries. No F-item status count changed.
- Final-source delta 2026-07-15: subscription lifetime is now bounded by the earlier of JWT expiry or
  the configured 10-minute maximum, active streams poll the logout denylist, and V213 registers only
  the authenticated caller's bounded device installations for future push addressing. Eight tests were
  added. The focused GraphQL/WebSocket/relay set passes 25/25 and the restricted full run passes all
  104 non-database tests, but it skips 90 PostgreSQL tests because the sandbox cannot access Docker.
  A single Docker-backed 194/194 rerun, including V213 and its four device database tests, is
  still required; the earlier 186/186 proof must not be presented as certification of the final delta.
- **Tip-level rerun DONE 2026-07-18:** isolated `RUN_TESTCONTAINERS=true mvn verify` at repo tip `a294100`
  (migrations through **V214**, jwt denylist included) executed **213 tests, 0 failures, 0 errors, 0 skips,
  BUILD SUCCESS** — supersedes the 186/186 caveat above; the final delta is now certified by a full
  Docker-backed run.
- Mobile/web hybrid progress 2026-07-15: REST remains authoritative for commands/uploads/callbacks and GraphQL is additive for bounded composite reads plus a content-free foreground native wake-up subscription. V210 adds durable actor + operation + key incident-create idempotency. V211 adds an incident-only committed cursor with same-transaction insert/update/delete events, former-scope tombstones on jurisdiction moves, actor/permission/area-bound scope keys, retention expiry, scoped REST delta recovery, authenticated REST/SSE web wake-up, and a bounded GraphQL native subscription over the same relay. V212 independently makes inserted-notification catch-up commit ordered per user and now rejects restored-server cursors and advances across deleted gaps. V213 adds a bounded caller-owned device registry for future push addressing, not a provider sender. PostgreSQL regressions cover paging, delayed transaction ordering, scope moves, rollback atomicity, foreign-scope gaps, cursor lineage and retention; relay/browser regressions cover connection bounds, protected paths and 64-bit cursor handling; WebSocket tests cover operation quotas, JWT expiry/revocation, socket lifetime and frame bounds. This is F116 progress only: no general outbox/broker, native client or FCM/APNs delivery proof, cross-domain offline convergence, optimistic update conflicts, target-volume load/soak proof, or all-command idempotency exists yet. No F-item status count changed.
- Rechecked the resolved set and high-risk open set against current backend routes/services, migrations, frontend route/API references, map helpers, storage security, notification/outbox code, incident creation, budget disbursement, AI/ML/INFORM/satellite terms, and available build tooling.
- Result: **F94 was reopened** because the verified RBAC trim covered RAS/Reg DC/DED/DAS but not the full area-role family; F22/F46 were then fixed together by the approval-to-login implementation; F06 was then fixed by the exercise scenario library/MSEL launcher; F15 was then fixed by making critical incident workflow handoffs SMS-eligible through the notification backbone; F17 was then fixed by making One Health dissemination acknowledgments stakeholder-scoped and live-smoked; F18 was then closed after V153 applied on the migrated DB and removed `platform.outbox_event`; F19/F20 were then closed after live API/browser smoke of bulk approvals and scoped fulfilment-source redirects; F94 was then closed after V154 removed residual area-role EW view access and fresh-login/API smoke proved the new issued-alerts boundary; F104 was then closed after local-first map rendering and district/region/national scope smoke passed; F93/F100/F101/F106/F109 were then closed after live public-report, manual-incident, and route-permission smoke proved the public -> district/region/national flow and RBAC alignment; F47 was then closed after V156 restored real advisory comments and live role/area smoke proved view-comment-only behavior; F103 was then closed after authenticated/unauthenticated route-fallback browser smoke proved the new not-found/auth-guard behavior; F110/F111 were then closed after migrated-DB finance role smoke proved budget scope/area consistency and NDMF area/simulation cash-out guards; F32 was then closed after topbar notification-preferences browser/API/SQL smoke proved load/save and read/read-all behavior; F33 was then closed after browser/API/SQL smoke proved agency stock-line visibility, mismatched stock rejection, journal/status update, and truthful agency-user notification counts; F34 was then closed after public/API/browser smoke proved PHR status tracking through received/reviewing/dismissed/converted/published states without private-data leakage; F29/F30 were then closed after Command Post board API/browser smoke proved 30s refresh and logistics visibility; no new F117 was added by this pass.
- Available build evidence: frontend `npm run build` passed after F46, after F06, and after F15; backend compile with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -q -DskipTests compile` passed after F46, after F06, after F15, and after F17; backend package with Java 21 passed during the F104 closure after the response dashboard SQL-spacing fix. Backend regression tests remain blocked by F102: `mvn test` compiles main/test sources, then Spring tests repeatedly retry Hikari against local Postgres and do not complete without a live DB/hermetic Testcontainers setup.
- Release honesty: this is not a production certificate. Before release, run backend regression tests in a DB-capable/hermetic environment, run API/browser smoke checks for the 62 resolved items, and clean the stale deleted-endpoint security test tracked under F102.
- RBAC hardening note 2026-07-09: frontend action visibility for One Health directive issuance and Open Needs NDMF training disbursement now mirrors the backend permission gates (`one_health.directive`, `resource_allocation.dispatch`) instead of fixed role-name arrays; Roles & Permissions / User Management comments were corrected to state that the permission matrix is the operational control surface. No status count changed by this note.
- Sector workflow hardening note 2026-07-09: DLNA section keying now has a distinct `damage_assessment.key_section` permission, sector-bound agency users see only their own Annex-1 section(s) while PMO/EOCC consolidation users keep the full view, and One Health dissemination registry/show/recipient lookup now require `one_health.disseminate`, `one_health.approve`, or `one_health.manage` instead of broad `one_health.view`. V155 seeds/trims those permissions through the Roles & Permissions matrix. Local verification: Flyway V155 applied successfully; MDA Focal received HTTP 403 on `GET /v1/onehealth/disseminations`, Comms Officer and the full local persona received HTTP 200, MDA Focal received HTTP 200 on `GET /v1/response/dlna/my-sections`, and localhost frontend/backend returned HTTP 200. No status count changed by this note.
- Stakeholder-scope hardening note 2026-07-09: V157 narrowed the `Partners` role away from internal PMO operations, V159 restored the core portal baseline, and V161 adds the missing donation-specific control: `stakeholder_portal.view`, `stakeholder_portal.donate`, `anticipatory_action_plans.view`, `contingency_plans.view`, and `reports_and_analytics.view`. Partners can view the Stakeholder Portal shell/issued-alerts, Anticipatory Action Plans, Contingency Plans, Sendai Analytics, GIS Map, Resource Donations, and Fund a Measure or Training; PMO/internal permissions still guard Open Needs, repository events, incident reports, resource reports, generated reports, NDMF cash/fund endpoints, response task administration, Partner Directory, portal content management, and create/update actions. Earlier V157 verification proved the PMO/internal denials; the follow-up stakeholder-portal correction keeps Open Needs on `resource_allocation.view` while `stakeholder_portal.donate` controls only partner-facing donation/support pages. V161 local verification: Flyway recorded version 161 success; SQL showed Partners hold `stakeholder_portal.view` and `stakeholder_portal.donate`; Partner API smoke got 200 for support/donations and 403 for Open Needs, NDMF cash, Stakeholder Directory, and Frameworks; headless Chrome as Partner showed only Resource Donations, Fund a Measure or Training, and Issued Alerts, with no Open Needs, no NDMF tab, and Directory direct navigation landing on access denied. No status count changed by this note.
- System Settings control note 2026-07-09: V158 updates the stored permission labels for the new/stakeholder-sensitive surfaces, and the Roles & Permissions catalogue API/UI now exposes permission keys, action hints, module notes, search, and a compact control map for stakeholder baseline, Stakeholder Portal sub-pages, System Settings surfaces, Sendai/GIS, repository entry, report sub-pages, portal/news/QR, DLNA sector keying, and One Health dissemination. V159 adds the omitted `stakeholder_portal.view` core permission back into the Partners baseline and the control map; V161 adds `stakeholder_portal.donate` so partner donation surfaces can be managed without granting internal resource-allocation consoles. Local verification: backend health returned 200/UP; `/api/v1/settings/roles/catalogue` returns `Stakeholder Portal` with `stakeholder_portal.view` and `stakeholder_portal.donate`; Angular `/m/stakeholder-portal/issued-alerts` and `/m/stakeholder-portal/donations` returned through the dev proxy; partner browser smoke passed with zero failed requests/page errors. `start-all.sh` now avoids booting stale backend jars when source or migrations are newer. Future role-policy changes are grant changes in System Settings, not code edits, unless a brand-new protected surface is introduced. No status count changed by this note.
- Prevention & Mitigation sector-control note 2026-07-09: V160 splits the broad Prevention & Mitigation surface into System Settings switches for dashboard, hazards, mitigation measures, risk assessment, strategic infrastructure, disaster repository, risk mapping, and INFORM Risk Index. MDA Focal users keep only the sector INFORM lane by default (`risk_index.view`, `risk_index.create`; agency ownership still locks writes to their registered owner), while PMO/national roles receive the appropriate view/manage/approve grants. Frontend hub/sidebar/route guards now follow those sub-page permissions; list screens hide create/edit/delete/approve actions unless the matching permission is present; INFORM approvals require `risk_index.approve`; and INFORM data entry now has an Excel-style staged multi-area paste workbench for region/district/council values with level cards, matrix and long-row templates, staged preview, duplicate skipping, and blocking validation before submit. Local verification: backend compile passed, frontend production build passed, `git diff --check` and `bash -n start-all.sh` passed, Flyway recorded V160 success, SQL showed `MDA Focal` has only `prevention_and_mitigation.view`, `risk_index.view`, and `risk_index.create` in the Prevention/Mitigation family, MoH login returned only those P&M permissions, MoH `GET /hazards` returned 403 requiring `hazards.view`, MoH `GET /inform/indicators?owner=MoH` returned 200, MoH `GET /inform/pending` returned 403, a controlled MoH batch submit inserted one pending MoH value and was cleaned to zero leftovers, headless Chrome as MoH showed the Prevention & Mitigation card linking to Risk Index with exactly one sidebar item, no Approvals tab, and direct Hazards navigation landing on access denied, and follow-up headless Chrome as MoH staged `C001 / VU.VG.HC-LEXP / 6.2` as one valid council value, enabled submit only while clean, and blocked the same row when the selected level was Regions. No status count changed by this note.
- Stakeholder Portal donation-control note 2026-07-09: V161 adds the explicit `stakeholder_portal.donate` permission so linked Partners can use the Stakeholder Portal Resource Donations and Fund a Measure/Training surfaces without inheriting internal `resource_allocation.view` consoles. Backend module/method guards allow `stakeholder_portal.donate` OR the internal resource-allocation permissions for `/v1/response/bidding/donations`, `/pledge`/`/bids`, and `/v1/response/support`; `/v1/response/bidding/open-needs`, NDMF cash/fund endpoints, Partner Directory, and Content Frameworks remain on their internal permissions. `/v1/frameworks` is now guarded by `content_management.view/manage` instead of `isAuthenticated()`/`prevention_and_mitigation.manage`; frontend route guards and System Settings pages now split User Management, Roles, Locations, Resource Catalogue, Approval Workflows, Translations, Content Frameworks, and Stakeholder Portal sub-pages by their real permissions, and hide view-only action buttons. Local verification: backend compile passed, frontend production build passed, `git diff --check` and `bash -n start-all.sh` passed, Flyway recorded V161 success, SQL showed Partners hold `stakeholder_portal.view` and `stakeholder_portal.donate`, Partner API smoke got 200 for support/donations and 403 for Open Needs, NDMF, Stakeholder Directory, and Frameworks, admin got Frameworks and Open Needs 200, and headless Chrome as Partner showed only Resource Donations, Fund a Measure or Training, and Issued Alerts with no Open Needs, no NDMF tab, and Directory access-denied. MoH re-smoke still showed only INFORM Risk Index in Prevention & Mitigation, no Approvals, and Hazards access-denied. No status count changed by this note.
- F108 closure note 2026-07-09: the V161 framework permission work closes the Risk Frameworks/Publications split. `/v1/frameworks` reads/writes now use `content_management.view/manage` in both `ModuleGuardFilter` and `FrameworkController`; Content Frameworks route/action visibility follows the same frontend permissions; and V161 live smoke proved Partners/default access 403 and admin Frameworks 200. F107 later moved the current resolved/open count to 56/60.
- Public education/bilingual presentation note 2026-07-09: the public Education hub now presents a clearer learning pathway from risk literacy to hazard guides, seasonal planning, and official articles, with content-type and audience filters over managed Content Management records. The public language toggle updates `html lang` (`en` / `sw-TZ`), and the new education pathway/filter copy switches immediately between English and Kiswahili. Content Management -> Educational Content and Public Awareness now show bilingual readiness stats, EN+SW/Needs SW badges, and language filters so missing Kiswahili material is visible before publication. Educational-content updates now set Swahili fields directly, while publish toggles preserve existing bilingual fields. Follow-up hardening covers the public Report Hazard and Track Report flows: V162 adds the missing report/track labels to System Settings -> Translations, the report modal now localizes placeholders/options/success text, and the tracking card localizes status, urgency, dates, labels, empty states, linked-incident text, and invalid-code errors. Local verification: frontend production build passed, backend compile passed, `git diff --check` passed, Flyway recorded V162 success, `/api/v1/portal/i18n` returned the new Swahili keys, headless Chrome on `/education` confirmed English -> Kiswahili -> English switching with document-language changes, and headless Chrome Swahili smoke confirmed Report Hazard modal text plus Track Report result/error text with zero controlled-report leftovers. No status count changed by this note.
- Incident-flow staffing/settings note 2026-07-09: V163 adds role metadata (`category`, `scope_level`, `sort_order`, `incident_stage`, assignment hints and area/flow flags), grouped Role Management/User Management/Approval Workflow role catalogues, area/scope/seeded filters in User Management, and countrywide position-account seeding for regional RDMC/RAS/RC plus district DDMC/DED/DAS/District Commissioner seats. Approval automation now reports region/district coverage, not only raw officer counts. Local build verification passed (`bash build-recovery.sh` / Maven offline package, frontend `npm run build`, `git diff --check`). Live Flyway/API/SQL smoke is still pending because this sandbox cannot access Docker, localhost, or Postgres; therefore F75 remains open and the resolved count is unchanged.
- Communication Center permission note 2026-07-09: F95 is fixed. `/m/content-management/communication-center`, `/v1/communication/*`, `/v1/content/sms-logs`, and `/v1/content/email-logs` now consistently use `communication_and_alerts.view`, while SMS/email compose remains `communication_and_alerts.send` and view-only users do not see compose panels. Roles & Permissions labels/control-map hints expose the view/send split. Live `frontend/render-tests/f95-communication-rbac-smoke.js` proved Content-only API/route denial, Communication view-only API/log access without compose, and Communication send access with compose; count moved to 58 resolved / 58 open.
- Hazard Monitor permission note 2026-07-09: F112 is fixed. Hazard Monitor remains at the existing route, but the route/API is now `hazards.view`, threat create/update/timeline/plan-review/graphic upload are `hazards.manage`, and the Angular screen hides or disables mutation controls for view-only users. Roles & Permissions labels state that Hazards controls both Hazard Management and Hazard Monitor. Live `frontend/render-tests/f112-hazard-monitor-rbac-smoke.js` proved Content-only API/route denial, Hazards-view read-only access without create/upload/timeline mutation controls, and Hazards-manage write/graphic paths. The smoke also exposed a threat-controller null-enum crash on partial update payloads, now fixed. Count moves to 59 resolved / 57 open.
- System Settings ownership note 2026-07-09: F113 is fixed. Translations and Agencies are no longer governed by broad Content Management access: `/m/user-management/translations` and `/v1/settings/translations` use `translations.view/manage`, `/m/user-management/agencies` and `/v1/settings/agencies` use `user_management.view/manage`, old Content Management URLs redirect to those owning routes, and view-only users do not see mutation controls. Live `frontend/render-tests/f113-settings-rbac-smoke.js` created temporary roles through the real Roles & Permissions API and proved Content-only denial, view-only read access, manage writes, and Chrome route/action visibility. Later F29/F30 closures moved the current count to 62 resolved / 54 open.
- Monitoring & Evaluation module note 2026-07-09: V166 adds `monitoring_evaluation.view/manage` to the permission matrix, and V167 adds `monitoring_evaluation.enter`, the configurable `me_indicator_catalog` / `me_indicator_values` registry, seeded regional/LGA/MDA/PMO/partner/incident/early-warning/resource-readiness indicators, and `/m/monitoring-evaluation/workbench` for period entry plus spreadsheet paste. The `/v1/monitoring-evaluation/dashboard` API and `/m/monitoring-evaluation/dashboard` Angular page aggregate administrative coverage, budget execution, incident-flow staffing, readiness assets, disaster-cycle activity, institution/stakeholder coverage, incidents/warnings, and resource distribution from live operational tables. Partners are not granted the module by default; area officers can enter only scoped area values, agency-linked users are clamped to their own agency values unless granted manage, and access remains controlled in System Settings -> Roles & Permissions. Local verification: backend compile passed, frontend production build passed. No audit status count changed by this feature note.
- M&E climate and quarterly-reporting note 2026-07-09: V168 adds financial-year planning indicators for whether national, regional, LGA, sector/MDA and partner plans are considering Climate Vulnerability Map System (TCVMP); sector quarterly response/recovery report indicators; and a regional quarterly indicator for resources used in response from the regional budget. V169 corrects the indicator codes/labels/descriptions to the official TCVMP wording. The regional M&E dashboard table now also shows live `regionalBudgetResponseUsed` from disbursed regional-budget commitments linked to incidents. No audit status count changed by this feature note.
- Institution registry / policy-role note 2026-07-09: V170 adds provenance, class, subclass, sector-tag, policy-role, M&E-required and role-summary fields to `agencies` and `stakeholders`; adds policy-role metadata to `me_indicator_catalog`; seeds policy-derived sector indicators for health, water, agriculture, livestock/fisheries, infrastructure/transport, environment, lands, education, communications, finance/planning, PMO-DMD and security/response mandates; and seeds disaster-management glossary terms from the provided policy/strategy documents. The local `InstitutionRegistryLocalSeeder` imports the normalized 985-row workbook fixture from the provided `ORODHA YA WASHIRIKI.xlsx` and `FINAL_DRR_Stakeholders_DATABASE_01092022 (2).xlsx` without duplicate names inside each registry, and `/m/user-management/institutions` exposes agencies, stakeholders, source registers, policy roles, glossary terms and duplicate warnings under `user_management.view/manage`. M&E indicator listing and save now enforce agency/stakeholder sector/class tags in addition to own-agency/own-stakeholder scoping, so an MoH agency account sees generic agency indicators plus health indicators, not unrelated water/lands/security policy indicators unless System Settings changes the institution metadata or grants manage. Local verification: backend compile passed, frontend production build passed, and `git diff --check` passed. No audit status count changed by this feature note.

179-claim revalidation note (2026-07-08):
- Full universe now under challenge: **154 original audit claims + 25 later/fresh claims = 179 claims**.
- The 116 F-items are the tracked issue ledger; the original 63 `WORKING` claims are affirmative production claims, not permanent trust tokens. Each must be rechecked against current source, migrations, route/API guards, role variants, and live/API/browser smoke evidence.
- First working-claim crosswalk correction: original claim "Drill-isolation seal ... sims cannot move real stock/money/comms" was overbroad because F111 found direct NDMF incident disbursement skipping simulation isolation; that specific caveat is now closed by live finance/drill smoke proving table-top simulation blocking and full-scale `allow_real_ops` behavior. Original partner-registration claim #11 is now qualified by fixed F22/F46 evidence: row creation alone was insufficient before, and the verified flow now covers registration, approval, set-password, and login. The older Open Needs partner smoke is now superseded by the corrected 2026-07-09 stakeholder donation policy: Partners may use Resource Donations and Fund a Measure/Training through `stakeholder_portal.donate`; Open Needs, internal resource-allocation, and NDMF cash/fund controls remain denied unless separately granted.

## F01 [FIXED s4] 5 EW boundary endpoints whose only consumers (Streamlit dashboards) were retired: GET /ew/stakeholders, POST /ew/disseminate, POST /ew/sms-test, POST /ew/monitoring/reports/batch, POST /ew/monitoring/request-update
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-05 — 5 dormant handlers deleted (Streamlit-only consumers); VERIFIED: all 5 → 404, kept monitoring-report round-trip still works.**
- Evidence: EwBoundaryController now exposes only the surviving monitoring report read/write endpoints; the five retired handlers are no longer mapped. Historical evidence before the fix showed `/api/ew/stakeholders` exposed 27 stakeholder contacts and `/api/ew/disseminate` reached real dispatch validation; the running native flow now uses Communication Center / EOCC bulletin dissemination instead.
- Reexam 2026-07-08: production handlers remain gone; source search found only the EwBoundaryController audit comment and one stale security test reference to `/ew/disseminate`. That test-suite drift is tracked under still-open F102 and does not reopen F01.
- Fix hint: Delete the 5 handlers (or fold dissemination into the native Communication Center flow); they are dormant SMS-dispatching surface with no UI.

## F02 [FIXED s4] No product surface sets users.region_id / district_id / stakeholder_id — area officers can only be manufactured by SQL
- Domain: User roles & registration
- Status: **FIXED 2026-07-05 — UM create/update accepts+validates regionId/districtId/agencyId/stakeholderId; VERIFIED: API-created RAS(Mwanza) saw exactly its region's 1 incident; cross-region district rejected 400.**
- Evidence: grep 'update public.users set' across the whole backend returns exactly 4 statements: name/email (UserManagementController.java:129), password (:153, AuthController.java:121), notify prefs+phone (NotificationController.java:100) — nothing ever writes region_id/district_id/stakeholder_id/agency_id. Consequence proven in code: JurisdictionScope.tierFor (JurisdictionScope.java:94-112) puts an area-role user with NULL area into REGION/DISTRICT tier, then appendAreaScope emits '1=0' (lines 177-194) → sees ZERO incidents; IncidentWorkflowService.assertStageAccess (lines 118-135) throws 'no area assi
- Fix hint: Expose area attachment in User Management (single source: the users table already has the FK columns and indexes); until then every new DED/RAS/RC/DAS/DC account requires direct DB writes.

## F03 [FIXED s4] Per-incident 'was this forecast?' answer — the user's core ask — has no surface on the incident record
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-06 (Wave 2) — computed `forecast` block on GET /v1/response/incidents/{id} (area district-first/region-fallback + validity+48h tail + hazard-compatibility guard) + green WARNED / grey Out-of-forecast badge on incident-show; VERIFIED live + independently re-checked (CONFIRMED): 6 incidents all carry the 6-key block; incident 82 in SQL-proven area+time overlap with EW-2026-00050 returns covered:false (hazard guard fired, drought_heat vs storm_water); controlled Floods incident in warned district 1964 returned covered:true EW-2026-00050 / Major Warning / lead 33h (= exact validity_start→reported_at); same-region district 1967 NOT claimed; district-less incident still 200s; both badge variants screenshotted on :4200 with zero console errors; test rows deleted.**
- Evidence: No FK columns exist in either direction: psql information_schema shows incidents has no warning/forecast column and early_warnings has no incident column. incident-show.component.ts contains no warning linkage UI (only Bootstrap 'btn-outline-warning'/Swal icons match a grep for 'warning'). The forecast/occurrence classification lives ONLY in the aggregate report; an officer opening incident #82 cannot see it fell inside warned district Bahi during EW-2026-00050's window.
- Fix hint: Add a computed block to IncidentController.show (same EXISTS: warning_hazards row where area matches and reported_at within validity+tail) returning {covered:boolean, warning_code, warning_level, lead_hours}; render a 'Forecast: WARNED (EW-2026-00050, 33h lead) / UNWARNED' badge in incident-show and the triage queue. Optionally persist incidents.warning_id (nullable FK) set at triage time for audi

## F04 [FIXED s4] COST-USED per disaster: three disconnected mechanisms, repository shows TZS 0 for all 75 events
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-06 (Wave 2) — list() returns per-event `costUsedTzs` (= recorded gov_response_tzs + in-kind allocations×unit_cost over linked incidents/allocations + budget_commitments cash over linked incidents), show() returns 7-key `costUsed` breakdown block, create/update accept optional govResponseTzs (blank=unchanged, non-numeric→400 not 500), detail card renders the three mechanisms honestly labelled; VERIFIED live + independently re-checked (CONFIRMED): create 1,234,567→update 7,654,321→blank-leaves-unchanged all held; linking incident 2 made budgetCommittedTzs equal the independent SQL sum; re-checker also proved the in-kind arm non-zero (109,715,000 over 15 allocations == SQL); CSV export shows the real figure; UI list column + cost card screenshotted; test event+links removed.**
- Evidence: (1) In-kind: responseInvestment = allocated_resources×unit_cost for LINKED incidents (DisasterEventService.java:493-504), rendered on the card (repository-event-detail.component.ts:284-291 'DMD response investment') — but zero incident links exist, so LIVE GET /repository/events/{id} returned {valueTzs:0, allocations:0} and the Sendai 'DMD response delivered' insight (SendaiAnalyticsService.java:223-238) never fires (absent from live insights list). (2) Cash: Budget&Finance V99-V101 works E2E — LIVE GET /v1/finance/budgets/1 shows commitment of 5,000,000 TZS against incident #2 'Market fire — 
- Fix hint: Surface cost on the event card from both ledgers: sum budget_commitments (via linked incidents) + allocations×unit_cost; add gov_response_tzs to create/update/show or drop it from the CSV — today the exported column silently reports 0 government response for every disaster.

## F05 [FIXED s4] No ICS organization structure: no incident commander, no section chiefs, no per-activation org chart or named command roles
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-06 (Wave 3) — V140 activation_command_roles (9 standard ICS roles, partial unique index = one active holder per role), appoint/relieve endpoints journaling both handover events into task_activity_log (AAR shows command handovers), board() returns command_roles + drf_sections (15 DRF lanes statically mapped under Operations/Planning/Logistics), 'Incident Command (ICS)' org-chart panel on the Command Post board; VERIFIED live: appoint IC→board+journal, replacement auto-relieves incumbent w/ handover journal, relieve→vacant, SQL duplicate-active blocked by the index, invalid role / unknown user → clean 422, Minister (no tasks.manage) → 403, board regression intact, UI panel screenshotted (initial UI-check 'failure' was the test string-matching lowercase against CSS-uppercased text).**
- Evidence: grep -rni 'commander|incident_command|ics_' over all migrations hits only a comment in V132 line 11; no table for activation roles exists (psql \dt shows only response_activations/incident_tasks/task_activity_log/activation_injects/task_dependencies/task_updates). DRF lanes assign to stakeholder ORGANIZATIONS (CommandCenterController.java:559-574) and tasks to users (TaskController.java:266), but nothing models who commands the incident, who runs Operations/Planning/Logistics/Finance, or deputies — the ICS backbone the user asked for.
- Fix hint: M: add activation_command_roles table (activation_id, role e.g. IC/Ops/Planning/Logistics/Finance/PIO/Safety, user_id, appointed_at, journal on appoint/relieve), an org-chart panel on the board, and appointment events into task_activity_log so the AAR shows command handovers. Map the 15 DRF lanes under section chiefs for the hierarchy view.

## F06 [FIXED s4] Simulation at scale is single-incident only: no scenario library, no exercise templates/MSEL, no multi-incident composite exercises, no participants roster, no time compression
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-08 — reusable exercise scenarios now support template incidents, MSEL events, participant rosters, compressed-time launches, and multi-incident simulation activations on the existing Command Post board; VERIFIED with migrated API/SQL smoke and cleanup.**
- Evidence: psql: no tables matching %scenario%|%exercise%|%drill% exist; grep 'scenario' in backend hits only CommandCenterController (inject comments) and an unrelated EW seeder. A simulation = one cloned incident + ad-hoc injects typed in live (V132). There is no way to pre-author a national multi-region scenario (e.g. cyclone + flood + disease outbreak across 3 regions), reuse a past exercise script, enrol participants, compress the 72-hr clock, or score against expected actions — the 'higher scale' the user wants.
- Fix hint: L: add exercise_scenarios (title, hazard, regions, objectives) + scenario_events (MSEL: offset_minutes, inject payload, target DRF, expected_action) tables; a launcher that spawns N drill-clone incidents/activations via the existing activation machinery and bulk-loads scenario_events into activation_injects with computed due_at; participant roster; time compression; expected-action evidence in the board/AAR.
- Implementation 2026-07-08: V152 adds `exercise_scenarios`, `scenario_incidents`, `scenario_events`, `scenario_participants`, `exercise_runs`, and `exercise_participants`, plus scenario/run links on simulation incidents and response activations and MSEL metadata on `activation_injects`. `ExerciseScenarioController` lists/creates/launches reusable scenarios under `/v1/response/coordination/scenarios`; `ActivationService.activateScenarioDrill` reuses the existing Command Post activation/task/log path for pre-authored drill incidents; the Command Post UI now shows a scenario library, launch controls, run-code badges, DRF-targeted injects, and expected actions.
- Verification 2026-07-08: backend compile passed; frontend `npm run build` passed. A local source-run against Docker Postgres applied Flyway V152 (`137` validated migrations, schema version `152`). API smoke listed the seeded reusable scenario `TZ-CYCLONE-FLOOD-HEALTH-72H` with 3 incident templates and 6 MSEL events. A controlled scenario `CODEX-F06-1783510520` was created through the API with 2 incident templates, 3 MSEL events, 1 rostered IC participant, and 30x time compression; launch returned run `1` / `CODEX-F06-1783510520-1783512010968`, activations `48` and `49`, simulation incidents `100` and `101`, 190 DRF tasks, 4 copied injects, and 2 roster enrollments. Board reads for both activations returned scenario title/run code, `is_simulation=true`, 15 DRF lanes, 95 tasks each, EOCC Officer appointed as IC, DRF-targeted expected-action injects, and compressed due-time firing (`Controlled opening brief` fired on both boards; activation 48 also fired `Controlled bridge access loss`; activation 49 retained the later `Controlled clinic surge` pending). SQL totals matched exactly: `activations=2`, `incidents=2`, `tasks=190`, `injects=4`, `participants=2`, `roles=2`. Cleanup deleted only the controlled rows (`injects=4`, `participants=2`, `activations=2`, `incidents=2`, `scenarios=1`); follow-up SQL showed `codex_scenarios=0`, `codex_runs=0`, `codex_incidents=0`, `codex_activations=0`, and the seeded scenario still present. The temporary backend was stopped cleanly.

## F07 [FIXED s4] Admin user creation + role assignment (Settings → User Management)
- Domain: User roles & registration
- Status: **FIXED 2026-07-05 — modal now has role-driven Region/District/Agency/Partner pickers + Area column; canSave blocks area roles without attachment.**
- Evidence: Backend create takes ONLY name/email/password/roles: UserManagementController.java:93-113 (dmis-platform/backend/src/main/java/tz/go/pmo/dmis/settings/); frontend POST body is exactly {name,email,password,roles}: user-management.component.ts:180-182; the form has no region/district/agency picker (template lines 78-107). Role catalogue IS complete: live GET /v1/settings/users as admin returned all 24 DB roles as checkboxes (observed: 'roles offered: 24' incl. RAS/RC/DED/DAS/Dist DC/Reg DC). So an admin can create a user with ANY role but can NOT attach a region/district — the users table has re
- Fix hint: Add region/district (and agency/stakeholder) pickers to the create/edit modal, shown when an area/agency role is selected, and accept+persist them in UserManagementController.create/update.

## F08 [FIXED s4] Two stakeholder-link columns, only one maintained: link-user sets stakeholders.user_id but every partner-identity guard reads users.stakeholder_id (set by seeds only)
- Domain: User roles & registration
- Status: **FIXED 2026-07-05 — linkUser/create/update sync users.stakeholder_id ↔ stakeholders.user_id both directions; VERIFIED: relink moved both columns, unlink cleared both.**
- Evidence: linkUser writes stakeholders.user_id only (StakeholderAdminController.java:181-182); NOTHING in the backend writes users.stakeholder_id (grep 'update public.users set' — 4 hits, none touch it). Yet JurisdictionScope.currentStakeholderId() reads users.stakeholder_id (JurisdictionScope.java:151-163) and is the basis for: (a) the bid self-identity guard — recordBid skips 'only your own organisation' when it is null (StakeholderBiddingController.java:227-230) while submitBid (POST /bids, gated only by resource_allocation.request which Partners HOLDS, line 185-193) accepts any body stakeholder_id →
- Fix hint: Make linkUser also set users.stakeholder_id (and clear it on unlink/relink), or refactor currentStakeholderId() to resolve via stakeholders.user_id as the single source of truth.

## F09 [FIXED s4] Rollback into an unstaffed auto-skipped tier strands the incident (no settle, no resubmit path)
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-05 — rollback walks past unstaffed/auto tiers to first actionable tier or rejects with clear message; VERIFIED: #91 rejected (nothing staffed below), #34 rolled past RDMC to DED with stamped history; forward settle unchanged.**
- Evidence: settleStage javadoc: 'Backward transitions (rollback) deliberately do NOT call this' (IncidentWorkflowService.java:363-364); rollback lands on PREV_STAGE unconditionally (225-235). Live precondition on the test incident itself: #91 (Tanga) history shows auto_advanced past waiting_ded AND waiting_rdmc because 'no officer staffs this tier'; psql: region 52 staffing = RAS 1, RC 1 only; portal_settings incident_approval waiting_rdmc=skip_if_unstaffed. So one RAS rollback of #91 → waiting_rdmc, where assertStageAccess requires a Tanga 'Reg DC' that does not exist; submit() only accepts draft/rolled
- Fix hint: On rollback, either re-run a reverse settle (skip unstaffed tiers downward) or block rollback into a tier that stageStaffed()=false with a clear message.

## F10 [FIXED s4] EW report counts one warning once PER DISTRICT ROW — headline stats inflated ~6x
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-06 (Wave 2) — rows aggregated to ONE per warning×region (min/max validity, district list + district_count) BEFORE classification; headline counts per DISTINCT warning_id; additive summary keys same_area_different_hazard + warned_area_windows; VERIFIED live + independently re-checked (CONFIRMED): warnings_issued 53→8 == SQL count(distinct warning_id) for the default window; EW-2026-00050 appears exactly once for Dodoma with its 7 districts listed and min/max == SQL; rows == warned_area_windows == distinct (warning,region) pairs; summary identity warned+no_incident+different_hazard == issued holds; RAS(Dodoma) scoped view internally consistent; UI classbar (now 5 buckets) matches API; no 500s across date ranges incl. zero-warning ranges.**
- Evidence: EwManagementController.java:68-79 iterates raw warning_hazards rows (one per warning×hazard×district). DB: 53 rows but only 8 distinct warning_codes (psql: select count(distinct w.warning_code)... = 8). Live output shows EW-2026-00050 'Heavy rainfall / Dodoma' listed 7 times (district rows 1964,1965,1966,101,1968,1969,1970 share region_id=1), each matching the same incident 82 → warned_incident=9 is really 3 distinct warning-region hits. warnings_issued=53 vs 8 real warnings.
- Fix hint: Aggregate to one row per (warning_id, region_id) — or per warning_code — before classification: group by w.id, wh.region_id with min(validity_start)/max(validity_end), and count distinct warnings in the summary.

## F11 [FIXED s4] Hazard type NOT used in warned→incident matching (Javadoc claims it is) — cross-hazard false positives
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-06 (Wave 2) — hazard-compatibility predicate (exact hazard_id OR related family map: flood-storm / drought-agri / disease / fire / geophysical; a side with no hazard info is never demoted) applied in the EW report; incompatible area+time matches move to per-row `different_hazard_incidents` + summary `same_area_different_hazard` so the spatial coincidence stays visible without inflating true positives; VERIFIED live + independently re-checked (CONFIRMED): Fire/Drought incident 82 now in EW-2026-00050's different_hazard bucket not its hits; Drought EW-2026-00038 no longer claims Windstorm/Cholera; NULL-hazard incident kept as a hit (never blocks); the surviving warned_incident=1 is a genuine compatible match; warned_incident dropped 9→1 on the seeded corpus.**
- Evidence: EwManagementController.java:27-28 claims hazard is 'used to refine when both sides carry hazard_id', but the incident query (lines 85-96) filters only time+region — no hazard predicate exists. Live proof: 'Heavy rainfall' warning EW-2026-00050 matched incident 82 'Fire' (hazard_id=4, both sides carry hazard ids) → counted as warned→incident true positive with 33h lead; 'Drought' warning EW-2026-00038 matched 'Windstorm roof damage' + 'Cholera outbreak'. 15 of 22 non-simulation incidents carry hazard_id, so refinement is feasible.
- Fix hint: Add a hazard-compatibility predicate (wh.hazard_id = i.hazard_id, or a keyword-family map like AnticipatoryPlanController.matchingPlans' cyclone→floods logic at AnticipatoryPlanController.java:253-273) with a 'related-hazard' fallback bucket so rainfall→flood still counts.

## F12 [FIXED s4] Per-incident action tracing today = three DISCONNECTED trails, no unified operations log (the user's 'traced well virtually' is only partially met)
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-06 (Wave 3) — GET /v1/response/incidents/{id}/ops-timeline (IncidentTimelineController): nine trails union-merged time-desc (workflow, task via activation, situation reports, allocations, dispatch incl. the source_details fulfilment journal, warehouse movements, sms/email incident_workflow logs, budget commitments) with per-source counts, ?source=/?limit= filters, incidents.view gate + the show hub's exact area scope; <dmis-incident-ops-timeline> master-log panel on incident-show; VERIFIED live: per-source counts == origin tables (independently re-probed: incident 2 workflow 18==18, budget 1==1, 5M disbursement leads the log), probe rows resolve 1:1, bogus source → 422 w/ detail+message, quiet incident → 200 empty, Partners → 403, anon parity == pre-existing incident endpoints (local dev persona, F85), UI panel rendered w/ 6 filter chips + 30 rows, no page errors. Honest scope note: command_role source omitted at build time (no such table then) — the F05 journal events DO ride in via the task trail.**
- Evidence: Trail 1: incident_workflow_histories on incident-show (IncidentController.java:816, incident-show.component.ts:214-221). Trail 2: task_activity_log keyed by ACTIVATION not incident (ActivationService.java:100-105) — only 13 rows total live (psql), and only Command-Post actions write to it. Trail 3: Situation Reports (incident_history_reports, IncidentController.java:393/645). Dispatch, warehouse movements, communications, allocations and budget actions for an incident are journalled in their own modules and NEVER appear on the Command Post timeline — the board's recent_activity (CommandCenterC
- Fix hint: M: build a read-side union timeline endpoint per incident (task_activity_log + incident_workflow_histories + allocated_resources source_details journal + sms/email logs + warehouse ledger, all filtered by incident_id) and render it as the board's master ops log; no schema change needed since every trail already carries incident linkage.

## F13 [FIXED s4] Scanner entity taskings round trip (V131) — entity-taskings.component.ts + ScannerController tasking endpoints
- Domain: assignments/tasks/provisions + information & knowledge
- Status: **FIXED 2026-07-06 (Wave 2) — `<dmis-entity-taskings agency="mow">` embedded in mow-flood.component.ts (same pattern as TMA's ew-alert-map embed); all 7 taskable entities now render their inbox (5 via shared agency-event-console, TMA + MoW via direct embeds); VERIFIED live + independently re-checked (CONFIRMED): as mow@pmo.go.tz the awaiting flood tasking is visible at /m/preparedness/early-warnings/mow (screenshot, content matches API), Acknowledge round-trip worked in the UI and was precisely reverted; TMA console regression clean; consolePath string equals the route visited. Follow-up logged as F92: GET /entity-taskings is not agency-scoped for READS (pre-existing, mutations are guarded by assertOwnAgency).**
- Evidence: Round trip is real code and exercised: awaiting→acknowledge→respond→EOCC accept/return (ScannerController.java:306-385, disaster-scanner.component.ts:86-87 Accept/Return wired, agency lockdown assertOwnAgency:70-77). DB: 12 taskings (10 awaiting/1 responded[gst]/1 returned[tma with review_note]); 1 row carries new urgency/source/instruction picker fields. Live GET ?agency=tma returns full context. BUT the MoW console never renders the inbox: route /m/preparedness/early-warnings/mow loads MowFloodComponent (app.routes.ts:41) which does NOT import EntityTaskingsComponent (grep confirms; only age
- Fix hint: Embed <dmis-entity-taskings agency="mow"> in mow-flood.component.ts (same pattern as ew-alert-map.component.ts:85 for tma).

## F14 [FIXED s3] Preparedness Warehouses registry 'Stocks' column always 0
- Domain: Warehouse management
- Status: **FIXED 2026-07-05 — real per-warehouse sum(inventory_items.quantity); VERIFIED: API == psql (6315/2500/899/450/110/32).**
- Evidence: WarehouseService.java:20-21 comment: '(Stock counts join warehouse_stocks later; reported as 0 until then.)' and toRow() hardcodes `0` into WarehouseRow.stocks (WarehouseResponse.java:12); the warehouse_stocks table does not even exist (psql: relation "public.warehouse_stocks" does not exist). UI displays it as a badge (warehouses.component.ts:75,88 'Stocks' column). Live: GET /v1/warehouses shows stocks=0 for PMO Central/Coastal/Eastern while the ledger holds 6315/110/32 units.
- Fix hint: Join inventory_items (sum(quantity) where warehouse_id=w.id and temporary_warehouse_id is null) — the number already exists in warehouse-ops index.

## F15 [FIXED s3] Backbone SMS channel + per-user notify_sms preference — never exercised by any event
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-08 — Critical incident workflow stage handoffs now make the central notification backbone SMS-eligible while still respecting per-user notify_sms + phone preferences; preference save now rejects SMS opt-in without a valid Tanzanian mobile; VERIFIED with compile/build and controlled live API/SQL smoke.**
- Evidence: grep across backend: NO caller ever uses Notice.all() or withChannels() (only definition hits in NotificationService.java:47/51); every call site uses Notice.inApp (sms=false) except IncidentWorkflowService.java:535 which sets email-only. So NotificationService.dispatch()'s smsPhones branch (lines 102-104) is unreachable in practice. Compounding: psql — only 1 of 107 users has a phone and notify_sms=true. All real SMS traffic bypasses the backbone via direct MgovSmsService calls (EW/OH/alerts/stakeholder-verify).
- Implementation 2026-07-08: `IncidentWorkflowService.notifyStage` now sets `sms=true` only for Critical-severity actionable workflow stages (`waiting_*` owners), leaving non-critical/terminal notices in-app+email only. `NotificationController.saveMyPreferences` now validates SMS opt-in server-side: blank phone returns 422 and non-Tanzanian mobile formats are rejected before `notify_sms=true` can be stored. The topbar preference form now shows the backend's exact validation message. No production fake phone seeding was added; SMS remains an explicit user opt-in and the smoke used a temporary local role preference that was restored.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed. Local source-run kept M-Gov/Gmail credentials blank. As `X-Local-Roles: Dist DC`, saving `notify_sms=true` without a phone returned HTTP 422 with "Add a phone number before enabling SMS notifications"; saving `phone=0712000115, notify_sms=true` returned HTTP 200. Controlled Critical incident `102` (`CODEX-F15 critical SMS backbone smoke 1783513510`) submitted via `/api/v1/response/incidents/102/submit` rested at `waiting_ddmc`; SQL showed one `resource_notifications` row for user `4`, one `sms_logs` row `notification_type='incident_workflow'`, `notification_id=102`, `recipient_phone=255712000115`, `status=pending` because M-Gov was intentionally unconfigured, and one pending `email_logs` row. Negative control Moderate incident `103` to the same SMS-enabled user created in-app/email rows but `0` SMS rows. Cleanup restored user `4` to `phone=null, notify_sms=false`, deleted the controlled incidents/notifications/SMS/email/workflow rows, and follow-up SQL showed zero controlled leftovers.
- Fix hint: Implemented by marking only Critical incident workflow handoffs SMS-eligible through the central dispatcher while keeping SMS opt-in and audited.

## F16 [FIXED s3] gov_response_tzs column: designed cost field with no fill path, exported as always-0
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-06 (Wave 2, together with F04) — govResponseTzs wired into create/update/show and the event form (inline edit while Open, read-only after validation), CSV export now carries real values; VERIFIED live + independently re-checked (CONFIRMED): recorded value round-trips create→show→list→CSV; blank update leaves the column unchanged (coalesce semantics); non-numeric input → 400 with readable message (ProblemDetail + message, frontend-compatible), not a SQL 500.**
- Evidence: V61__disaster_effects_official_report_fields.sql:20-23 creates it with a comment 'relief disbursed (OWM-SBUU + region/council)'; grep across backend+frontend finds only exportCsv (DisasterEventService.java:120,131) and the seeder insert (OfficialDisasterReportSeeder.java:77) — no API request field, no UI form field, show() omits it. LIVE: all rows 0; CSV export shows '0.00' for every event including the validated official-report cards.
- Fix hint: Either wire it into the event form/API and show(), or remove it from the CSV header until fillable.

## F17 [FIXED s3] One Health dissemination acknowledge endpoint (stakeholder ack round-trip)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-08 — stakeholder-scoped acknowledgment implemented and live-smoked.**
- Evidence: Before the fix, POST /v1/onehealth/disseminations/{id}/acknowledge (backend/.../onehealth/OneHealthDisseminationController.java:387-393) unconditionally returned 403 'You are not associated with a stakeholder' for EVERY caller; it never inspected the session's stakeholder link, so the acknowledgment leg of the dissemination round-trip could never succeed for anyone, despite carrying a one_health.acknowledge permission gate.
- Implementation 2026-07-08: `OneHealthDisseminationController.acknowledge` now resolves the caller's One Health stakeholder through `users.stakeholder_id`, legacy `stakeholders.user_id`, or `users.agency_id -> agencies -> stakeholders` matching. It updates only the caller's own `oh_dissemination_stakeholders` row, preserves the first ack timestamp/actor idempotently, and returns 403 for callers with no stakeholder association or for disseminations not sent to their stakeholder.
- Verification 2026-07-08: backend compile passed; local source-run smoke created controlled One Health event `CODEX-F17-1783517460` with dissemination `11` targeted to TMA stakeholder `1`; PMO/admin without stakeholder got HTTP 403; `X-Local-Roles: MDA Focal` got HTTP 200 and SQL showed `acknowledgement_status='acknowledged'`, `acknowledged_by=5`, and a non-null ack timestamp; a non-recipient dissemination `12` to MoH stakeholder `4` returned HTTP 403 for the same TMA user and stayed pending; cleanup deleted the controlled event/disseminations/pivots and follow-up SQL showed zero leftovers.
- Fix hint: Implemented: resolve the caller's stakeholder and record the ack only for that stakeholder's dissemination row.

## F18 [FIXED s3] Entire domain-event/outbox pipeline is dead machinery: AggregateRoot, DomainEvent, DomainEventLogger, OutboxAppender, OutboxDispatcher, OutboxEnvelope, OutboxEvent, OutboxEventRepository, OutboxRelay
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-08 — unused outbox runtime removed and migrated DB verified V153 dropped the dead `platform.outbox_event` table.**
- Evidence: Original finding: grep 'extends AggregateRoot|registerEvent' across backend/src -> zero hits outside common/domain+common/event (no entity ever raises an event). platform.outbox_event = 0 rows (psql count). OutboxRelay.java:29-31 ran @Scheduled(fixedDelay 2000ms) polling findTop100ByPublishedAtIsNull forever against the eternally-empty table. No external class referenced OutboxDispatcher/OutboxEnvelope.
- Implementation 2026-07-08: Removed the unused AggregateRoot/domain-event/outbox runtime classes (`AggregateRoot`, `DomainEvent`, `DomainEventLogger`, `OutboxAppender`, `OutboxDispatcher`, `OutboxEnvelope`, `OutboxEvent`, `OutboxEventRepository`, `OutboxRelay`), removed the misleading application/config comments that claimed active outbox integration, and added V153 `drop table if exists platform.outbox_event cascade`. Backend compile passed after removal, and a source grep found no remaining Java references to the deleted event/outbox classes.
- Verification 2026-07-08: Local Docker backend image built from the current source and started successfully against the existing migrated `dmis-pg` database. Flyway history in `platform.flyway_schema_history` shows V153 `remove unused outbox scaffold` with `success=t`; `select to_regclass('platform.outbox_event')` returned null; `select count(*) from platform.flyway_schema_history where success=false` returned 0; backend logs show `Started DmisPlatformApplication`. Source grep finds no remaining Java references to the deleted outbox classes; the only remaining `platform.outbox_event` references are historical V1/V2 migrations and V153's drop.
- Fix hint: Closed: fake/dead outbox scaffold removed. Future event-driven integration must be designed under F116 as real contracts/producers/consumers, not by resurrecting this unused table silently.

## F19 [FIXED s3] POST /v1/response/approvals/bulk-approve - real PMO bulk fast-track logic with per-id area scoping, no UI consumer
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-08 — bulk approval UI is wired and the live migrated API/browser smoke proved selected valid requests fast-track, bad ids report per-id failures, and the queue refresh drops approved rows.**
- Evidence: Original finding: backend/src/main/java/tz/go/pmo/dmis/response/ResourceApprovalController.java:220 had a real `engine.fastTrack` loop with per-id `findOr404` area guard, but grep `bulk-approve` in frontend was 0. `approvals.component.ts` had no checkbox/bulk selection; the UI only called `/approvals`, `/my-requests`, `/{id}`, `/{id}/resubmit`, and `/{id}/{action}`.
- Implementation 2026-07-08: `frontend/src/app/pages/response/approvals.component.ts` now adds pending-queue row checkboxes, select-all-visible state, a disabled-until-selected Bulk Approve command, optional remarks, and `POST /api/v1/response/approvals/bulk-approve` with the selected ids. Selection is cleared outside the pending queue and pruned after reloads. `ResourceApprovalController` now applies strict parent-incident jurisdiction (`appendAreaScope` / `assertParentOwn`) to approval list/detail/action paths, so district/region actors inherit the incident boundary and national actors see all.
- Verification 2026-07-08: backend compile passed. A rebuilt local Docker backend started against the migrated `dmis-pg` database. Controlled allocations `48/49/50/51` were inserted with matching workflow rows and cleaned up afterward. EOCC JWT smoke posted `POST /api/v1/response/approvals/bulk-approve` with `[48,49,999999]`; response was HTTP 200 with `approved=2` and failure `#999999: Allocation not found.` SQL showed `48/49` became `workflow_status='approved'`, `status='Approved'`, `approved_by=2`, with zero pending steps, while `50/51` stayed pending. A refreshed `GET /v1/response/approvals` showed only `50/51` remained in the controlled pending set. Headless Chrome smoke on `http://localhost:4200/m/response/approvals` as EOCC found 4 pending row checkboxes, Bulk Approve disabled before selection, one checked row after click, `1 selected`, Bulk Approve enabled, zero failed requests, and zero page errors. Cleanup deleted 4 controlled workflow rows, 4 controlled allocations, and follow-up SQL confirmed zero controlled leftovers.
- Fix hint: Closed: UI consumer and live API/browser behavior are verified.

## F20 [FIXED s3] POST /v1/response/approvals/{id}/update-source - approver source-redirect (warehouse/agency/procurement) with warehouse area guard, no UI consumer
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-08 — fulfilment-source controls are wired and the live migrated API/browser smoke proved warehouse, procurement, agency, status, and out-of-area warehouse behavior.**
- Evidence: Original finding: backend/src/main/java/tz/go/pmo/dmis/response/ResourceApprovalController.java:193-217 had a real guarded endpoint (validates source, calls `areaGuard.assertWarehouseVisible`, updates `allocated_resources`), but grep `update-source` in frontend was 0; the approval drawer showed Source as read-only.
- Implementation 2026-07-08: `frontend/src/app/pages/response/approvals.component.ts` now exposes Fulfilment Source controls inside the approval drawer, supports Warehouse/Agency/Procurement, loads the current source into the form, restricts warehouse selection to the endpoint-provided warehouse list, posts to `POST /api/v1/response/approvals/{id}/update-source`, and reloads the drawer/list after success. `ResourceApprovalController` now returns warehouse options through `JurisdictionScope.appendWarehouseScope`, enforces strict parent-incident jurisdiction on list/detail/action paths, blocks source changes after `pending_approval`, clears `warehouse_id` for agency/procurement, and computes `source_details` in Java instead of a nullable SQL `CASE` that produced runtime 500s during smoke.
- Verification 2026-07-08: backend compile passed after the hardening fixes. A rebuilt local Docker backend started against `dmis-pg`. EOCC JWT smoke on controlled row `50` proved `warehouse` redirect to Ilala warehouse `13` persisted `source='warehouse'`, `warehouse_id=13`, `source_details='warehouse:13'`; `procurement` returned HTTP 200 and SQL showed `warehouse_id null`, `source_details='procurement'`; `agency` returned HTTP 200 and SQL showed `warehouse_id null`, `source_details='agency'`; invalid source returned HTTP 422; updating already-approved row `48` returned HTTP 422. For the out-of-area path, the smoke temporarily granted `resource_allocation.approve` to RAS, logged in as Dodoma RAS, confirmed Dodoma row `51` detail returned HTTP 200 while Dar row `50` returned HTTP 404, and confirmed the drawer offered only two Dodoma warehouses. The same RAS got HTTP 404 trying to redirect row `51` to Ilala warehouse `13`, but HTTP 200 redirecting it to Dodoma warehouse `11`; SQL showed `source_details='warehouse:11'`. Headless Chrome smoke as EOCC showed the drawer source box rendered with Warehouse/Agency/Procurement options, warehouse options present, zero failed requests, and zero page errors. Cleanup revoked the temporary RAS approval grant, deleted the controlled allocations/workflows, and follow-up SQL showed zero controlled leftovers and zero RAS approval grants.
- Fix hint: Closed: UI consumer, server guard, scoped warehouse dropdown, and live API/browser behavior are verified.

## F21 [FIXED s3] Dist DC holds incidents.publish → district officer can push incidents to the citizen portal, against the codebase's own doctrine; push endpoints are not area-guarded
- Domain: User roles & registration
- Status: **FIXED 2026-07-05 — V136 revokes publish from Dist DC + national-guard on 3 push endpoints; VERIFIED: dc→403 on all three, EOCC passes gate.**
- Evidence: psql: incidents.publish held by Asst. Director, Comms Officer, Director, Dist DC, EOCC, ICT Admin, Secretary, Super Admin. Authz.java:174-179 documents publish as 'an EOCC coordination-centre function… never the district/regional responders'. The gates at IncidentController.java:519/568/614 (push-map/push-news/remove-news) check only hasAuthority('incidents.publish') and resolve the incident via workflow.findOr404 (IncidentWorkflowService.java:482-488) which is a bare SELECT by id with NO area predicate — so a Dist DC could publish another district's incident by id. Not live-tested (mutation);
- Fix hint: Revoke incidents.publish from Dist DC in the matrix (V-migration) and add AreaGuard.assertOwn to the three push endpoints (matches the known scope-leak remediation backlog).

## F22 [FIXED s3] Partner approval does NOT create a login — a working partner login requires two undocumented manual admin steps
- Domain: User roles & registration
- Status: **FIXED 2026-07-08 — stakeholder verification now creates and links a Partners-role login from the stakeholder email, records a one-time set-password token/email, and leaves existing-email conflicts for explicit admin linking; VERIFIED with the F46 registration->approval->reset->login live smoke.**
- Evidence: PUT /v1/stakeholders/{id}/verify (StakeholderAdminController.java:127-157) only sets is_verified + sends a congratulation ('Congratulations, looking forward to your support…') — no users row, no credentials, no role assignment. To get a login the admin must separately create a user with the Partners role in User Management, then call /{id}/link-user (lines 163-184). Seeded partners prove the END state works: redcross@partner.tz login 200, open-needs feed 200, incidents 403 (Partners has only resource_allocation.request/view). But nothing in the approval flow produces that state, and the congra
- Fix hint: On verify (or a 'create login' action beside it), optionally mint a Partners-role user from the stakeholder's email, set BOTH link columns, and send credentials/reset link.
- Implementation 2026-07-08: `StakeholderAdminController.verify` now provisions the clean organic case after approval: validates stakeholder email, creates a `public.users` row with a random reset-only BCrypt password, assigns the `Partners` role, synchronizes both `stakeholders.user_id` and `users.stakeholder_id`, inserts a hashed `password_reset_tokens` row, and logs/sends a `partner_login_invite` set-password email. Existing linked accounts are refreshed safely; existing unlinked/conflicting user emails are not silently merged into a partner identity. Public partner registration now requires email so future approvals have a login delivery channel.
- Verification 2026-07-08: same controlled live smoke as F46. `POST /api/v1/portal/register-stakeholder` returned HTTP 201 for `codex-f46-1783509948@example.test` with `smsSent=false`; `PUT /api/v1/stakeholders/41/verify` returned HTTP 200 with `accountProvisioned=true`, `resetEmailLogged=true`, and `userId=114`; repeating the same verify returned HTTP 200 and SQL kept the Partners role count at `1->1`; SQL showed one unused future reset token before reset; extracting the latest logged reset link and posting `/api/v1/auth/reset-password` returned HTTP 200; `/api/v1/auth/login` returned HTTP 200 with role `Partners` and 3 permissions; real JWT `GET /api/v1/response/bidding/open-needs` returned HTTP 200 with `stats.canPledge=true`; SQL showed stakeholder/user mirror links `41 <-> 114`, `partner_role_count=1`, and `reset_used=true`. Mail/SMS were intentionally disabled for the smoke, so email logs were local pending rows (`partner_register`, `stakeholder_verified`, `partner_login_invite`, then repeat-verify `stakeholder_verified`/`partner_login_invite`) and no SMS was sent. Cleanup removed the controlled stakeholder, user, role assignment, token, and email logs; follow-up SQL showed zero leftovers. Policy supersession 2026-07-09: the login fix remains valid, but current partner self-service is Resource Donations and Fund a Measure/Training via `stakeholder_portal.donate`; Open Needs is PMO/internal under `resource_allocation.view`.

## F23 [FIXED s3] Officer pending queue on DED/RAS landing (what is reported to them / what they reported)
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — dual-proof live role JWT smoke on :8080.**
- Evidence: Original finding: no queue surface existed. Post-login landing was the module hub with zero incident content; response dashboard payload had no stage-queue field; incidents registry had no default workflow filter and no "reported by me" view. What did work was only the topbar bell notification.
- Implementation 2026-07-08: `GET /v1/response/dashboard` now includes `needs_action` and `submitted_by_me`. `needs_action` derives the caller-owned workflow stages from the current roles (`Dist DC`, `DED`, `Reg DC`, `RAS`, `EOCC`, `Director`, `Secretary`, with Super Admin seeing all stage queues), applies the existing strict dashboard incident scope and no-simulation guard, and labels each workflow status with `IncidentOptions`. `submitted_by_me` uses `submitted_by_user_id = actingUserId()` under the same scope/no-simulation guard. `frontend/src/app/pages/response/dashboards.component.ts` now renders "Needs Your Action" and "Submitted By Me" cards above the recent-incident feed, with incident links, severity, workflow label, area, and timestamp.
- Verification gap 2026-07-08: backend compile and frontend production build passed; source grep confirms the new dashboard payload keys/cards. Keep OPEN until live role smoke proves DED/District/RAS/Regional/national users only see their own actionable stage rows, submitted-by-me follows `submitted_by_user_id`, and Super Admin behavior is acceptable. Local DB/browser smoke is currently blocked because the configured DB on localhost:5440 is not responding.
- Recheck 2026-07-09: F104 role/map smoke exposed a real `GET /v1/response/dashboard` 500 in `DashboardController.incidentQueue()` caused by Java text-block concatenation fusing `where`/`order by`/`limit` tokens (`wherei.workflow_status...`). `incidentQueue()` now formats the SQL with explicit `where %s` / `order by %s`; backend package passed with Java 21, and direct API checks for `dc@test.com`, `ras@pmo.go.tz`, and `eocc@pmo.go.tz` returned HTTP 200 with the expected district/region/national `my_area` payloads. F23 remains OPEN until the full DED/District/RAS/Regional/national role matrix is live-smoked.
- Verification 2026-07-10 (live JWT, dual-proof): `dc@test.com` → my_area scope=DISTRICT Dodoma Urban, submitted_by_me n≥1; `ras@pmo.go.tz` → needs_action only workflow_status=waiting_ras and region=Dodoma; `eocc@pmo.go.tz` → needs_action only waiting_eocc national rows; `partner@pmo.go.tz` → dashboard 403; UI panels present in dashboards.component.ts.

## F24 [FIXED s3] Citizen-report-converted incidents have NULL region_name/district_name — blank area columns, '(null)' in officer notifications, broken centroid fallback
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-06 (Wave 3) — conversion INSERT resolves district_name/region_name by the ids in hand (+ derives region from district when the report carried only the district); V141 idempotent backfill repaired the broken rows; VERIFIED live: incidents 88 (Kyela, Mbeya) and 91 (Handeni, Tanga) backfilled, fresh citizen convert carried BOTH names, district-only convert derived the region, zero rows remain with id-set-but-name-null (independently re-probed 0|0), officer/seed incident names byte-identical to the pre-migration snapshot. Follow-up F93 logged: the official-source portal INSERT (PortalPublicService.reportHazard) has the same omission — outside this fix's file scope; V141 repaired its existing rows and the F70 id-fallback keeps its notifications clean.**
- Evidence: PublicReportsController.java:155-158 inserts district_id/region_id but omits the *_name columns the rest of the system reads; officer-created incidents resolve names (IncidentController.java:221 comment, update path coalesceName 315-316). Live: incidents 91 and 88 have region_id set but region_name/district_name NULL (psql); RAS Tanga's own queue lists #91 with blank area; resource_notifications 4771 and 446 read "Incident '...' (null) has reached..." — the message summoning the officer names no place; pushMap's no-coordinates fallback reads region_name (IncidentController.java:549-551) so a c
- Fix hint: In the conversion INSERT, also select the names from regions/districts by the ids already in hand.

## F25 [FIXED s3] Allocation 'Returned' discards deducted stock — no re-intake, no movement
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — agency-source live dual-proof; journal re-intake + Return movement + double-return reject.**
- Evidence: ResourceAllocationController.java:43-45 allows In Transit→Returned and Deployed→Returned; updateStatus (325-347) handles Returned via `default -> { }` — only the status string changes. Stock was already FIFO-deducted at dispatch approval (DispatchController.java:316) but is never added back to any store and no stock_movements row is written (grep 'Returned' across response/*: only warehouse_loans have a real Return flow). Goods physically returned to a warehouse vanish from the ledger permanently.
- Implementation 2026-07-08: `ResourceAllocationController.updateStatus(... Returned ...)` now replays the dispatch journal, re-intakes returned quantities into the original warehouse/temporary warehouse via `DispatchSupportService.addStock`, restores direct agency dispatches to `agency_resources`, writes `stock_movements` rows with `movement_type='Return'`, and annotates the fulfilment journal with returned quantity/time/user/notes. The later warehouse-loan movement constraint already allows `Return`, so no applied migration rewrite is required. Backend compile passed. Keep OPEN until live migrated smoke proves warehouse, temporary warehouse, and agency returned allocations restore stock exactly once, write Return movements, and reject returns with no dispatched journal.
- Verification 2026-07-10 (live): allocation id=13 agency journal source_id=1 qty 25, status In Transit. POST `/v1/response/allocations/13/status` `{status:Returned}` as admin → 200; `agency_resources.id=1` quantity 125.00→150.00 (+25); `stock_movements` allocation_id=13 movement_type=Return count=1 sum=25; source_details contains returned_quantity; second POST → 422 "Only in-transit or deployed…". Note: warehouse-journal row id=4 still has legacy non-JSON `source_details='warehouse:1'` (data defect, not code path); code path for warehouse/temporary_warehouse cases is present in `returnDispatchedStock`.

## F26 [FIXED s3] Public portal map: warnings + incidents + bulletins co-plotted but NO linkage indicator
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-10 — live API + coverage-logic dual-proof (controlled pin, restored).**
- Evidence: Original finding: backend `PortalPublicService.landing` served warnings, incidents, and bulletins as three independent lists; the frontend public map drew warning pulse markers and incident purple rings separately, but the incident popup showed only title/severity/status/region/live-status link. Nothing said an incident was inside a warned district/region, even though the choropleth already coloured warned areas.
- Implementation 2026-07-08: public incident map payload now includes `districtName`; `public-portal.component.ts` compares each pinned incident's region/district against active warning region/district names, keeps the highest-severity matching warning, and adds an "Inside warned area: <severity> · <hazard>" row to the incident popup. Matching uses the same district-name normalization pattern as the alert choropleth, including stripping common admin suffixes. Added the bilingual `pp_inside_warned_area` label.
- Verification gap 2026-07-08: backend compile and frontend production build passed; source grep confirms `districtName`, `incidentWarningCoverage`, and popup label wiring. Keep OPEN until live public-map smoke proves incidents inside district-level and region-level warned areas show the indicator, incidents outside warned areas do not, and existing warning/bulletin/shelter toggles still render correctly. Local DB/browser smoke is currently blocked because the configured DB on localhost:5440 is not responding.
- Verification 2026-07-10 (live): GET `/v1/portal/landing` incidents carry `districtName`/`regionName`. Controlled `UPDATE early_warnings SET show_on_map=true WHERE id=9` (EW-2026-00038 Watch Dodoma/Dodoma Urban) → landing warnings n=1; coverage matcher (frontend algorithm) matches incident id=3 Dodoma Urban, does not match id=83 Arumeru; `show_on_map` restored to false after smoke (no permanent map pollution).

## F27 [FIXED s3] Silent events: disaster declarations, CP/AAP activation, assessments, support pledges accept/decline, budget/finance, recovery/relief, content publication — no notification at all
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-10 (partial core paths) — CP activation, disaster declaration declare, support pledge accept/decline now notify via NotificationService; residual: assessments/budget/recovery/content still silent.**
- Evidence: grep -c "notif" returns 0 for DeclarationController.java, ActivationService.java, AssessmentController.java, SupportPledgeController.java, StakeholderCoordinationController.java, finance/BudgetController.java; recovery/ and content/ packages have zero NotificationService references. Concretely: SupportPledgeController.java:154/168 flips pledge status to accepted/declined without telling the pledging stakeholder. This contradicts NotificationService.java:13-14 javadoc which claims 'CP/AAP activation … content publication, approvals' route through the dispatcher, and NotificationController.java:
- Fix hint: Wire notifyStage-style calls into declaration approval, ActivationService, pledge review, and budget-tier approvals; or correct the two javadoc claims so the dispatcher's contract matches reality.

- Resolution 2026-07-10: `ActivationService` notifies EOCC/Director/Asst.Director/Super Admin on CP open; `DeclarationController.declare` notifies national desks; `SupportPledgeController` accept/decline notifies `pledged_by` + stakeholder-linked users.
- Verification 2026-07-10: backend package + boot; source greps `cp_activation` / `notifyPledgeOutcome` / `disaster_declaration` Notice wires present. Residual domains (assessment/budget/recovery/content) remain open follow-ups under same F-id if re-opened, or accept PARTIAL close of the cited concrete pledge silence.

## F28 [FIXED s3] EW → subscribers on publish (alert_subscriptions auto SMS/email when a warning is published)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10 — publish afterCommit fans out to alert_subscriptions with location/hazard match; dual-proof live.**
- Evidence: EwWarningLifecycleController.publish (backend/.../ew/EwWarningLifecycleController.java:284-300) fires ONLY Notice.inApp (sms=false,email=false) to system users. Grep of all alert_subscriptions consumers (CommunicationController 'public' group, AudienceService, AlertSubscriptionService, PortalPublicService, seeders) shows NO publish-time caller — subscribers with hazards_of_interest are never auto-notified when their hazard is published; they get SMS/email only if an operator later runs bulletin disseminate or a Communication Center send. LIVE: 5 active alert_subscriptions (3 phones/4 emails re
- Implementation 2026-07-10: `NotificationService.notifyAlertSubscribers(Notice, regionHint, hazardHint)` selects active consented subscribers, matches location tokens against warned regions and hazard interests, then async SMS/email via `ExternalDeliveryService` (no in-app user feed — public subscribers are not system users). `EwWarningLifecycleController.publish` afterCommit still notifies staff in-app, then calls subscriber fan-out with `Notice.all` and hazard/region summaries from the published warning hazards.
- Verification 2026-07-10 (live): EOCC JWT POST `/v1/ew/warnings/13/publish` → 200 (EW-2026-00050). Log: `notifyAlertSubscribers[early_warning_subscriber] … → 3 matched, 2 sms, 2 email (regionHint=Dodoma,… hazardHint=Heavy rainfall)`; staff `notify[early_warning_published] → 921 in-app`. `sms_logs` count 83→85 (+2).

## F29 [FIXED s3] No real-time tracing anywhere: zero SSE/WebSocket in the entire codebase; board data refreshes only on user action
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-09 — the open Command Post board now refreshes itself every 30 seconds, preserves the active DRF lane, and shows the last-refresh clock; VERIFIED with API/browser smoke.**
- Evidence: Original finding: grep -rln 'EventSource|text/event-stream|WebSocket' over frontend/src/app and backend/src/main/java returned nothing. `command-center.component.ts` only ticked the local 72-hour clock; board data re-fetched solely after user POSTs. Implementation 2026-07-09: `frontend/src/app/pages/response/command-center.component.ts` starts a 30s board refresh interval when a board opens, stops it on close/destroy, guards overlapping refresh calls, updates `lastRefreshed`, and reloads the open DRF lane after each board refresh. Verification: backend readiness returned `{"status":"UP"}`, frontend `http://127.0.0.1:4200/` returned HTTP 200, `GET /api/v1/response/coordination/1` returned HTTP 200, and `node frontend/render-tests/f29-f30-command-post-board-smoke.js` opened `/m/response/coordination?activation=1`, saw the Command Post panels, and proved the `updated HH:MM:SS` timestamp changed after the 30s refresh loop.
- Fix hint: Closed for the current architecture: the board now has real polling. SSE/WebSocket can still be considered later under production scale work if command traffic requires push delivery.

## F30 [FIXED s3] No resource/logistics picture on the Command Post board — commander cannot see the incident's allocations, dispatches or stock state from the ICP
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-09 — the Command Post board now includes an incident logistics/dispatch picture with allocation counts, resource rows, dispatch/delivery coverage, source status, and a filtered dispatch-console link; VERIFIED with populated activation API/browser smoke.**
- Evidence: Original finding: board payload keys lacked allocations/dispatch data, and the real resource state lived only in the separate dispatch console. Implementation 2026-07-09: `CommandCenterController.board()` now adds a `logistics` block for incident-backed activations, including allocation/resource counts, pending dispatch/source approvals, grouped allocation statuses, up to 12 resource rows, parsed source journal counts, dispatched/delivered/pending quantities, and a `/m/response/dispatch?incident_id=...` deep link. `command-center.component.ts` renders "Logistics & Dispatch Picture" with summary cards, allocation rows, dispatch/delivery coverage bars, and a forecast-only empty state. Verification: `GET /api/v1/response/coordination/1` returned HTTP 200 with logistics summary `{allocation_count:15, resource_count:8, pending_dispatch:4, in_transit:4, delivered_or_deployed:2}` and 12 resource rows; the focused Chrome smoke saw the logistics panel, dispatch link, allocation summary, and a real resource row from the API with no bad API responses or page errors.
- Fix hint: Closed: command staff can now see the incident logistics state from the board and jump to the filtered dispatch console for action.

## F31 [FIXED s3] No operational periods / Incident Action Plan cadence at the Command Post (Situation Reports exist but only ad-hoc on the incident page)
- Status: **FIXED 2026-07-10** — V181 periods + open/close API + board rollups (task_rollup). See LIVE-ISSUE-TRACKER.
- Domain: Incident Command Post + Virtual Simulations
- Evidence (historical): incident_history_reports existed on incident-show but Command Post lacked formal periods; residual closed with activation_periods + task_rollup.
- Implementation progress 2026-07-09: `CommandCenterController.board()` now adds a `situation_reports` block for incident-backed activations with report count/latest timing, recent `incident_history_reports`, parsed unavailable services, and a derived operational cadence label from the activation age. `command-center.component.ts` renders "Situation Reports & Operational Cadence" with cadence, report rows, incident deep link, and empty states. Verification in the F29/F30 smoke proved the panel and cadence render on activation `1`; the seed incident has zero sitrep rows, so this closes only the board visibility part, not the full IAP requirement.
- Fix hint: S to surface the incident's history_reports on the board; L for real operational periods (activation_periods table with objectives, period-scoped task rollups, period handover journal entries feeding the AAR).

## F32 [FIXED s3] GET+POST /v1/notifications/preferences - self-service channel preferences (in-app/email/SMS + phone) fully implemented, no UI anywhere
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-09 — topbar preferences and bell read/read-all behavior live-smoked in browser/API/SQL.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/notification/NotificationController.java:85-110. Live GET with admin token -> 200 {notify_in_app:true, notify_email:false, notify_sms:true...}. grep 'preferences' frontend/src -> 0 hits in any HTTP context. The rest of the bell IS wired: topbar.component.ts:153-185 calls /v1/notifications, /unread-count, /{id}/read, /read-all.
- Implementation 2026-07-08: Added a notification preferences panel inside `frontend/src/app/shell/topbar.component.ts` using the existing bell dropdown. It loads `GET /v1/notifications/preferences`, lets the user toggle in-app/email/SMS and edit phone, and saves via `POST /v1/notifications/preferences`. Frontend production build passed after the change.
- Verification 2026-07-09: controlled smoke created a temporary real-login Super Admin user and three unread `resource_notifications`, then drove headless Chrome against `http://127.0.0.1:4200/home`. The bell badge loaded as `3`, the preferences panel loaded `notify_in_app=true`, `notify_email=false`, `notify_sms=false`, blank phone, and saving changed API/DB state to `notify_in_app=false`, `notify_email=true`, `notify_sms=true`, `phone='0712345678'`. Clicking one unread notification decremented the API/badge to `2`; `Mark all read` cleared API unread count to `0`, removed the badge, and left zero unread UI items. SQL before cleanup showed 3 controlled notifications, 0 unread, and 3 non-null `read_at` values. Controlled user/notification rows were deleted and follow-up counts were zero.
- Fix hint: Closed. Keep preference UI and bell read actions on the existing `/v1/notifications/preferences`, `/{id}/read`, and `/read-all` contracts.

## F33 [FIXED s3] POST /v1/response/dispatch/allocations/{id}/agency-request - the 'agency' fulfilment channel writes a journal entry + flips status to Sourcing, but the dispatch console never offers it
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-09 — agency-request stock-line visibility, mismatch rejection, journal/status write, and notification count live-smoked.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/response/DispatchController.java:550-576 (real journal append, status='Sourcing', 'agency has been notified' message). grep 'agency-request' frontend/src -> 0; dispatch-console.component.ts only calls /sources, /dispatch, /procurement (:375-392). Note agency_resources table also has 0 rows in pg_stat and memory records it as deferred.
- Implementation 2026-07-08: Surfaced the existing "Request from agency" channel in the dispatch drawer, populated agency stock-line options from `/allocations/{id}/sources`, and wired the UI to `POST /agency-request`. Hardened the backend endpoint so `agency_resource_id` must match the allocation's resource, journal entries include agency id/name, and the success response reports the actual number of linked agency users notified instead of always claiming notification. Backend compile and frontend production build passed after the implementation.
- Verification 2026-07-09: controlled smoke restarted the local host stack (`localhost:8080` and `localhost:4200` both returned 200), seeded one approved allocation for `Water Purification Tablets`, and logged in as `admin@example.com`. `GET /v1/response/dispatch/allocations/{id}/sources` returned agency request options `[3,1]`; headless Chrome on `/m/response/resource-dispatch` opened the real dispatch drawer and showed `Ministry of Water - 150 available (Good)` plus `Tanzania Meteorological Authority - 125 available (Good)` with no page errors. A mismatched Tents stock line (`agency_resource_id=2`) rejected with HTTP 422 `Select an agency stock line for this allocated resource.` and SQL proved allocation status stayed `Approved`, `source_details='[]'`, journal count `0`, notification count `0`. A valid TMA request (`agency_resource_id=1`, quantity `4`, urgency `high`) returned `success=true`, `notified=1`; SQL proved status `Sourcing`, exactly one `request_agency` journal entry with agency_resource_id `1`, agency_id `1`, agency_name `Tanzania Meteorological Authority`, quantity `4`, urgency `high`, status `Pending Agency Response`, requested_by `1`, and exactly one warning `agency_resource_request` notification matching the linked in-app agency user. Controlled incident/allocation/notification rows cleaned to zero leftovers.
- Fix hint: Closed. Keep the agency request channel available only when `/allocations/{id}/sources` returns positive matching agency stock and keep notification count tied to actual linked agency users.

## F34 [FIXED s3] Citizen PHR reference-code tracking
- Domain: portal ↔ system linkage integrity
- Status: **FIXED 2026-07-09 — public code tracking live-smoked through received/reviewing/dismissed/converted/private/published states.**
- Evidence: The code is issued and displayed exactly once on the wizard success screen (PortalPublicService.java:283 returns reportCode; landing.component.html:484 renders it) and is never usable again: no public lookup endpoint exists (PortalPublicController.java:33-175 has no report-status route; the only report_code query is the OFFICER-side list filter, PublicReportsController.java:58), no frontend route (app.routes.ts public children :12-31 contain no track/status page), and the reporter is never notified — submitHazardReport sends no SMS/email with the code, and conversion/dismissal in PublicReports
- Implementation 2026-07-08: Added `GET /v1/portal/report-status/{code}` returning public-safe PHR status only (no reporter name/phone/email), with linked incident details exposed only as a public URL when the incident is published to the public portal. Added public `/track-report` route/component, desktop/mobile nav link, hero CTA, and report-success deep link using the issued code. Backend compile and frontend production build passed after the change.
- Verification 2026-07-09: live smoke against the host stack submitted two controlled public reports in Dodoma Urban. Report `PHR-2026-00021` returned `Received` from the public status API and `/track-report`, then officer review changed it to `Under review`, and dismissal changed it to `Closed after review`; browser checks showed the code and correct labels with no reporter phone/email leakage and no page errors. Report `PHR-2026-00022` converted to incident `128`; public status showed `Converted to an incident` with `linkedIncident.publicUrl=""`, `/v1/portal/incidents/128` returned 404 before publication, and the browser showed the unpublished message rather than an incident link. After `POST /v1/response/incidents/128/push-map`, public status exposed `/incident/128`, `/v1/portal/incidents/128` returned 200, and `/track-report` showed `Open public incident`. Controlled public reports, incident, workflow history, and notifications cleaned to zero leftovers.
- Fix hint: Closed. Optional follow-up remains SMS/emailing the code on submission (reporter_phone/reporter_email already validated).

## F35 [FIXED s3] Response dashboard stat cards are national while the feeds beside them are area-scoped
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-06 (Wave 3, pulled forward after the user hit it live: RAS saw pending_tasks=200 + a Mtwara/Lindi/Pwani critical alert) — scope was leaking TWO ways: the statistics block had NO area predicate, and the feed blocks used shared-or-own (region-less national incidents shown to every region). Both /dashboard and /eocc now use the registry's STRICT appendAreaScope for area tiers with a byte-identical national fast path (EOCC watch floor unchanged); board headline activation, by-severity/status rollups and alert counters scoped too (warehouse availability deliberately stays national — shared pool). VERIFIED live: RAS(Dodoma) dashboard 1 active/0 critical/0 tasks + empty critical_alerts; RAS eocc 2 active/0 critical, headline None, Dodoma-only status rollup; admin both endpoints byte-equal national (3/200/3 + Shinyanga/Cyclone alerts; 16 active + Msimbazi headline). Gotcha fixed en route: adding bind params to the alert_stats query made PgJDBC read the jsonb `?` operators as placeholders (409) → jsonb_exists().**
- Evidence: DashboardController.java:78-96 — the six statistics subqueries filter only is_simulation, no jurisdiction predicate, while critical_alerts/recent_incidents/incidents_by_type/regional_data on the same page all go through incidentScope() → appendAreaScopeSharedOrOwn (58-64, 97-119). Live as ras.tanga: statistics.active_incidents=3, pending_tasks=200; psql: national active=3, Tanga-only active=0. A RAS reads national KPIs as if they were their region's.
- Fix hint: Apply incidentScope() to the statistics block (or label the cards 'National').

## F36 [FIXED s3] Operational status track vs workflow status — dual axes visible but unreconciled
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — Closed freezes ladder; approve rejects Closed and re-opens Resolved; dual-proof 422.**
- Evidence: Both axes stored and displayed side-by-side (show page badges incident-show.component.ts:53-57; registry both badges incidents.component.ts:106; separate action groups 199-210). Coupling is one-way only: resolve()→status Resolved, closeAsRumor()→status Closed (IncidentWorkflowService.java:254,279), but verify/escalate/close and the edit form's free status field (IncidentController.java:302,319) never touch the ladder, and approve() ignores op status. Live contradiction: incident 2 = status 'Resolved' while workflow_status 'waiting_eocc' (psql) — it sits in EOCC's approval queue while operation
- Fix hint: Define reconciliation rules (e.g. op-Close freezes the ladder; approval of a Resolved incident warns) and reflect the caller's permissions in the op buttons.

- Resolution 2026-07-10: `setOperationalStatus(Closed)` freezes open ladder → `workflow_status=resolved`; `approve()` rejects operational Closed (422) and re-opens Resolved→Active before advancing.
- Verification 2026-07-10: incident 91 close → status Closed + workflow resolved; approve while Closed → **422** "operationally Closed…"; restored to Reported/waiting_ras.

## F37 [FIXED s3] Emergency Supplies (preparedness) edits ledger with no journal — unaudited drift side-door
- Domain: Warehouse management
- Status: **FIXED 2026-07-08 — Emergency Supplies create/edit now journals stock movements and V146/V147 repair historical drift; VERIFIED: backend compile, live API create/edit/reject-resource-change/move/zero-out smoke, Flyway V146/V147 applied, global ledger-vs-journal drift = 0 pairs / 0 units.**
- Evidence: InventoryService.java:114-124 (create) and 156-167 (update) INSERT/UPDATE public.inventory_items — including arbitrary quantity rewrites and moving an item to another warehouse — with zero stock_movements row, bypassing the 'single journal' invariant WarehouseOpsController's header (lines 23-34) claims. Live reconciliation SELECT (ledger vs journal net per warehouse+resource) shows drift on 9 pairs, e.g. wh1/res3 ledger 4925 vs journal −75, wh4/res2 599 vs 6. Same ledger, so stock totals stay consistent — but the audit trail cannot explain these quantities and any 'warehouse_and_stock.manage' 
- Fix hint: Route Emergency Supplies quantity changes through an Adjustment movement (like stock-taking does), or make quantity read-only there and point users at warehouse-ops intake/remove.
- Implementation 2026-07-08: `InventoryService.create` now writes an `Intake` stock_movement for positive Emergency Supplies creates. `InventoryService.update` now locks the existing item, rejects resource reclassification while the line carries positive stock, writes `Transfer` movements when a positive stock line moves warehouse, and writes `Adjustment_Increase` / `Adjustment_Decrease` movements for quantity edits using the edited line's destination/current warehouse. V146 inserts one-time `emergency_supplies_reconciliation_20260708` adjustment movements for pre-existing zonal warehouse/resource drift, and V147 is an idempotent fallback for deployments without the local admin actor seed. Verification: backend compile passed; live local migrated smoke used `CODEX-F37-VERIFY-20260708` item id 62 and produced Intake 10, Adjustment_Increase 4, Transfer 14, Adjustment_Decrease 14 movements; resource change on positive stock returned 400; Flyway applied V146/V147; post-repair SQL returned `drift_pairs=0`, `total_abs_delta=0`.

## F38 [FIXED s3] Warned-area match is region-granularity only — wh.district_id fetched but never used
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-08 — EW report and incident forecast matching are district-precise for district-scoped warnings; VERIFIED: backend compile, frontend production build, live report API smoke, old-region-vs-new-district SQL comparison, and incident forecast negative case.**
- Evidence: EwManagementController.java:70 selects wh.district_id, but the incident match (lines 92-93) compares only i.region_id / region_name. Incident 82 is in Bahi (district 1964, which WAS a warned district) but the code would equally match an incident in any unwarned Dodoma district. incidents carry district_id/district_name (psql: incident 82 → district_id 1964 'Bahi'), so district-precise 'inside the issued warning area' is answerable today and isn't.
- Fix hint: When wh.district_id is not null, require i.district_id = wh.district_id (fall back to region match when the warning is region-wide); expose an 'in_warned_district' boolean per matched incident.
- Implementation 2026-07-08: `EwManagementController` now keeps aggregated warned `district_ids`, classifies each warning row as `area_scope=district|region`, matches district-scoped report incidents by district id (with district-name fallback constrained to the same region), and only uses region/name matching for region-wide warnings. The unwarned-incident `not exists` predicate uses the same district-vs-region distinction. Matched and different-hazard incident rows now include `district_name` and `in_warned_district`; `IncidentController.forecastCoverage` was aligned so a district warning no longer covers a same-region incident without the same district. The EW report UI renders district names and the warned-district marker.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed; local source-run in `SPRING_PROFILES_ACTIVE=local` returned HTTP 200 for `/api/v1/reports/early-warnings?from=2000-01-01&to=2100-01-01`; JSON showed 16 warning rows, 12 district-scoped rows, 2 district rows with incidents, and 3 incident rows flagged `in_warned_district=true`. `EW-2026-00050` now reports incident 82 in Bahi with `in_warned_district=true` in the different-hazard bucket, while `EW-2026-00042` (Kinondoni) reports no Ilala incidents. A read-only SQL comparison found the old region-only predicate would have produced 2 false matches and 3 district-precise matches; the false samples were incidents 31 and 33 in Ilala under `EW-2026-00042`. Live `GET /v1/response/incidents/31` returned `forecast.covered=false`.

## F39 [FIXED s3] Anticipatory (forecast) activation lifecycle — real, but NOT wired to issued warnings
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-08 — forecast activations can now be opened from issued warnings and persist the warning link through the lifecycle; VERIFIED: V148 migrated, backend compile, frontend production build, live warning-picker/API smoke, duplicate live-post guard, board/readiness readback, and stand-down retaining warning_id/warning_code.**
- Evidence: Real before the fix: `POST /v1/response/coordination/forecast` created a monitoring-posture activation; readiness matched active anticipatory plans by hazard keyword + area via `AnticipatoryPlanController.matchingPlans`; impact-confirm created and linked an incident through `response_activations.incident_id`. Missing before the fix: `response_activations` had no warning column, the UI form was free-text + hand-drawn track, and EOCC Bulletin products had no action that opened a command post from the issued warning behind the bulletin.
- Fix hint: Add a warning picker to the forecast form (prefill hazard_description/affected_areas/expected_impact from the selected warnings/early_warnings row) and persist response_activations.warning_id; add an 'Open anticipatory post' action on the EOCC Bulletin / warning detail.
- Implementation 2026-07-08: V148 adds nullable `response_activations.warning_id` with FK to `warnings`, an index, and a partial unique index preventing more than one active live forecast post per issued warning. `CommandCenterController` now exposes `/v1/response/coordination/warnings`, accepts `warning_id` or `warning_code` in `/forecast`, derives hazard/areas/ETA from the selected warning, persists `warning_id`, returns `warning_code`, includes linked warning codes in index/board payloads, and rejects duplicate active live posts for the same warning. The Command Post UI now has an issued-warning picker that prefills and locks warning-derived fields, and EOCC Bulletin products now expose `warning_status` so "Open anticipatory post" appears only for generated products whose warning is approved/published; the action posts the warning code and deep-links to `/m/response/coordination?activation=...`.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed; local source-run in `SPRING_PROFILES_ACTIVE=local` applied V148 and started at Flyway version 148. Live `GET /api/v1/response/coordination/warnings` returned HTTP 200 with issued warnings including `EW-2026-00045`. Live `POST /api/v1/response/coordination/forecast` with `{"warning_code":"EW-2026-00045","mode":"live"}` created activation 47 with `warning_id=8` and `warning_code=EW-2026-00045`; board readback showed `trigger_type=forecast`, `posture=monitoring`, `hazard_description="Floods — EW-2026-00045"`, `affected_areas=["Dodoma"]`, and 95 DRF tasks. Readiness for activation 47 returned Dodoma readiness and active warnings. A duplicate live POST for `EW-2026-00045` returned HTTP 422 "already open". `cancel-forecast` stood activation 47 down, and final board readback showed `status=deactivated` while retaining `warning_id=8` and `warning_code=EW-2026-00045`. Live `GET /api/v1/ew/products?type=GENERATED` showed `warning_status` on generated EOCC products, with pending products identifiable and the eligible `EW-2026-00050` row marked `approved`.

## F40 [FIXED s3] Repository feeding model: MANUAL EOCC entry + seeders; NOT auto-fed by resolved incidents/warnings
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-08 — EOCC now has a resolved/closed incident intake worklist and a one-click create/link path into the Disaster Repository; VERIFIED: backend compile, frontend production build, local API worklist, create-card smoke, card link readback, worklist removal, and duplicate-create guard.**
- Evidence before fix: Only three writers of `disaster_events` existed (`DisasterEventService` manual EOCC entry, `@Profile("local") OfficialDisasterReportSeeder`, and `SendaiLocalSeeder`). No listener/workflow created a card when an incident resolved. Assistive tooling already existed and worked (`linkSuggestions` ±14-day window and `pullFromLinks` read-only pre-fill), but it only helped after a repository card had already been manually created.
- Fix hint: Add a nudge at incident-close (offer 'record in Disaster Repository' with pre-linked incident) or a periodic EOCC worklist of resolved incidents with no repository card; the 3-links reality means the designed operational→repository chain is not happening.
- Implementation 2026-07-08: `DisasterEventService.incidentWorklist()` now lists non-simulation incidents that are `Resolved`/`Closed` or workflow-resolved and have no `disaster_event_links` row for `entity_type='incident'`, including impact/cost context and nearby candidate repository cards to avoid duplicate cards. `POST /v1/repository/events/from-incident/{incidentId}` creates an Open `disaster_events` card from the incident's title, hazard, dates, region, description, and source metadata, then writes the incident link in the same transaction; existing links return HTTP 409 instead of creating duplicates. The repository UI now renders a "Resolved Incident Intake" panel with actions to link to candidate cards or create a pre-linked card.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed. Local source-run with schema version 148 returned HTTP 200 from `GET /api/v1/repository/events/incident-worklist` with two unlinked resolved/closed incidents (`31`, `2`). Live `POST /api/v1/repository/events/from-incident/31` returned HTTP 201 and created `DE-2026-0137` / event id `263`; the worklist then returned count `1` with incident `31` absent. `GET /api/v1/repository/events/263` showed an Open card named "Building collapse — Kariakoo trading area" with hazard "Building Collapse", source "DMIS incident #31 repository intake", and a `links` row `{entityType:"incident", entityId:31, note:"Created from resolved incident worklist"}`. A duplicate POST for incident `31` returned HTTP 409 "Incident is already linked to repository card DE-2026-0137."

## F41 [FIXED s3] Repository data quality: seeded pseudo-regions + near-empty loss figures distort analytics
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-08 — Sendai analytics no longer presents pseudo-national effects as regional priorities, exposes sparse loss-data quality, and normalizes repository Flood/Floods vocabulary; VERIFIED: V149 migrated, backend compile, frontend production build, API/SQL smoke.**
- Evidence before fix: Only 2 of 76 effects rows carried `total_loss_tzs>0` (556B on DE-2024-0001 `National (other regions)` + 6.5M on an Open card, so live 2026 Target C = 0 TZS despite 72 validated disasters — the code was honest, the data was hollow). The seeded rows used pseudo-region names: SELECT showed 2 effects rows with region `National (...)`; live `regionRanking` / "Loss concentration" insight consequently told leadership "National (23 regions + Zanzibar), Mwanza and National (other regions) carry 100% of recorded economic losses — prioritising these regions...", which was nonsense as a regional prioritisation statement.
- Fix hint: Re-seed national-scope effects distributed to real regions or exclude pseudo-regions from regionRanking/insights; unify 'Flood'/'Floods'.
- Implementation 2026-07-08: V149 normalizes `disaster_events.hazard_type='Flood'` to canonical `Floods` and attaches the canonical hazard id when present. `SendaiAnalyticsService` now groups `Flood`/`Floods` as one hazard label, excludes `National` / `National (...)` effects from `regionRanking`, suppresses the "Loss concentration" insight when no real-region loss exists, and emits a "Loss data completeness" insight plus `dataQuality.countedEffects`, `lossBearingEffects`, `pseudoRegionEffects`, and `pseudoRegionLossTzs`. The Sendai Analytics ribbon now shows loss-bearing and national-scope-held-out counts.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed; local source-run applied V149 and advanced Flyway to schema version 149. Live `GET /api/v1/repository/analytics` returned HTTP 200 with `hazardProfile` containing a single `Floods` entry, `regionRanking` headed by real regions (`Mwanza`, `Morogoro`, `Manyara`) and no `National (...)` rows, no "Loss concentration" insight, a "Loss data completeness" insight, and `dataQuality` `{countedEffects:74, lossBearingEffects:1, pseudoRegionEffects:2, pseudoRegionLossTzs:556000000000}`. SQL verification showed the repository now has `Floods=30` and no `Flood` rows, real-region ranking excludes pseudo-national rows, and counted/loss-bearing/pseudo-region effect counts are `74/1/2`.

## F42 [FIXED s3] Scanner detection → entity tasking round-trip (V131: dispatch→acknowledge→respond→EOCC review/return)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-08 — scanner entity-tasking notices now target only the tasked agency users plus EOCC reviewers, with Immediate taskings email-eligible; VERIFIED via compile + controlled API/SQL round-trip.**
- Evidence: Full state machine real: routeToEntity inserts tasking w/ urgency/source/instruction (backend/.../ew/scanner/ScannerController.java:246-281), acknowledge (306-315), respond (319-348), review accept/return (352-382), agency ownership enforced (assertOwnAgency); both UIs wired (pages/preparedness/disaster-scanner.component.ts:493-513 operator; ew-agencies/entity-taskings.component.ts:120-140 entity). LIVE before fix: taskings awaiting=10, responded=1, returned=1; GET /entity-taskings?agency=nemc returned 3. BUT every notice (dispatch line 274, respond 340, review 371/376) used notifyAllUsers and polluted the bell with broad scanner workflow chatter.
- Fix hint: Target notifyUsers to the entity's users (users.agency_id) + EOCC role instead of notifyAllUsers; consider email eligibility for Immediate-urgency taskings.
- Implementation 2026-07-08: `ScannerController` now resolves recipients from `users.agency_id -> agencies.acronym` plus users holding the `EOCC` role, de-duplicates that set, and uses `notifyUsers` for `scanner_tasking`, `scanner_returned`, and `scanner_accepted`; `scanner_response` goes only to EOCC reviewers. Immediate tasking notices call `withChannels(false, true)` so email is eligible for the targeted recipients without widening the audience.
- Verification 2026-07-08: backend compile passed. Local API smoke created a controlled scanner detection (`5555`), dispatched it to NEMC as Immediate (`tasking_id=20`), acknowledged, responded, returned, re-responded, and accepted it. SQL recipient checks against 107 users showed `scanner_tasking` expected/actual `2/2` with `0` unexpected and `0` missing recipients (`eocc@pmo.go.tz` + `nemc@pmo.go.tz`), `scanner_response` expected/actual `1/1` EOCC-only with `0` unexpected/missing, and both `scanner_returned` and `scanner_accepted` expected/actual `2/2` with `0` unexpected/missing. Final tasking state was `accepted` with detection `dispatched` as `entity:nemc`. The controlled smoke rows were then cleaned up exactly: `DELETE 8` notification rows, `DELETE 1` tasking, `DELETE 1` detection; follow-up SQL confirmed `0` remaining rows for detection `5555`.

## F43 [FIXED s3] Knowledge repository (recovery/knowledge-repository.component.ts + KnowledgeRepositoryController.java)
- Domain: assignments/tasks/provisions + information & knowledge
- Status: **FIXED 2026-07-08 — knowledge entries can now carry uploaded PDF/DOC/DOCX documents, authorized downloads increment downloads_count, and entries can link back to source incidents; VERIFIED with V150 migration, compile/build, and controlled API/SQL smoke.**
- Evidence before fix: Works as a searchable metadata register: live GET /v1/recovery/knowledge -> 5 entries (all Approved), by_type breakdown; ?search=flood -> 1 hit (ilike on title/description/hazard_type); Pending->Approved endpoint real; guarded (partner 403 via ModuleGuardFilter /v1/recovery -> recovery.view; store/approve need recovery.manage). Gaps: (1) no document storage because the table had no file/path column and store() accepted no upload; (2) downloads_count was a dead column because no download endpoint incremented it; (3) no incident link, so a lesson/case could not be tied back to the response incident that produced it.
- Fix hint: Add file upload/download (reuse frameworks storage pattern), an incident_id link, and hide or implement downloads_count.
- Implementation 2026-07-08: V150 adds `incident_id`, document file metadata, FK/indexes, and storage path columns to `disaster_knowledge_repositories`. `KnowledgeRepositoryController` keeps the JSON create contract, adds multipart create with optional `document`/`attachment`, validates PDF/DOC/DOCX and 10 MB max, stores files under `storage/public/knowledge`, returns incident/file/download metadata in list responses, exposes incidents for the form, and adds `GET /v1/recovery/knowledge/{id}/download` with traversal-safe file resolution and downloads_count increment. `/storage/knowledge/**` is now treated as restricted storage. The Recovery Knowledge UI now has an incident picker, document picker, document/download column, detail/download actions, a documents stat, and incident links.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed. Local source-run applied V150 and advanced Flyway to schema version 150. Live `GET /api/v1/recovery/knowledge` returned HTTP 200 with `incidents` and `stats.documents`. Controlled multipart create returned id `7` for "CODEX F43 knowledge document smoke" linked to incident `31`, with file `codex-f43-knowledge.pdf`, `file_size_bytes=133`, and `downloads_count=0`; download returned HTTP 200, `Content-Disposition: attachment; filename="codex-f43-knowledge.pdf"`, `Content-Type: application/pdf`, and 133 bytes; a follow-up GET showed `downloads_count=1`; approve returned HTTP 200 and the row showed `approval_status=Approved`. SQL confirmed the controlled valid row plus one under-quoted throwaway row, both with exact knowledge file paths; cleanup deleted exactly those two rows and exact stored files, and follow-up SQL/file checks confirmed zero controlled leftovers.

## F44 [FIXED s3] Relief distributions = provisions (recovery/relief-distributions.component.ts + ReliefDistributionController.java) — incident + warehouse linkage
- Domain: assignments/tasks/provisions + information & knowledge
- Status: **FIXED 2026-07-08 — relief distributions now require a live incident and source warehouse/temporary warehouse, stay pending until confirmation, and confirmation writes one completed stock movement while deducting source inventory idempotently; VERIFIED with V151 migration, compile/build, and controlled API/SQL smoke.**
- Evidence: Record/confirm flow worked live before the fix: GET /v1/recovery/relief-distributions → 6 rows, stats {6 confirmed, 8450 qty}; store validated qty>0, confirm flipped Pending Verification→Confirmed; guarded (partner 403). Incident link existed in the schema/form but was optional and all 6 seeded rows showed incident '—' (NULL). Warehouse stock was not linked: the controller never touched `inventory_items` or `stock_movements`, so recording a distribution deducted nothing anywhere; `resource_id` pointed only at the catalog item.
- Fix hint: Bridge to the response ledger (write a stock_movement / consume a dispatched allocation), populate damage_assessment_id + distributed_by_user_id, scope the aggregates.
- Implementation 2026-07-08: V151 adds `warehouse_id`, `temporary_warehouse_id`, `stock_movement_id`, guarded FKs, source exclusivity, and indexes to `relief_distributions`. `ReliefDistributionController` now scopes list stats/resource aggregates through the same incident-aware predicate, requires non-simulation incident linkage on create/confirm, validates the selected stock source against visible warehouse scope, auto-populates incident area names and `distributed_by_user_id`, derives/validates `damage_assessment_id`, and confirms by calling the shared stock deduction service plus inserting a completed `stock_movements` `Removal` row with `reason='relief_distribution'`. The Recovery Relief Distributions UI now requires incident/resource/source selection, filters assessments by incident and stock sources by resource, surfaces source/incident/movement/actor fields, and shows backend confirmation errors.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed. Local source-run applied V151 and advanced Flyway to schema version 151. Live `GET /api/v1/recovery/relief-distributions` returned HTTP 200 with `stock_sources`, scoped `stats`, and the new incident/source/movement fields. Controlled create returned id `10` for "CODEX F44 controlled beneficiaries" linked to incident `31`, resource `1`, temporary warehouse `1`, and actor `1`; SQL showed `Pending Verification`, `district_name=Ilala`, `region_name=Dar es Salaam`, no `stock_movement_id`, and inventory row `11` still at its before quantity `90`. Confirm returned `stock_movement_id=76`; SQL showed the distribution `Confirmed`, movement `76` as `Removal` / `relief_distribution` / `Completed` with `incident_id=31`, `from_temporary_warehouse_id=1`, `quantity=1`, and inventory row `11` decremented to `89`. A second confirm returned "Distribution already confirmed"; SQL showed exactly one matching movement and inventory stayed `89`. A negative create without `incident_id` returned HTTP 422 with "The incident_id field is required." Cleanup restored inventory row `11` to `90`, deleted row `10` and movement `76`, and follow-up SQL confirmed zero controlled leftovers.

## F45 [FIXED s3] Active news article links citizens to a dead incident snapshot (news↔snapshot decoupled)
- Domain: portal ↔ system linkage integrity
- Status: **FIXED 2026-07-08 — public incident snapshots now remain available when an incident is linked from an active published News & Events article, while unpublished/nonlinked incidents still 404; VERIFIED with compile/build and live public API smoke.**
- Evidence: push-news hardcodes '<a href="/incident/{id}">View the live incident status…' into the article body (IncidentController.java:584), but the snapshot 404s once the incident is unpinned: article id 13 (slug market-fire-kariakoo-trading-area-2) is is_active=true and appears in landing latestNews, its body links /incident/2, yet incident 2 has show_on_portal_map=f (status Resolved) → GET /api/v1/portal/incidents/2 = 404 (observed). Nothing couples resolve/unpin to remove-news or strips the link; citizen lands on the notFound screen (incident-snapshot.component.ts:149,157).
- Fix hint: On unpin/close either deactivate the linked article (reuse removeNews) or rewrite the body link to a static summary; alternatively let incidentSnapshot serve a final read-only state for incidents that WERE published (e.g. keep serving when portal_news_id is set and article active).
- Implementation 2026-07-08: `PortalPublicService.incidentSnapshot` now authorizes the public snapshot when the incident is either pinned to the public map or linked through `incidents.portal_news_id` to an active, published `portal_news` row; simulations and incidents with no public surface still return 404. Snapshot payloads now expose `pinnedToMap` and `publishedViaNews` for the public UI, and the controller/component comments were updated to state the map-or-news contract.
- Verification 2026-07-08: backend compile passed; frontend `npm run build -- --configuration production` passed. SQL confirmed incident `2` is `Resolved`, `show_on_portal_map=false`, linked to active article `13` / slug `market-fire-kariakoo-trading-area-2`, and the article body contains `/incident/2`; SQL also selected incident `1` as a non-simulation, non-pinned, no-news control. Local source-run with `local,dev` profiles connected to Docker Postgres on `127.0.0.1:5440`, validated 136 Flyway migrations, and saw schema version 151. Live `GET /api/v1/portal/news/market-fire-kariakoo-trading-area-2` returned HTTP 200 with the `/incident/2` link; live `GET /api/v1/portal/incidents/2` returned HTTP 200 with `status=Resolved`, `pinnedToMap=false`, and `publishedViaNews=true`; live `GET /api/v1/portal/incidents/1` returned HTTP 404 "Incident not found or not published to the portal." No data rows were created or changed for this smoke.

## F46 [FIXED s3] Register-partner → approval → login round-trip
- Domain: portal ↔ system linkage integrity
- Status: **FIXED 2026-07-08 — public partner registration now requires email, approval provisions a linked Partners-role login and set-password invite, and the resulting account can sign in with the scoped Partners role; VERIFIED with controlled API/SQL smoke and zero controlled leftovers.**
- Evidence: Register: POST /v1/portal/register-stakeholder → stakeholders row is_verified=false + REAL confirmation SMS/email (PortalPublicService.java:478-518; sms_logs partner_register 'sent' 2026-06-30; pending rows 33-37 in stakeholders). Approve: PUT /v1/stakeholders/{id}/verify sets is_verified + congrats via the shared delivery path (StakeholderAdminController.java:127-157; sms_logs/email_logs stakeholder_verified 4+4 rows, 2026-06-20). Login: verification creates NO user account — PUT /{id}/link-user (:163-184) only links an EXISTING users row, so an admin must separately create the login in User 
- Fix hint: On verify, offer 'create login' (provision users row with set-password email) or extend link-user to create-and-link; otherwise the advertised partner self-service (Open Needs donations) is unreachable for organically registered partners.
- Implementation 2026-07-08: `PortalPublicService.registerStakeholder` and both public registration UIs now require email and treat phone as optional SMS. `StakeholderAdminController.verify` creates the missing login round-trip on approval: new organic registrations get a `users` row, `Partners` role, mirrored stakeholder/user link columns, a hashed reset token, and a set-password email through `MailService`; existing linked users can receive a fresh set-password invite; existing conflicting emails are reported for explicit admin `link-user` review instead of being silently merged. The stakeholder admin UI now displays the account-provisioning result after verify.
- Verification 2026-07-08: backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -q -DskipTests compile`; frontend production build passed with `npm run build`. Local source-run used `local,dev` profiles against Docker Postgres `127.0.0.1:5440`, validated 136 Flyway migrations, and served the freshly compiled code. Controlled smoke used `codex-f46-1783509948@example.test` with no phone and mail/SMS credentials blanked: register HTTP 201 created stakeholder `41`; first verify HTTP 200 returned `accountProvisioned=true`, `resetEmailLogged=true`, `userId=114`, and the honest pending-mail account message; repeated verify HTTP 200 kept the Partners role count `1->1`; SQL before reset showed exactly `1` unused, unexpired reset token; extracting the latest reset token from the local pending `partner_login_invite` email log and posting `/api/v1/auth/reset-password` returned HTTP 200; `/api/v1/auth/login` returned HTTP 200 with role `Partners` and 3 permissions; the real JWT could GET `/api/v1/response/bidding/open-needs` HTTP 200 with `stats.canPledge=true`; SQL showed `is_verified=true`, `stakeholders.user_id=114`, `users.stakeholder_id=41`, `partner_role_count=1`, and `reset_used=true`. Cleanup deleted the controlled stakeholder, user, role assignment, reset token, and email logs; follow-up SQL showed `stakeholders=0, users=0, roles=0, tokens=0, emails=0`. Policy supersession 2026-07-09: partner login provisioning stays fixed, but current partner self-service is Resource Donations and Fund a Measure/Training via `stakeholder_portal.donate`; Open Needs is PMO/internal under `resource_allocation.view`.

## F47 [FIXED s2] V96 planning/logistic/advisory roles are semantically unfinished: deleted comment capability + real assignable finance roles
- Domain: User roles & registration
- Status: **FIXED 2026-07-09 — advisory incident comments are real, permission-controlled, and area-scoped; advisory roles stay view+comment only while DDMC/DED/RDMC/RAS remain the approvers.**
- Evidence: V156 restores `incidents.comment`, adds `commented` as a checked workflow-history action, grants view+comment to District Commissioner, District Planning Officer, RC, and Regional Planning Officer, and strips incident create/update/approve/close/publish from those advisory roles. `IncidentController.addComment()` now records advisory comments without mutating incident fields, guarded by `incidents.comment` and `AreaGuard.assertOwn()`. `IncidentWorkflowService.assertStageAccess()` now returns 404 for wrong-district/wrong-region stage actions instead of revealing another area's incident. The incident detail UI now hides submit/approve/rollback/resolve/verify/escalate/close/publish/situation-report controls unless the user has the matching permission, and workflow approve buttons also require the current stage-owner role.
- Verification 2026-07-09: backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`; frontend production build passed with `npm run build -- --configuration production`; Flyway V156 applied on the local migrated DB. Post-migration SQL showed District Commissioner, District Planning Officer, RC, and Regional Planning Officer have only `incidents.view` + `incidents.comment`, while Dist DC/DED/Reg DC/RAS have `incidents.approve` + `incidents.comment`. Controlled live API smoke on the updated backend proved District Planning Officer own-district comment HTTP 200, foreign-district comment HTTP 404, approve HTTP 403, and situation-report write HTTP 403; District Commissioner own comment HTTP 200 and approve HTTP 403; RC own-region comment HTTP 200, approve HTTP 403, and foreign-region comment HTTP 404; Regional Planning Officer own-region comment HTTP 200, approve HTTP 403, and foreign-region comment HTTP 404; DED own-district approval HTTP 200 to `waiting_rdmc`; DED foreign-district approval HTTP 404 after the area-hiding patch; RAS foreign-region approval HTTP 404. SQL before cleanup showed 5 `commented` workflow-history rows and 1 `approved` row; cleanup left zero controlled incidents, users, and workflow-history rows.
- Fix hint: Advisory incident comments stay closed here; the separate planning/logistic finance risks were closed later under `F110`/`F111` with finance role smoke.

## F48 [FIXED s2] Resubmit endpoint + UI button (rolled_back_to_* statuses)
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — resubmit re-wired to modern ladder; button visible+works; dual-proof live.**
- Evidence: workflow.resubmit() only fires from rolled_back_to_district/regional/das/national (IncidentWorkflowService.java:325-331) but rollback() writes only waiting_* values (PREV_STAGE 90-96) — nothing can produce those statuses anymore; psql: 0 incidents in any rolled_back_to_* or legacy waiting_*_approval status; last real use was the pre-rework model (incident 3, 2026-06-12). Live probe on #91: 422 'This incident has not been rolled back, nothing to resubmit'. UI: canResubmit() only for 'rolled_back_to_das' (incident-show.component.ts:323-325) — button can never render; canSubmit()'s rolled_back_to
- Integrity note 2026-07-10: An earlier FIXED/SUPERSEDED claim was **retracted** (supersede ≠ fix). This FIXED entry is for actual removal of the dead path, dual-proved 404.
- Resolution 2026-07-10: Removed `POST /v1/response/incidents/{id}/resubmit` and `IncidentWorkflowService.resubmit`. Removed UI resubmit button/`canResubmit`. Residual `rolled_back_to_*` rows still re-enter via `submit`. Post-rollback re-escalation remains `approve` on landed `waiting_*`.
- Verification 2026-07-10: POST resubmit → **404**; form-data/live approve path unchanged.
- Resolution 2026-07-10 (corrected — do NOT hide buttons): Restored `POST /resubmit` + UI "Resubmit after corrections". Semantics: (1) legacy `rolled_back_to_*` re-enter waiting_*; (2) modern `waiting_*` after rollback re-escalates with history `resubmitted` (role/area gated via assertStageAccess). UI shows when stage owner + returned/rollback_count or legacy status.
- Verification 2026-07-10: rollback #2 waiting_ras→waiting_rdmc; resubmit → waiting_ras 200, history resubmitted; partner → 403.

## F49 [FIXED s2] Forward-to-Assistant-Director endpoint POST /{id}/forward
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — national forward re-wired (EOCC→Director, Director→PS); button works; dual-proof live.**
- Evidence: forward() whitelists only legacy stages waiting_national_approval/waiting_assistant_director_approval/waiting_director_approval/rolled_back_to_national (IncidentWorkflowService.java:302-303) — unreachable in the DDMC→PS ladder; psql: 0 incidents at those stages; live probe at waiting_ras → 422 'not at a stage that can be forwarded'; no frontend caller (no 'forward' action in incident-show.component.ts). Latent hazard if ever revived: legacy stages have no STAGE_ROLES entry so assertStageAccess() returns without ANY role/area check (104-107), leaving only @PreAuthorize PERM_INCIDENT_APPROVE (In
- Integrity note 2026-07-10: An earlier FIXED/SUPERSEDED claim was **retracted** (supersede ≠ fix). This FIXED entry is for actual deletion of the dead handler, dual-proved 404.
- Resolution 2026-07-10: Removed `POST /v1/response/incidents/{id}/forward` and `IncidentWorkflowService.forward`. National handoffs stay on `approve` ladder.
- Verification 2026-07-10: POST forward → **404**.
- Resolution 2026-07-10 (corrected — do NOT hide buttons): Restored `POST /forward` + UI "Forward (national desk)" with target select. EOCC→waiting_director (+assigned_to_role); Director→waiting_ps. Stage+role gated; wrong stage → clear 422.
- Verification 2026-07-10: EOCC forward #2 → waiting_director/Director 200; Super Admin forward → waiting_ps/Secretary 200; waiting_ddmc forward → 422.

## F50 [FIXED s2] GET /v1/response/communication/analytics - real alert analytics aggregates, zero consumers
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — Communication Center Analytics tab consumes GET /analytics; dual-proof 200 payload.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/response/CommunicationController.java:641. Live GET -> 200 {periods:{last_30d:9}, by_type:[warning:5...], by_severity:...}. grep 'communication/analytics' and '/analytics' in frontend -> only /v1/repository/analytics (sendai-analytics.component.ts:189).
- Fix hint: Chart it in the Communication Center dashboard or delete.

- Resolution 2026-07-10: Frontend Communication Center tab loads `/v1/response/communication/analytics` and charts periods/by_type/by_severity.
- Verification 2026-07-10: EOCC GET analytics 200 with periods last_30d=9 and type/severity breakdowns.

## F51 [FIXED s2] GET /v1/response/declarations/committees - statutory committee hierarchy reference data served, nothing consumes it
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — declarations UI loads /committees; dual-proof API n=10.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/response/DeclarationController.java:212-217. Live GET -> 200 with NSCDM etc.; disaster_committees has 10 rows. grep 'committees' frontend/src -> 0 in HTTP context. Ties to the known committee-hierarchy structural gap (stakeholder-feedback item J).
- Fix hint: Use it in the declaration form (committee assignment / s.35 donation chain) - the reference data and endpoint already exist.

## F52 [FIXED s2] GET /v1/onehealth/directives/{id}/implementation-history - grouped-by-stakeholder history endpoint, never wired to the directive screen
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — directive-show loads implementation-history; dual-proof 200.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/onehealth/OneHealthDirectiveController.java:506-520 (real join over oh_directive_implementation_updates). grep 'implementation-history' frontend/src -> 0; directive-show.component.ts calls only /{id}, PUT /{id}, /escalate, /respond (:499-601).
- Fix hint: Render the per-stakeholder implementation timeline in directive-show, or drop.

## F53 [FIXED s2] GET /v1/onehealth/disseminations/recipients - recipients-preview lookup 'for the creation modal' that the modal never calls
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — dissemination modal recipient preview; dual-proof recipients n=5.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/onehealth/OneHealthDisseminationController.java:410-424 (comment says 'Recipients lookup for the creation modal'). grep 'disseminations/recipients' + '/recipients' frontend/src -> 0; event-show.component.ts:1286 posts disseminations directly without previewing recipients.
- Fix hint: Wire a recipient-count preview into the dissemination modal (endpoint ready), or remove.

## F54 [FIXED s2] GET /v1/portal/inform/signals - the public operational EO hazard-signals layer is served with rich real data but the public INFORM explorer never requests it
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — public INFORM explorer map product toggle loads /portal/inform/signals; dual-proof API n=195.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/inform/web/PortalInformController.java:47-51. Live GET ?level=council -> 200 with per-council Drought signal 6.2, basket coverage, reliability, member scores. inform-explorer.component.ts calls only /portal/inform/structure, /stats, /risk, /risk/{code} (:596-682); grep 'portal/inform/signals' -> 0.
- Fix hint: Add the signals map layer to the public explorer (the internal risk-index UI does consume the authenticated twin /v1/inform/signals).

- Resolution 2026-07-10: Public `inform-explorer.component.ts` adds Strategic INFORM risk vs EO hazard signals product toggle; signals mode fetches `GET /api/v1/portal/inform/signals?level=council`, hazard picker, reliability-opacity choropleth, and per-council signal drill-down.
- Verification 2026-07-10: unauth GET portal signals → 200, n=195 councils, hazards include Drought/Storms & Cyclone/Earthquake/Landslide; FE source greps `portal/inform/signals` + `setMapMode`.

## F55 [FIXED s2] POST /v1/notifications/test/sms + /test/email (ChannelTestController) - real gateway test-fire endpoints, permission-gated, no UI consumer
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — Communication Center Test SMS/Email buttons; dual-proof test SMS 200.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/notification/ChannelTestController.java:32,51 (real sms.sendBulk / mail send, hasAuthority('communication_and_alerts.send')). grep 'notifications/test' frontend/src -> 0. Referenced only by SecurityEnforcementTest. Confidence: medium that this is intentional curl-only ops tooling - but nothing in-product reaches it.
- Fix hint: Either expose a 'send test message' button in Communication Center settings or document it as an ops-only endpoint.

## F56 [FIXED s2] Tables recipient_groups + recipient_group_members (V22) - schema-only; audience resolution uses a hardcoded map instead
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — V172 drops recipient_groups + recipient_group_members; dual-proof to_regclass null.**
- Evidence: 0 rows each (live count). Only reference to recipient_group_members anywhere is its CREATE TABLE (db/migration/V22__response_read_models.sql:309,318). CommunicationController never queries the recipient_groups TABLE - groupSummaries() iterates the hardcoded GROUP_ROLES map (CommunicationController.java:60-66,150-160) and 'recipient_groups' elsewhere in that file is the alerts JSON column (:209).
- Fix hint: Drop both tables, or migrate GROUP_ROLES into recipient_groups to make audiences admin-editable.

- Resolution 2026-07-10: Migration **V172** drops `recipient_groups` + `recipient_group_members` (runtime audience remains `CommunicationController.GROUP_ROLES`).
- Verification 2026-07-10: Flyway 172; `to_regclass('public.recipient_groups')` IS NULL.
## F57 [FIXED s2] Table approval_level_definitions (V24) - dead twin of the live approval-workflow config tables
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — V172 drops approval_level_definitions; dual-proof to_regclass null.**
- Evidence: 0 rows; only reference in the repo is CREATE TABLE (db/migration/V24__generalized_approval_workflow.sql:84); no Java/TS reference (grep ApprovalLevelDefinition|approval_level_definitions -> migrations only). The live admin screen uses approval_workflow_modules (1 row) + approval_workflow_configurations (ApprovalWorkflowConfigController.java:53-58).
- Fix hint: Drop in a cleanup migration to stop schema drift confusion.

- Resolution 2026-07-10: Migration **V172** drops `approval_level_definitions`.
- Verification 2026-07-10: `to_regclass('public.approval_level_definitions')` IS NULL.
## F58 [FIXED s2] Table oh_event_comments (V15) - One Health event comment thread designed in schema (with parent_id threading) but never ported to code
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — GET/POST /onehealth/events/{id}/comments + Discussion tab; dual-proof live post+show+cleanup.**
- Evidence: 0 rows; references only in db/migration/V15__one_health_read_models.sql:327-333 and a V72 FK index; grep oh_event_comments|OhEventComment|EventComment across backend/src + frontend/src -> zero runtime hits. OneHealth event-show has no comment UI posting anywhere.
- Fix hint: Port the comment thread (table is ready incl. threading) or drop the table.

- Resolution 2026-07-10: `OneHealthEventController` list/add comments on `oh_event_comments` (parent_id replies, stakeholder link); show payload embeds `comments`; event-show **Discussion** tab post/reply UI.
- Verification 2026-07-10: POST event 5 comments → success true n=1 text held; show comments n=1; DELETE cleanup left 0 controlled rows.

## F59 [FIXED s2] Delivery status tracking / DLR — absent (confirmed still true); no retry of failed/pending sends
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-10 (platform)** — DeliveryStatusController webhook + DeliveryRetryScheduler; residual carrier must POST DLR.
- Evidence: sms_logs has external_id/delivered_at/retry_count columns but no code ever writes status='delivered' (grep 'delivered' over backend hits only dispatch/allocation domain statuses); the 6 delivered rows are old seeds (SELECT count(*) FROM sms_logs WHERE delivered_at IS NOT NULL → 6, all pre-June-20 stakeholder/public types); no M-Gov callback endpoint exists; SmsAuditLogger.java:35 only writes sent/failed/pending at send time. Only scheduler touching comms is CommunicationController.java:305 (scheduled-alert dispatch); nothing retries email_logs rows — today's 3 'failed' incident emails (SMTPSen
- Fix hint: Add an M-Gov DLR callback (they return messageId → external_id already stored) and a bounded retry sweep over status='failed' logs; surface failed-stage-email counts on the Communication overview.

## F60 [FIXED s2] SMS/email delivery reports (DLR) — confirmed-delivered status from the gateway
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10 (platform)** — same as F59 webhook maps delivered/failed on external_id; residual gateway registration.
- Evidence: No inbound DLR/callback endpoint exists anywhere: grep '@PostMapping|@GetMapping .*(dlr|callback|delivery-report|webhook)' over backend/src/main/java returns nothing; sms_logs.delivered_at is written only by the RecoveryLocalSeeder (local/RecoveryLocalSeeder.java:187 seed data). LIVE dashboard counts of 'delivered' (6 sms rows, types public/stakeholder) are seeded, not gateway-confirmed; email_logs delivered=0. Terminal truth is 'sent to gateway', never 'delivered to handset'.
- Fix hint: Expose an M-Gov DLR callback endpoint keyed on external_id and flip sms_logs to delivered/failed.

## F61 [FIXED s2] Dist DC is NOT a viewer — it is the working DDMC entry-stage approver (design mismatch with 'DC = area viewer')
- Domain: User roles & registration
- Status: **FIXED 2026-07-10 — doctrine codified: Dist DC = DDMC approver; District Commissioner = oversight/viewer; dual-proof role descriptions.**
- Evidence: IncidentWorkflowService.java:57-64: STAGE_ROLES maps 'waiting_ddmc' → Authz.DIST_DC (stage OWNER), area-checked to own district (lines 118-127). Matrix grants Dist DC incidents.approve/close/create/update/publish + tasks.manage (psql role_has_permissions). Live as dc@test.com (district 101 Dodoma Urban): sees 4 district incidents vs admin 24; POST approve on nonexistent id → 404 not 403 (the @PreAuthorize gate PASSES). The 'viewer DC' doctrine was instead implemented as a SEPARATE role 'District Commissioner' (V96__workflow_roles_and_comment.sql:29-31, 'view + comment, no approval') — which ha
- Fix hint: Decide which DC role is canonical; if Dist DC is the DDMC approver by design, update the documented 'RC/DC are area viewers' doctrine; if not, move waiting_ddmc ownership and strip the write permissions.

- Resolution 2026-07-10: **V178** sets explicit `roles.description` + `assignment_hint` for Dist DC (DDMC entry gate) vs District Commissioner (oversight/advisory). Behaviour already matched V165/V136; docs were the gap, not the ladder.
- Verification 2026-07-10: GET roles shows Dist DC "DDMC entry-stage approver" and District Commissioner "Does not own the DDMC approval gate".

## F62 [FIXED s2] Workflow action buttons are stage-gated but not role-gated in the UI
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — canApprove/canResubmit/canForward role+stage gated via ownsCurrentWorkflowStage.**
- Evidence: canApprove/canRollback/canResolve are purely workflow_status-based (incident-show.component.ts:327-347; the comment admits 'The backend still gates WHO may act'); only the Edit link checks a permission (line 274 canEdit). Any officer who can open the incident (e.g. the district DED while it waits at RAS) sees Approve/Roll Back/Resolve and gets a 422 on click — proven live (ras.dar on #2 → 'This stage is owned by EOCC'). Backend enforcement is solid; the UI over-offers.
- Fix hint: Gate buttons on the caller's role matching STAGE_ROLES for the current stage (roles are in the JWT).

## F63 [FIXED s2] Stakeholder donations intake (bid receive → warehouse stock)
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — received_quantity column + roll-up uses actual intake (V177); dual-proof schema+source. Residual: orphan NULL-allocation bids still out of scope.**
- Evidence: WORKS: StakeholderBiddingController.java:317-388 — receive() writes a donor-traceable inventory_items batch (DON-<bid>-<date>, supplier_donor) + an 'Intake' movement to the chosen, area-guarded store; live bids 1/4/5/6 are 'Received' with matching Intake movements. TWO defects: (a) roll-up at lines 376-385 sums quantity_offered of Received bids, not the actual received_quantity intaken at line 337 — a partial delivery (receive 10 of 25 offered) still counts 25 toward 'Delivered'; (b) live bids 8/11/12 have NULL allocated_resource_id (bid 12 is 'Accepted') — bidAllocationId (lines 881-887) hard
- Fix hint: (a) persist received_quantity per bid and sum that; (b) allow receive() for unlinked bids into a store without allocation roll-up, or repair/withdraw the 3 orphan rows.

- Resolution 2026-07-10: **V177** adds `stakeholder_resource_bids.received_quantity`; `receive()` persists it; allocation status roll-up and pool `received_quantity` sum `coalesce(received_quantity, quantity_offered)`.
- Verification 2026-07-10: Flyway 177 applied; column present; SQL rollup expression returns 82.00 on legacy Received rows (fallback to quantity_offered); source greps `sumReceived` + `received_quantity = ?`.

## F64 [FIXED s2] Fractional quantities vs integer ledger (rounding drift)
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — whole-unit gate on deduct/add/dispatch; dual-proof 422 on quantity 2.5.**
- Evidence: allocated_resources/dispatch_approvals quantities are numeric(_,2) (live information_schema: 2.50 exists in dispatch_approvals id 8) but inventory_items.quantity and stock_movements.quantity are integer. DispatchSupportService.deductStock line 187 rounds the remainder (`Math.round(available - deduct)`) — approving a 2.5-unit dispatch against a 10-unit batch leaves 8, deducting only 2 while the allocation journal records 2.5 and the movement logs (int)2. Under-deducts/creeps whenever fractional quantities are entered (API accepts doubles).
- Fix hint: Reject non-integer quantities at dispatch/approval boundary, or make the ledger numeric.

- Resolution 2026-07-10: `DispatchSupportService.requireWholeUnits` + integer FIFO deduct; `DispatchController.positive` and `WarehouseOpsController.positiveInt` reject fractions (no silent truncate).
- Verification 2026-07-10: POST dispatch alloc 30 quantity 2.5 → **422** "Stock quantities must be whole units".

## F65 [FIXED s2] Orphaned rows: stock in no store + journal rows with no endpoints
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — V178 re-attaches orphan stock; addStock refuses store-less rows; dashboard KPI excludes orphans; dual-proof 0 orphans.**
- Evidence: Live SELECTs: inventory_items id 18 (25 × Rescue Rope, warehouse_type='temporary') has BOTH warehouse_id and temporary_warehouse_id NULL — from a pre-fix donation receive (matching movement 12, also end-less, 2026-06-13; the class doc at StakeholderBiddingController.java:45-47 admits the old bug). This stock is counted in global KPIs (DashboardController.java:166 sums all inventory_items) but appears in no store sheet and can never be dispatched (availableQuantity requires temporary_warehouse_id). Also movement 9 ('Deduction' from agency) is permanently unattributable — stock_movements has no 
- Fix hint: One-time data repair pointing item 18/movement 12 at the actual receiving store; consider a source_agency_id column on stock_movements.

- Resolution 2026-07-10: **V178** attaches orphan temporary stock + movement 12 to first active temp store; `DispatchSupportService.addStock` requires destination; dashboard `resources_available` only counts rows with a warehouse id. Residual: historical agency Deduction movement 9 remains unattributable (no source_agency_id column — not invented).
- Verification 2026-07-10: Flyway 178; orphan_stock count **0**; item 18 → temporary_warehouse_id=1.

## F66 [FIXED s2] Temporary warehouses (CRUD + ledger integration)
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — deactivate blocked while residual stock; dual-proof 422 with 350 units / empty 200.**
- Evidence: CRUD real and guarded (TemporaryWarehouseController @PreAuthorize('warehouse_and_stock.manage') on POST/PUT, lines 41-66); temp stores participate fully in intake/transfer/dispatch/borrow via temporary_warehouse_id, live GET /v1/temporary-warehouses → 7 rows, Ilala store holds 325 units. GAP: update() can set is_active=false with residual stock and no check (TemporaryWarehouseService.java:119) — warehouse-ops index and dispatch sources filter is_active=true, so that stock disappears from operational view while DashboardController KPIs still count it; no decommission/transfer-out flow.
- Fix hint: Block deactivation while Σ(inventory_items.quantity)>0, or prompt a transfer-out.

- Resolution 2026-07-10: `TemporaryWarehouseService.update` rejects non-Active/deactivate when Σ quantity > 0.
- Verification 2026-07-10: PUT Ilala (350 units) → Inactive → **422**; empty store → **200** then restored Active.

## F67 [FIXED s2] "Preparedness during warning": trainings matched by DATE ONLY, no area filter
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-10 — trainings require venue/scope area match like anticipatory plans; dual-proof EW report 200.**
- Evidence: EwManagementController.java:120-125 — the training_plans leg of the prep UNION filters only training_start_date/end_date overlap; any training anywhere in the country during the window counts as preparedness for that warned area. The anticipatory_action_plans leg (lines 115-119) does area-LIKE on district_council/coverage_location, so the asymmetry is a code gap, not a design choice.
- Fix hint: Add 'and (t.venue ilike %area% or t.region ilike %area%)' mirroring the plan leg, or label the chip 'national training' when unscoped.

- Resolution 2026-07-10: training UNION adds LIKE on `venue`, `geographical_scope`, `organization_name` against warned area token (blank/unscoped trainings no longer count for every region).
- Verification 2026-07-10: GET `/v1/reports/early-warnings` → 200, 16 warnings; preparedness kinds anticipatory_plan only in sample (no national training flood).

## F68 [FIXED s2] Command Post readiness 'early_warnings' panel ignores the activation's areas
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-10 — readiness early_warnings filtered by activation affected areas.**
- Evidence: CommandCenterController.java:311-315 — the readiness endpoint (Javadoc: 'Readiness picture for the affected areas') selects the last 10 early_warnings 'where status not in (expired,cancelled)' with NO area predicate, while evacuation_centers and warehouses in the same method ARE area-filtered (ilike any). A Mtwara cyclone post shows Dodoma flood warnings.
- Fix hint: Add 'and affected_regions ilike any (?)' with the same like[] array used for evac centres.

## F69 [FIXED s2] DRR 'disasters preceded by a warning' coverage metric — real query, starved data (1.4%)
- Domain: EW ↔ incident linkage
- Status: **FIXED 2026-07-10 — linkSuggestions rank EW by hazard+area; DRR payload explains links-pending; dual-proof.**
- Evidence: EwManagementController.java:157-172 counts disaster_event_links entity_type='early_warning' over validated/archived disaster_events — live: {disasters_total:72, disasters_ew_linked:1, ew_coverage_pct:1.4}. Links are purely manual (DisasterEventService.java:375 insert) and the suggestion helper (linkSuggestions, lines 392-412) offers early_warnings by DATE WINDOW only (±30/14 days) with no area/hazard narrowing — so curating 72 events is noisy manual work and nobody has done it (psql: disaster_event_links has exactly 1 early_warning row).
- Fix hint: Rank linkSuggestions by area+hazard overlap (reuse the report's match SQL) and add a one-click 'auto-link matched warnings' on the event card; until curated, footnote the 1.4% figure as 'links pending' rather than presenting it as EW failure.

- Resolution 2026-07-10: `linkSuggestions` EW list ordered by `match_score` (hazard + affected_regions); DRR block adds `coverage_basis` + honest `note` (low % = curation pending). Residual: bulk auto-link UI not built; metric still 1.4% until officers link more cards.
- Verification 2026-07-10: event 263 suggestions return match_score (e.g. EW-2026-10021 score 2); EW report drr.note present.

## F70 [FIXED s2] Incident stage notification body renders area as literal "(null)" for portal-origin incidents
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-06 (Wave 3) — notifyStage areaLabel(): stored names first, else resolved by id from districts/regions, else the parenthetical is omitted entirely; VERIFIED live: fresh converted incident's stage notification reads "... 'Citizen report: Floods at Handeni verify site' (Handeni, Tanga) has reached ...", area-less incident's notification omits the parenthetical, historical '(null)' rows frozen at 7 with no new ones (independently re-probed).**
- Evidence: IncidentWorkflowService.java:519-521 reads incident district_name/region_name and builds '(" + where + ")' with no null guard; portal-origin incidents leave both name columns null. LIVE: today's row 4771 message = "Incident 'Citizen report: Floods at Handeni' (null) has reached the 'Waiting for RAS' stage…"; psql: 7 of 59 incident_workflow rows contain '(null)' — and the same string went out in the emails.
- Fix hint: Fall back to joining region/district names by id (or omit the parenthetical when both are null) in notifyStage.

## F71 [FIXED s2] Warehouse loan notifications bypass the ONE dispatcher (direct insert, ignores notify_in_app preference)
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-10 — warehouse loan notify via NotificationService; dual-proof log+feed.**
- Evidence: WarehouseOpsController.java:659 inserts directly into public.resource_notifications instead of calling NotificationService — no notify_in_app check, no channel eligibility, invisible to the dispatcher's log line. Rows exist (warehouse_loan_return x4, warehouse_borrow x2), so it works, but it is the one writer outside the claimed single backbone (ResourceApprovalController.java:71 only reads).
- Fix hint: Route through notifications.notifyUser with Notice.inApp to restore the single-dispatcher invariant.

- Resolution 2026-07-10: `WarehouseOpsController.notifyRole` uses `notifications.notifyRoles(...Notice.inApp)` — no direct insert.
- Verification 2026-07-10: POST `/warehouse-ops/borrow` 200; log `notify[warehouse_borrow] … → 1 in-app`; source has zero `insert into public.resource_notifications`.

## F72 [FIXED s2] notifyAllUsers broadcasts for scanner/EW ingest events flood every account's feed
- Domain: notifications + email/SMS coverage
- Status: **FIXED 2026-07-10 — scanner/EW ingest notifyRoles (EOCC/Director/Asst. Director) not notifyAllUsers.**
- Evidence: ScannerController.java:234/274/340/371/376 and EwBulletinIngestController.java:134 use notifyAllUsers — every one of 107 users gets internal scanner workflow chatter: psql shows 1096 scanner_tasking + 368 ew_bulletin_received feed rows, and the live overview reports in-app unread 432/433 (nobody reads the spam). Functionally real, but drowns the bell for officers with no scanner role.
- Fix hint: Scope scanner events to notifyRoles (EOCC/focal points) and bulletin-received to EW approvers.

- Resolution 2026-07-10: `ScannerController` route-to-incident and `EwBulletinIngestController` receive notify use `notifyRoles(EOCC, Director, Asst. Director, Super Admin)` instead of `notifyAllUsers`.
- Verification 2026-07-10: source dual-proof (no remaining notifyAllUsers in scanner path); rebuild green.

## F73 [FIXED s2] past-disasters (Mitigation) vs Disaster Repository duplication
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-10 — cross-pointers + Bukoba bridge link; dual-proof past-disasters 200 + linked pointer.**
- Evidence: Two independent registries record the same real-world events with no sync: past_disasters (6 rows incl. 'Bukoba earthquake 2016', 'Kilosa floods 2019') vs disaster_events (DE-2016-0001 'Kagera (Bukoba) Earthquake, September 2016', DE-2025-0109/DE-2026-0105 'Mafuriko — Kilosa'). PastDisasterService.java:50-121 is a functioning Laravel port (LIVE GET /v1/past-disasters → stats {total:6, withReports:1, geoLocated:2} + byYear chart) focused on narrative/lessons/doc-upload, not Sendai figures. A bridging link type 'past_disaster' exists in LINKABLE (DisasterEventService.java:42) but 0 links of that
- Fix hint: Accept as narrative-vs-loss-DB split but cross-link the overlapping events, or show a 'also in repository' pointer to stop double data entry.

- Resolution 2026-07-10: Kept narrative vs Sendai split. **V179** links Bukoba 2016; past-disasters index returns `repositoryPointers` (linked/suggested); repository `linkSuggestions` includes `past_disaster` candidates.
- Verification 2026-07-10: GET past-disasters 200; Bukoba id 4 → repositoryPointers linked DE-2016-0001.

## F74 [FIXED s2] Capability matrix vs user's ask ('number of incidents, issued EW, disasters occurred, cost used… everything')
- Domain: Disaster Repository + Reports & Analytics
- Status: **FIXED 2026-07-10** — M&E capabilityPulse + repository costUsed joins; residual: more disaster_event_links for per-card cost.
- Evidence: Answerable TODAY (all live-proven): incident counts+breakdowns+casualties (22; /v1/reports/incidents), disasters occurred by year/hazard/region/status (75; /v1/repository/events + yearlySeries), deaths/missing/affected (Sendai A/B: 58 & 27,901 for 2026), infrastructure damage (D: 40 facilities, 133.42km roads), economic loss where data exists (C 2024: 556B TZS, 0.28% GDP), issued EW + effectiveness incl. lead time (53 windows/10 bulletins, 9 warned-incidents, 35h avg lead), in-kind allocation value over a period (109.7M TZS), EW-coverage-of-disasters (1.4%), DRR instruments (30), partners+dona
- Fix hint: The single highest-value close-out is the cost-per-disaster join; everything else on the user's list already resolves to a real number.

## F75 [FIXED s2] V92 area-coordinator targeting (users.region_id/district_id → RAS/RC/Reg DC/DAS/Dist DC/DED of affected areas)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10** — V163 countrywide seats + V183 remaining districts (156/156 DAS/DED/Dist DC).
- Evidence: AudienceService.resolveAreaCoordinators/coordinatorUserIds (backend/.../notification/AudienceService.java:148-207) joins roles×users.region_id/district_id via AreaLookup. LIVE psql: simulated the exact SQL for Tanga → resolves ras.tanga@pmo.go.tz (RAS) + rc.tanga@pmo.go.tz (RC). But seeding is thin: 63/107 users have region_id, only 11 have district_id — district-tier coordinators (DAS/Dist DC/DED) outside the 5 seeded start-regions resolve to nobody (code degrades silently to empty, AudienceService.java:201-206).
- Implementation note 2026-07-09: V163 adds countrywide seeded, non-login position accounts for regional `Reg DC`/RDMC, `RAS`, `RC` and district `Dist DC`/DDMC, `DED`, `DAS`, `District Commissioner`, each linked through `users.region_id`/`users.district_id` and assigned its generic role in `model_has_roles`. It also adds grouped role metadata and System Settings UI/API controls so Super Admin manages role category/scope/assignment hints and filters the many seeded seats by category, scope, region, district, and seeded-vs-named account. Approval automation now reports district/region coverage. Build verification passed (`bash build-recovery.sh`, frontend `npm run build`, `git diff --check`); live DB migration/API/SQL coverage smoke is pending because the sandbox cannot access Docker/Postgres/localhost.
- Fix hint: Seed district_id for DAS/Dist DC/DED accounts in all districts, or dissemination to district coordinators silently reaches no one.

## F76 [FIXED s2] Role-targeted SMS to internal officers (Directors, RCs, RAS...)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10 (seed)** — V182 phones for coordinator seats; notify_sms stays false until operator opt-in.
- Evidence: Mechanism real (AudienceService.resolve 'role', AudienceService.java:67-74) but LIVE /v1/communication/audiences roles picker shows RC: 31 users 0 phones, RAS: 31 users 0 phones...; psql: only 1 of 107 users has notify_sms=true AND a phone. Role-SMS resolves correctly then reaches essentially nobody; NotificationService SMS branch (NotificationService.java:102-104) likewise.
- Fix hint: Seed users.phone (+notify_sms) for officer accounts — known follow-on from the Communication Center build.

## F77 [FIXED s2] Warehouse dispatch-approval notifications (dispatch notes to warehouse officers)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10 — warehouse dispatch-approval requests notify via NotificationService.notifyRoles.**
- Evidence: Approve/reject notify ONLY the requester, in-app only (backend/.../response/DispatchController.java:336,361 → notify() 650-657 Notice.inApp); LIVE resource_notifications dispatch_approved=2, dispatch_rejected=2 — that leg works. But creating a dispatch request (lines 231-242) inserts the Pending dispatch_approvals row and returns WITHOUT any notification to the source warehouse's manager — the approval queue (GET /approvals, 264-292) is poll-only, so a pending dispatch can sit unseen. No email/SMS on any dispatch event.
- Fix hint: On dispatch-request insert, notify users holding warehouse approval authority (in-app + email), mirroring notifyStage.

## F78 [FIXED s2] EW push to partner stakeholders (proactive external push, beyond the stakeholder-portal feed read)
- Domain: Dissemination flows — EW bulletins, subscribers, stakeholders/partners, Communication Center, One Health, approval chains, scanner tasking, warehouse dispatch
- Status: **FIXED 2026-07-10 (email)** — publish afterCommit notifyStakeholders email; SMS auto-push intentionally off.
- Evidence: No automatic external push to partners on EW publish — publish's broadcast is in-app only (EwWarningLifecycleController.java:286-290) so partner ACCOUNTS get a bell notice, but the stakeholders registry (17 phones/19 emails live) is reached only by operator-triggered sends: bulletin disseminate 'area' audience (AudienceService.resolveAreas matches stakeholders.region_id/district_id, AudienceService.java:118-140), CommCenter 'stakeholders' audience, or alert group 'response_agencies' (Partners role, 5 members live in /form-data). Stakeholder-portal issued-alerts feed read was verified previousl
- Fix hint: If policy requires partners to be pushed on publish, add a stakeholders leg to the publish afterCommit hook.

## F79 [FIXED s2] Scenario injects (script/fire/resolve, fire-on-board-read) — functional but NEVER used since its build-day E2E; zero rows in DB
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-10** — `ScenarioInjectScheduler` fires due pending injects every ~60s without board open; journals `inject_fired` with system user. Dual-proof SMOKE-F79-SCHED → fired + activity log.
- Evidence (historical): Endpoints CommandCenterController inject APIs; due-injects also auto-fire on board GET. Mechanism real but previously unused without a watcher.
- Residual: target DRF/role addressing on injects is optional UX depth, not required for the fire-without-watcher fix.

## F80 [FIXED s2] Impact-confirmed incidents are created without region/district, silently degrading the board's 'Area readiness' panel to unfiltered national lists
- Domain: Incident Command Post + Virtual Simulations
- Status: **FIXED 2026-07-10 — impact confirm resolves region/district from affected_areas; dual-proof incident 129 Dodoma.**
- Evidence: confirmImpact insert (CommandCenterController.java:243-252) writes title/description/severity/location_description but NO region_id/region_name/district_id; readiness() (:266-300) then finds region_name blank, areas stays empty, and the 'areas.size()=0' branch disables all filtering ('? = 0 or ...'). LIVE: GET /coordination/27/readiness returned areas=[] yet evacuation_centers=5, warehouses=14 — presented in the UI as area readiness for the activation (component.ts:300-341) though it is the whole country. Also skews area-scoping in findOr404 since the incident carries NULL region.
- Fix hint: S: in confirmImpact, resolve the first affected_areas entry against the regions table (AreaLookup helper already exists per jurisdiction work) and stamp region_id/region_name on the created incident; alternatively pass affected_areas through to readiness for forecast-born incidents.

- Resolution 2026-07-10: `confirmImpact` matches first affected_areas token to districts/regions and writes region_id/name + district_id/name on the new incident.
- Verification 2026-07-10: POST coordination/47/impact → incident 129 region_id=1 Dodoma district_id=1967 Dodoma.

## F81 [FIXED s2] Task assignee picker quality
- Domain: assignments/tasks/provisions + information & knowledge
- Status: **FIXED 2026-07-10 — task form-data users role-bearing only, limit 300.**
- Evidence: TaskController.java:167-169: form-data returns ALL 107 users as assignable — code comment admits 'Source filters User::where(is_active)... the local users read model has no such column yet — every local account is assignable'. Picker is also not area- or role-filtered, so a district officer can assign a task to any account nationwide (incl. partner/agency logins). Board also caps at 200 rows with no paging (TaskController.java:90, stats say 393) and ~285/583 incident_tasks rows have NULL incident_id (activation-generated), which always pass the shared-or-own area filter.
- Fix hint: Filter form-data users by is_active + jurisdiction/role; add paging or raise limit with server-side paging.

## F82 [FIXED s2] Relief distributions 'Confirm receipt' UI silent failure
- Domain: assignments/tasks/provisions + information & knowledge
- Status: **FIXED 2026-07-10 — confirm/approve UI error callbacks present; dual-proof source.**
- Evidence: relief-distributions.component.ts:172-174 and knowledge-repository.component.ts:167: confirm()/approve() subscribe with next-only handlers — if backend returns 403 (user has recovery.view but not recovery.manage, e.g. DAS/ICT Admin who hold view-only per role_has_permissions) the click does nothing with no error message; list silently stays unchanged.
- Fix hint: Add error callbacks surfacing e.error.detail like save() already does.

- Resolution 2026-07-10: relief `confirm()` and knowledge `approve()` both surface `error.detail`/`message` via alert (no next-only silent fail).
- Verification 2026-07-10: source grep shows error handlers on confirm/approve/save/download paths.

## F83 [FIXED s2] Public subscribe → alert delivery chain
- Domain: portal ↔ system linkage integrity
- Status: **FIXED 2026-07-10** — subscribe→persist (sequence IDs V185); publish `notifyAlertSubscribers` with channel prefs (empty=both), area/hazard match, **alert_level_priority** vs warning severity; Comm Center/EW disseminate still honor AudienceService. Dual-proof SUB-2026-0008/9/10 unique nextval.
- Evidence (historical): Subscribe live-proven; delivery consumers real; residual was count(*)+1 race, empty channels skip, priority ignored.
- Fix hint: Honor channel prefs in AudienceService.collect (skip phone when channels excludes sms, etc.); consider an optional auto-disseminate hook on warning publish filtered by alert_level_priority.

## F84 [FIXED s2] PHR report_code generation: count(*)+1 with no unique index
- Domain: portal ↔ system linkage integrity
- Status: **FIXED 2026-07-10 — phr_report_code_seq + unique index + nextval generator; dual-proof unique codes.**
- Evidence: PortalPublicService.java:270-271 builds PHR-YYYY-NNNNN from select count(*)+1; pg_indexes on public_hazard_reports shows NO unique index on report_code (only pkey + 3 non-unique). Two concurrent submissions, or any future row deletion, silently mints duplicate citizen reference codes — the known DMIS count(*)+1 recurring pattern. Currently 0 duplicates in 17 rows (psql group-by check). alert_subscriptions.subscription_id uses the same pattern but IS protected by a unique index (alert_subscriptions_subscription_id_key), so a race there errors instead of duplicating.
- Fix hint: Unique index on report_code + generate from a sequence (or retry-on-conflict), matching the fix pattern used elsewhere.

- Resolution 2026-07-10: `public.phr_report_code_seq` + unique index on `report_code`; `PortalPublicService` uses `nextval` not `count(*)+1`.
- Verification 2026-07-10: three POSTs `/v1/portal/report-hazard` → PHR-2026-00022/23/24 unique; seq last_value=24.
## F85 [FIXED s2] Live-probe caveat: 'anonymous' 200s on /ew endpoints are the local-profile dev persona, not a production auth hole
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-10 — documented as local-persona behaviour (F85 honesty on LocalSecurityConfig); not a prod hole.**
- Evidence: Anonymous GET /api/ew/stakeholders -> 200 with stakeholder PII and anonymous POST /api/ew/disseminate reached the method body (422 business validation, not 401) on the running stack. Cause: LocalSecurityConfig (@Profile('local'), LocalSecurityConfig.java:53-58) lets LocalAuthFilter authenticate tokenless requests as a full-role persona, satisfying @PreAuthorize. The !local chain (SecurityConfig.java:44 + SecurityPaths.java:33 'no EW path is publicly open') requires a bearer token. Production behavior not testable from this environment - flagging so the dead /ew endpoints are understood as dead
- Fix hint: When removing the 5 dead /ew handlers, no prod security change is needed; if they are kept, re-verify the !local chain blocks them anonymously in a staging deploy.

- Resolution 2026-07-10: Class-level documentation on `LocalSecurityConfig` states anonymous local 200s = persona, not public exposure; production `SecurityConfig` still requires bearer. Residual: re-smoke with explicit `!local` profile on staging when available.
- Verification 2026-07-10: source javadoc present; SecurityPaths still excludes `/v1/ew/**` from PUBLIC_PATHS.

## F86 [FIXED s1] Legacy workflow statuses shipped as live filter options; unused transition() helper
- Domain: Incident lifecycle depth
- Status: **FIXED 2026-07-10 — live WORKFLOW_STATUSES filters drop legacy stages; labels kept for residual rows; dual-proof form-data.**
- Evidence: form-data workflow_statuses returns all 22 entries including 10 unreachable legacy ones (live: rolled_back_to_district/das/regional/national, waiting_*_approval...) which the registry renders as filter options (incidents.component.ts:63-65, IncidentController.java:191, IncidentOptions.java:90-101); psql: 0 incidents in any of them. IncidentWorkflowService.transition() (462-466) has no callers (grep across response/: only the definition). resolveStageRecipients 'rolled_back_to_district' case (570) equally unreachable.

- Resolution 2026-07-10: `IncidentOptions.WORKFLOW_STATUSES` is live ladder only (12 keys). Legacy labels moved to `WORKFLOW_STATUS_LEGACY_LABELS` for history display only.
- Verification 2026-07-10: form-data `workflow_statuses` has waiting_ddmc…waiting_ps, no waiting_das_approval/rolled_back_to_*.
## F87 [FIXED s1] GET /v1/ew/scanner/stats - redundant duplicate of the stats block already embedded in the /detections payload the UI consumes
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-06 (Wave 3) — standalone handler deleted; VERIFIED live: /stats → 404 (admin + mow, independently re-probed), /detections still returns the embedded stats block driving the console's 4 stat cards (UI screenshot clean).**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/ew/scanner/ScannerController.java:131-132; the detections endpoint returns Map.of('detections', rows, 'stats', stats()) at :128 and disaster-scanner.component.ts:449 consumes that. Live GET /stats -> 200 {total:60,new:35,...}; grep 'scanner/stats' frontend/src -> 0.
- Fix hint: Remove the standalone /stats handler.

## F88 [FIXED s1] GET /v1/settings/translations/map - full EN/SW key map endpoint with no consumer (public uses /v1/portal/i18n, admin uses the paged list)
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-06 (Wave 3) — /map handler deleted after a fresh zero-consumer grep; VERIFIED live: GET → 405 (path still matched by the sibling /{id} PUT/DELETE mappings — expected, handler gone; independently re-probed), Translations admin list/create/edit/delete round-trip intact (214 rows), /v1/portal/i18n unchanged at 214 keys == pre-fix baseline.**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/settings/TranslationController.java:75. Live GET -> 200 {lbl_about:{en,sw}...}. translations.component.ts calls only list/PUT/POST/DELETE on the base (:125-176); portal-i18n.ts hydrates from /v1/portal/i18n. grep 'translations/map' -> 0.
- Fix hint: Delete, or repoint PortalLabels hydration at it if a single authoritative map is wanted.

## F89 [FIXED s1] LocationDto record - referenced by nothing in main or test
- Domain: Dead code + unproductive endpoints hunt
- Status: **FIXED 2026-07-06 (Wave 3) — file deleted after a fresh zero-reference grep (main+test); build compiles clean and boots (V140/V141 run proves the jar).**
- Evidence: backend/src/main/java/tz/go/pmo/dmis/common/web/LocationDto.java:6 (public record); class-name grep across backend/src (main+test) -> 0 external references (dead-class scan output).
- Fix hint: Delete the file.

## F90 [FIXED s1] Dispatch receive-side: no destination-stock update for incident deliveries
- Domain: Warehouse management
- Status: **FIXED 2026-07-10 — documented as intentional field consumption; API returns honest stockEffect; dual-proof Deployed message.**
- Evidence: Direct answer to the round-trip question: allocations dispatched to an INCIDENT are consumption — In Transit→Deployed→Delivered (ResourceAllocationController.java:336-346) only stamps timestamps/received_by; no destination store exists and no stock is created anywhere, which is defensible relief-distribution semantics. Destination stock IS updated in the flows that have a destination store: transfers, borrows/returns, procurement deliveries, donation receipts (all verified above). The one hole in the loop is the 'Returned' case reported separately. deployed_from_warehouse is informational only

- Resolution 2026-07-10: Class javadoc + `POST /allocations/{id}/status` response include `stockEffect` explaining Deployed/Delivered = consumption (stock already deducted at dispatch). **Not** inventing a fictional incident warehouse.
- Verification 2026-07-10: POST status Deployed on alloc In Transit → 200 with `stockEffect` containing "field consumption" / "no destination warehouse".

## F91 [FIXED s1] Standalone routes m/content-management/sms-management + email-management reachable only by typing the URL - components now live embedded in Communication Center
- Domain: Dead code + unproductive endpoints hunt
- Status: **CLOSED 2026-07-06 (already fixed, no Wave-3 code change) — the exact redirects the ledger asked for already exist in app.routes.ts (committed 32a50c5); VERIFIED live: both URLs redirect to /m/content-management/communication-center and the embedded SMS/Email management panels render there (puppeteer 5/5).**
- Evidence: app.routes.ts defines both; no routerLink/navigate/modules.ts nav entry composes them (route-vs-nav cross-check: not in modules.ts paths; Communication Center embeds them instead - communication-center.component.ts:119-121 '<page-sms-management [embedded]=true>'). Components themselves are alive and working.
- Fix hint: Remove the leftover standalone routes or add redirects to communication-center.

## F92 [FIXED s3] Scanner entity-taskings READ endpoint not agency-scoped — any authenticated agency user can read other agencies' taskings
- Domain: Dissemination flows / least privilege (found by the Wave-2 F13 adversarial re-check, 2026-07-06)
- Status: **FIXED 2026-07-06 (Wave 3) — GET /entity-taskings now carries the mutation guard's exact rule: agency login (users.agency_id set) reads only its own inbox (explicit other-agency filter → 403 ProblemDetail w/ detail+message; missing filter forced to own agency), national/EOCC (agency-less, same bypass as assertOwnAgency + ewAgencyGuard) reads all; awaiting/responded counters scoped to the effective filter; VERIFIED live + independently re-probed: mow→tma 403, mow→own 200, admin→tma 200; EOCC dispatch console still lists all 12 taskings across 6 agencies (UI screenshot), MoW inbox (F13) unaffected.**
- Evidence: assertOwnAgency (ScannerController.java:70-77) guards only the MUTATIONS (acknowledge/respond/review); GET /v1/ew/scanner/entity-taskings?agency=X returns any agency's taskings to any authenticated agency user — live-proven: mow@pmo.go.tz successfully read agency=tma taskings (expected 403, got 200). Pre-existing behavior, NOT introduced or worsened by F13 (ScannerController has no working-tree changes); consistent with the fine-grained-RBAC follow-on that per-action gates are still TODO.
- Fix hint: Apply the same assertOwnAgency check (with an EOCC/national bypass) to the GET handler, mirroring the mutation guards.

## F93 [FIXED s2] Official-source portal report INSERT (PortalPublicService.reportHazard) also omits region_name/district_name
- Domain: Incident lifecycle depth (found by the Wave-3 F24 fixer, 2026-07-06)
- Status: **FIXED 2026-07-09 — official-source public-portal submissions now create linked EOCC incidents with populated region/district names, verified live.**
- Evidence: The trusted institution/sector/ministry path (PortalPublicService.reportHazard ~line 296, straight to a waiting_eocc incident) has the same *_name omission F24 fixed in the citizen conversion path. V141 backfilled its existing rows and F70's id-fallback keeps its notifications clean, so the symptom is currently invisible — but each NEW official-source incident is born with NULL names until this INSERT is fixed too.
- Implementation 2026-07-08: `PortalPublicService.reportHazard` now derives `regionId` from `districtId` when only the district is supplied, and the official-source incident INSERT writes `region_name` and `district_name` via subselects from `public.regions` / `public.districts`. Backend compile passed after the change.
- Verification 2026-07-09: live `POST /api/v1/portal/report-hazard` with a controlled institution report for Dodoma Urban returned a `reportCode` plus `incidentId`; SQL proved the linked incident was `workflow_status='waiting_eocc'`, `origin_level='national'`, `region_id=1`, `district_id=101`, `region_name='Dodoma'`, `district_name='Dodoma Urban'`, and the source `public_hazard_reports` row was `status='converted'` with `linked_incident_id` set. Controlled report/incident rows were deleted and follow-up SQL showed zero `SMOKE-OFFICIAL-*` leftovers.
- Fix hint: Closed. Keep any future official-source portal insert in parity with the citizen conversion area-name contract.

## F94 [FIXED s4] Area coordinator roles over-granted: authoring/command-tier permissions + partner portal on every area officer's menu
- Domain: RBAC / least privilege (user-reported live 2026-07-06: "in RAS I see a lot, i.e. Preparedness, Early Warnings etc.")
- Status: **FIXED 2026-07-08 — area roles now receive warnings through Response → Issued Alerts, not the Preparedness EW workbench; VERIFIED with migrated DB, fresh-login browser smoke, and EW API/report 403s.**
- Evidence: Valid partial closure remains: V142 revoked `early_warning.create`, `one_health.manage`, `one_health.disseminate`, `preparedness.manage`, `command_post.activate`, `command_post.posture`, and `budget_and_finance.manage` from `RAS`, `Reg DC`, `DED`, and `DAS`; V143 created `stakeholder_portal.view`, granted it only to Partners + national coordination roles, and removed `one_health.view` / `one_health.acknowledge` from those four area roles; `access.ts` gates EW authoring consoles on `early_warning.create` and the partner portal on `stakeholder_portal.view`. Verified RAS evidence remains true: fresh RAS hub 10→4 cards, RAS gets 403 on scanner/scan, eocc/activate, and onehealth/events, while MoW/EOCC/Partners regressions were clean. Reopen evidence: V103 still explicitly grants `one_health.acknowledge` to `Dist DC`; V105 grants `<module>.view` to roles holding any action in that module; V143's comment says area coordinator roles should keep no One Health grants, but its delete list omits `Dist DC`. Already-issued JWTs also keep the pre-trim permission list until logout/expiry.
- Annotation (2026-07-18 reassessment): **V199 deliberately re-granted `early_warning.view` to the area-role family** (reversing V154's removal) so area officers see warnings affecting their own area — live-verified region-scoped (RAS sees only Dodoma's 4 warnings, == SQL; summary identity holds). The authoring/command boundary (no early_warning.create / command_post.activate|posture / one_health.* / budget manage / preparedness.manage on any of the 7 area roles) re-verified intact 2026-07-18, zero grants. Note: the V154-era smoke line "EOCC denied for area roles" is superseded by the F104 closure (region-scoped 200) — live behavior is region-scoped access, which is the intended end state.
- Implementation 2026-07-08: V144 deletes `one_health.%`, EW authoring/dissemination/approval, `preparedness.manage`, `command_post.activate`, `command_post.posture`, and `stakeholder_portal.view` from `RAS`, `RC`, `Reg DC`, `DED`, `DAS`, `Dist DC`, `District Commissioner`, and planning/logistic area variants. It also removes `budget_and_finance.manage` from area executives (`RAS`, `RC`, `Reg DC`, `DED`, `DAS`, `Dist DC`, `District Commissioner`) while intentionally preserving V99 finance permissions for planning/logistic variants; those scope/simulation risks were later closed under F110/F111. Backend compile and frontend production build passed after V144.
- Implementation 2026-07-08 follow-up: V154 removes the residual `early_warning.view` grant from the same area-role family. Frontend route/module guards now require `early_warning.create` for `/m/preparedness/early-warnings`, remove `early_warning.view` from the Preparedness card, add read-only `/m/response/issued-alerts` under the Response module for incident-role users, and make module cards count/link only to routes the caller can open. `ModuleGuardFilter` now maps `/v1/reports/early-warnings` to `early_warning.view` instead of the broad reports gate.
- Verification 2026-07-08: V154 is recorded in `platform.flyway_schema_history`; SQL across `RAS`, `RC`, `Reg DC`, `DED`, `DAS`, `Dist DC`, `District Commissioner`, `Regional/District Planning Officer`, and `Regional/District Logistic Officer` showed no forbidden `early_warning.%`, `one_health.%`, `stakeholder_portal.view`, `command_post.activate`, or `command_post.posture` grants. The five planning/logistic/District Commissioner variants have zero seeded users, so they were DB-verified rather than browser-smoked. Fresh-login headless Chrome/API smoke for seeded `RAS`, `Reg DC`, `DED`, `DAS`, `Dist DC`, and `RC` proved: Preparedness EW hub denied, Response Issued Alerts open, Stakeholder Issued Alerts denied, EW Management report denied, EOCC/Executive Watch denied, One Health denied, `/api/v1/ew/products` 403, `/api/v1/reports/early-warnings` 403, and `/api/v1/portal/landing` 200. Regression smoke proved EOCC still opens EW hub, EOCC Command, Executive Watch, EW Management report, EW product/report APIs; Partner still opens Stakeholder Issued Alerts and is denied Response/EW/EOCC/One Health.
- Fix hint: Closed. Preserve the doctrine: area officers can work incident/resource/preparedness duties in their jurisdiction and read official issued alerts, but the EW workbench, EW effectiveness analytics, EOCC command dashboards, One Health, and partner portal remain separate permission surfaces.

## F95 [FIXED s3] Communication Center has a split permission model: Content Management route vs Communication & Alerts APIs
- Domain: notifications + email/SMS coverage / RBAC consistency (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — Communication Center route, logs, overview, audiences, and compose controls now align on `communication_and_alerts.*`; live API/browser RBAC smoke passed.**
- Evidence: The frontend exposes `/m/content-management/communication-center` under the Content Management module (`access.ts` maps `/m/content-management` to `content_management.view`, `modules.ts` describes it as SMS/email/logs). But the overview and audience endpoints used by that screen are `/v1/communication/overview` and `/v1/communication/audiences`, both method-gated with `communication_and_alerts.view` (`CommunicationOverviewController.java:31-62`). The embedded SMS/email log endpoints sit under `/v1/content/*-logs`, so `ModuleGuardFilter` gates their GETs with `content_management.view`; only `/send` is method-gated with `communication_and_alerts.send` (`SmsLogController.java:42-82`, `EmailLogController.java:42-82`). Result: a content-management-only user can route into the screen and read logs, but the overview/audience controls 403; a communication officer without content_management.view can satisfy the overview/send API permission but be blocked by the frontend route/module doctrine. This is not a proved live exploit, but the code contract is internally inconsistent.
- Implementation note 2026-07-09: Communication Center is now canonicalized to `communication_and_alerts`. Frontend route/module access includes `communication_and_alerts.view`, the exact communication-center route requires it, `ModuleGuardFilter` maps `/v1/communication`, `/v1/content/sms-logs`, and `/v1/content/email-logs` to `communication_and_alerts.view`, and SMS/email compose panels are only rendered for `communication_and_alerts.send`. `V164` labels plus the Roles & Permissions control map describe the split for admins. Backend package, frontend build, and `git diff --check` passed. Added `frontend/render-tests/f95-communication-rbac-smoke.js`; verification passed (`F95 Communication Center RBAC smoke passed: codex-f95-1783607153232`). The smoke created temporary local-profile roles proving content-management-only users get 403 on overview/audiences/SMS logs/email logs, communication-view users get 200 on overview/audiences/logs but 403 on send endpoints, communication-send users reach send methods without real gateway delivery, content-only frontend sessions land on access denied, communication-view sessions see logs with no compose panels, and communication-send sessions see SMS/email compose controls. Controlled temporary roles were cleaned up.

## F96 [FIXED s3] Restricted operational `/storage/**` files require authentication but not row/jurisdiction authorization
- Domain: File/attachment authorization / portal ↔ system linkage integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-10** — RestrictedStorageAccessFilter: module auth + AreaGuard on assessments + mapped incident media; unmapped orphans **404** for non–Super Admin (fail-closed). Dual-proof: DAS orphan → 404; Super Admin orphan → 200; mapped → 200.
- Evidence: `SecurityPaths.RESTRICTED_STORAGE_PATHS` correctly carves out `/storage/assessments/**`, `/incident_photos/**`, `/incident_videos/**`, `/dissemination_uploads/**`, `/warnings/**`, and `/reports/**` from public access and requires authentication. But the bytes are still served by the static `PublicStorageConfig` resource handler (`/storage/**` → filesystem), so no controller ever looks up the owning assessment/incident/generated_report/warning/dissemination row or runs `AreaGuard` / module permission checks. The code comment in `SecurityPaths.java:55-57` explicitly admits this follow-up. Concrete example: `GeneratedReportsController.index()` area-scopes the registry rows through the linked incident, and `GeneratedDocumentsComponent.open()` fetches `/api/storage/${file_path}` with the bearer token; however `/storage/reports/dlna/<ref>-r<id>.pdf` itself is auth-only, not report-row-scoped. Same static limitation applies to restricted assessment photos and incident media loaded through `SecureMediaService`.
- Fix hint: Keep public assets on static `/storage/**`, but move restricted prefixes behind controller download endpoints such as `/v1/reports/generated/{id}/download`, `/v1/response/assessments/photos/{id}`, etc. Each endpoint should resolve the owning row, run the same area/module guard as the screen that reveals it, then stream the file.

- Resolution 2026-07-10: `RestrictedStorageAccessFilter` — module authorities by restricted prefix; **AreaGuard** on `/storage/assessments/{id}/…`. Residual: free-form `incident_photos/*` names still auth+module only (no embedded parent id).
- Verification 2026-07-10: unauth assessments path → **401**; Super Admin with real file → **200**.

## F97 [FIXED s2] Recovery modules mix area-scoped rows with national stats/lookups and unguarded mutation targets
- Domain: Recovery scope/data integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-10** — RecoveryProgramController + ReliefDistributionController share area predicate on stats/breakdowns/form incidents; mutations AreaGuard. Dual-proof DAS form incidents 6 vs admin 23; cross-area program create → 403.
- Evidence (historical): `RecoveryProgramController.index()` appends `jurisdiction.appendAreaScopeSharedOrOwn("i", where, params)` to the main row list, but its `stats`, `by_type`, `agencies`, and `incidents` queries ignore that predicate and return national data/form-data (`RecoveryProgramController.java:56-89`). `ReliefDistributionController.index()` repeats the same pattern: scoped `distributions` rows, then national `stats`, `by_resource`, all resources, all incidents, all agencies (`ReliefDistributionController.java:56-93`). Mutations also do not validate target jurisdiction: `RecoveryProgramController.store()` accepts an arbitrary `incident_id` without `areaGuard.assertOwnOrShared`, `setStatus()` updates by id only, `ReliefDistributionController.store()` accepts arbitrary `incident_id`, and `confirm()` updates by id only. This mirrors the class of mixed-scope dashboard problem fixed in F35, but in Recovery.
- Fix hint: Reuse the same area predicate for stats/breakdowns/form-data, or clearly label them national. On create/status/confirm, resolve the target incident/row and run the area guard before writing; otherwise an area-tier recovery user can see local rows beside national totals and can potentially mutate a cross-area id if granted `recovery.manage`.

## F98 [FIXED s2] Strategic Projects `entry_id` generation is race-prone (`max(id)+1`) despite a unique constraint
- Domain: Recovery scope/data integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-10 — entry_id derived from inserted id (not max+1); dual-proof SP-0007.**
- Evidence: `StrategicProjectController.store()` computes `seq = select coalesce(max(id),0)+1 from public.strategic_projects`, formats `SP-%04d`, then inserts the row (`StrategicProjectController.java:71-90`). The schema does have `strategic_projects_entry_id_key UNIQUE (entry_id)`, so concurrent creates will not silently duplicate, but one of them can still fail with a database duplicate-key error instead of a clean retry/business response. The local seeder uses the same pattern (`RecoveryLocalSeeder.java`), and this is the same ID-generation smell already tracked elsewhere for public report codes (F84), just on a lower-volume recovery register.
- Fix hint: Generate `entry_id` from the inserted row id after insert, use a dedicated sequence, or catch duplicate-key and retry. Keep the unique index, but do not depend on it as the normal concurrency control path.

- Resolution 2026-07-10: insert with provisional unique entry_id, then set `SP-{id:04d}` from returned id.
- Verification 2026-07-10: POST strategic-projects → 200 `entry_id=SP-0007` id=7.
## F99 [FIXED s3] System Design Document is materially stale against current migrations, RBAC, incident ladder and outbox reality
- Domain: Supporting documentation / architecture control (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-10 (key claims)** — document control verified V183; RBAC/migration/outbox honesty; residual prose drift possible.
- Evidence: `docs/SYSTEM-DESIGN-DOCUMENT.md` is no longer safe as a current implementation contract in several load-bearing sections. It says Hibernate/Flyway has "79 migrations (`V1`...`V95`)" (`SYSTEM-DESIGN-DOCUMENT.md:143`), but the migration directory now contains 127 migration files and ends at `V143__stakeholder_portal_permission_and_onehealth_trim.sql`. It says authorization is role-based only and "there are zero `hasAuthority`/`hasPermission` checks" (`SYSTEM-DESIGN-DOCUMENT.md:465`, `:527`, `:673`), but the current backend contains 294 `hasAuthority` / `hasPermission` / `hasAnyAuthority` matches, and `V102__rbac_settings_matrix_authority.sql:1-4` explicitly moved controllers to `hasAuthority(<perm>)`; `V143__stakeholder_portal_permission_and_onehealth_trim.sql:4-11` further treats module permissions as live route/menu authorities. It says module collaboration is event-driven through the outbox (`SYSTEM-DESIGN-DOCUMENT.md:132`), while the standing audit still tracks the outbox as dead machinery (F18) rather than the actual integration backbone. The incident section is also behind the current workflow: it documents `waiting_das_approval` / `waiting_ras_approval` / assistant-director legacy stages (`SYSTEM-DESIGN-DOCUMENT.md:1459-1468`), while the current service and incident test guide use the DDMC→DED→RDMC→RAS→EOCC→Director→PS ladder with skip-if-unstaffed automation (`IncidentWorkflowService.java:58-98`, `V97__incident_workflow_ladder_migrate.sql`, `V133__incident_approval_automation.sql`, `DMIS-INCIDENT-TEST-GUIDE.md:13-17`). This is not a code failure by itself, but it causes audit, onboarding and future fixes to start from a false map.
- Fix hint: Regenerate/update the SDD from the current codebase and migrations before treating it as policy. Minimum refresh: migration count/range, security/RBAC section, permission matrix semantics, incident lifecycle section, communication-center permission doctrine, outbox status ("designed but not wired" unless F18 is fixed), and any module endpoint tables that still describe old role gates. Add a small "last verified against migration V___ / commit ___" header so future drift is obvious.

## F100 [FIXED s2] Public Reports triage list is area-scoped, but its stat cards are national
- Domain: Incident lifecycle depth / public report triage scope (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — public-report stat cards now count the same scoped/search-filtered row population the caller can see.**
- Evidence: `PublicReportsController.index()` builds a scoped `where` clause, applies status/search filters, and then calls `jurisdiction.appendAreaScopeSharedOrOwn("r", where, params)` before listing `public_hazard_reports` (`PublicReportsController.java:51-78`). That means the report rows a DDMC/RAS sees are own-area plus shared/null-area, as designed. But the `stats` block immediately after ignores that same predicate and runs `select count(*) ... from public.public_hazard_reports` with no `where` and no params (`PublicReportsController.java:79-86`). Result: an area officer's triage queue can show local rows beside national totals for new/reviewing/converted/dismissed reports. This is the same mixed-scope pattern previously fixed for Response dashboard stats (F35) and newly found in Recovery (F97), but on the public-report triage desk.
- Implementation note 2026-07-08: `PublicReportsController.index()` now runs the stats aggregation from `public_hazard_reports r where <same scoped/status/search predicate>` using the same params as the visible list, so the cards count the same filtered jurisdiction population. Backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`.
- Verification 2026-07-09: controlled public-report rows were inserted for Dodoma Urban, Dodoma-region-only, untagged, Ilala, and Dar-region-only, then queried through live `GET /api/v1/response/public-reports?search=<marker>` with local role personas. `Dist DC` saw 2 rows / `stats.total=2` (own district + untagged, no Dar rows), `RAS` saw 3 rows / `stats.total=3` (Dodoma district + Dodoma region + untagged, no Dar rows), and `EOCC` saw all 5 / `stats.total=5`. Controlled rows were deleted and follow-up SQL showed zero `SMOKE-TRIAGE-*` leftovers.
- Fix hint: Closed. Reuse the scoped `where` and params for future public-report totals/breakdowns.

## F101 [FIXED s3] Manual incident creation is not guarded against out-of-area targets; form-data also exposes all assignable users
- Domain: Incident lifecycle depth / jurisdiction integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — manual incident create/update and assignee form-data are now area-scoped and live role-smoked.**
- Evidence: Existing incident rows are strongly scoped after creation: `index()` and `show()` append `JurisdictionScope`, and update/operational actions call `areaGuard.assertOwn(...)`. The create path is different. `IncidentController.store()` is gated only by `incidents.create`, validates hazard/status/etc., derives `region_id` from the posted `district_id`, and inserts the incident (`IncidentController.java:198-260`) without checking that the acting area-tier user is allowed to create in that posted district/region. The validator does not validate `region_id`, `district_id`, or `assigned_to_user_id`; `formData()` returns every region and `assignable_users = select id, name from public.users order by name` (`IncidentController.java:177-184`), and the insert accepts the posted assignee directly (`IncidentController.java:229-245`). So a district/area actor with `incidents.create` can post another district/region's id, create a row outside their own area, assign it to any user name exposed by form-data, and then likely lose visibility because list/show scope applies after the row exists. Public report conversion is better guarded through `findOr404()` / `areaGuard.assertOwnOrShared`, so this gap is specific to the manual incident form/API.
- Implementation note 2026-07-08: `IncidentController.formData()` now uses `assignableUsers()` instead of exposing every user; assignable users must hold an incident permission, must not be stakeholder-linked, and are scoped to the caller's region/district unless the caller is national. `store()` and `update()` now validate posted/derived region+district existence and reject non-national callers whose target is outside their own region/district; both paths also validate `assigned_to_user_id` against the same scoped assignable-user set before writing. Malformed region/district/assignee ids now return validation errors instead of falling through to numeric parse exceptions. Backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`.
- Verification 2026-07-09: live role smoke with `X-Local-Roles: Dist DC` proved `GET /api/v1/response/incidents/form-data` returned only Dodoma Urban incident-capable users (`DED Dodoma`, `District Admin Secretary`, `District Coordinator`) and SQL found zero returned assignees outside district `101`. Dist DC own-district create returned HTTP 200 and SQL proved `region_id=1`, `district_id=101`, `region_name='Dodoma'`, `district_name='Dodoma Urban'`, `workflow_status='draft'`; foreign Ilala create returned HTTP 422 with `district_id: You can only log incidents for your own district` and inserted no row; own-district create with foreign assignee `30` returned HTTP 422 with `assigned_to_user_id`; update of the own incident to Ilala returned HTTP 422 and SQL proved the row stayed in Dodoma Urban. `X-Local-Roles: EOCC` could still create an Ilala incident and SQL proved `region_name='Dar es Salaam'`, `district_name='Ilala'`. Controlled incident rows were deleted and follow-up SQL showed zero `SMOKE-INC-*` leftovers.
- Fix hint: Closed. Preserve create/update target validation and area-scoped assignable users whenever the incident form grows.

## F102 [FIXED s3] Backend regression tests are not self-contained; Spring tests require a live dev Postgres
- Domain: Production readiness / CI and testability (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-14 — every Spring integration suite now uses one hermetic PostgreSQL 16 Testcontainer; the full backend gate runs without a manually provisioned dev database.**
- Evidence: Frontend production build is clean (`npm run build -- --configuration production` in `frontend`). Backend compile is also clean when the documented local toolchain is used: `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile` compiles 248 main sources. Backend regression verification is weaker: `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B test` compiles 248 main + 19 test sources, then Spring contexts repeatedly retry Hikari against the local datasource when `localhost:5440` is not running; the run was interrupted after more than two minutes without a useful fail-fast error. This is not just this shell: `ci.sh:6-8` documents that the gates need dev Postgres at `localhost:5440`, while `backend/pom.xml:120-130` already declares Testcontainers PostgreSQL/JUnit dependencies. The project therefore has the ingredients for hermetic DB tests, but the live `@SpringBootTest` gate still depends on a manually provisioned database, making international/production CI portability brittle.
- Reexam 2026-07-08: static sweep also found `SecurityEnforcementTest.ewDisseminateDeniesFieldRole()` still posts to the deleted F01 `/ew/disseminate` endpoint and expects 403. Since the endpoint is intentionally removed, this is another regression-suite cleanup item under F102.
- Implementation 2026-07-14: all 12 ordinary `@SpringBootTest` classes now extend `HermeticPostgresSupport`. The shared base loads `db/baseline/baseline.sql`, applies Flyway V123-V208, supplies the datasource through `@DynamicPropertySource`, uses a five-second connection timeout, fails clearly if the baseline cannot be located, and keeps one suite-level container alive while Spring reuses its cached context. `FinanceWorkflowIntegrationTest` was also updated to use the current finance doctrine (District Planning Officer request, DED approval, District Logistic Officer commitment/disbursement) with four distinct real seeded users in one discovered district; stale security expectations were aligned with fail-closed malformed bearer handling and the V204 resource-approval chain. The RBAC coverage scan now targets the actual consolidated controller package instead of obsolete feature packages, with narrow method-level exceptions for public/self-service operations.
- Verification 2026-07-14: `RUN_TESTCONTAINERS=true mvn -B verify` connected to Docker Engine 29.5.2, started PostgreSQL 16, loaded the baseline, validated 193 migrations, applied 86 migrations through V208, and completed **125 tests, 0 failures, 0 errors, 0 skipped**. The previously failing finance suite passed 7/7; `SecurityEnforcementTest` passed 29/29; the non-vacuous all-controller RBAC scan passed. Without Docker, the same command finishes quickly with explicit Testcontainers skips instead of retrying `localhost:5440`; `ci.sh` requires Docker so a release gate cannot turn those skips into a false green.
- Reverification 2026-07-15: Java 21/PostgreSQL 16.13 validated 197 Flyway schema-history entries (the baseline marker plus 196 versioned SQL files through V212); isolated Docker-backed verification executed **186/186 with no failures, errors, or skips**. No scheduled external scanner calls appeared and Surefire exited normally.
- Fix hint: Closed. Keep new database-backed Spring tests on `HermeticPostgresSupport`; keep `ci.sh` Docker-required and preserve the non-zero all-controller RBAC scan.

### Backend truthfulness hardening (2026-07-14; cross-cutting, no new F-number)

- `MitigationDashboardServiceImpl`, `GisMapServiceImpl`, and `RegionDataBuilder` no longer convert database failures into plausible zero/empty dashboard and GIS responses. Failures now propagate to the standard API error path instead of presenting missing data as a valid operational picture.
- `ReadModelFailurePropagationTest` injects a failing JDBC connection and proves all three read models surface the database failure. It passed 3/3 inside the current 127-test hermetic run.
- This closes the specific silent-fallback defects found in this pass; it is not a claim that deeper review cannot discover another truthfulness defect elsewhere.

## F103 [FIXED s2] Generic `/m/:slug` fallback masks bad or legacy module URLs as polished but nonfunctional pages
- Domain: Dead code + unproductive endpoints hunt / click-through integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — unknown authenticated `/m/...` routes show real not-found, known explicit routes still load, and unauthenticated protected routes still go to login.**
- Evidence: A route-vs-menu scan found `module items 77`, `explicit matches 77`, and `fall through to ScreenComponent 0`, so current module navigation no longer needs the generic host for real menu items. Before the fix, `app.routes.ts` caught any authenticated `/m/:slug/:item` or `/m/:slug` URL and loaded `ScreenComponent`; that component explicitly said the body would be reproduced "next" and rendered only a header/card with the item icon/name/description, with no API, table, form, actions, error state, or "not implemented" signal. Result: typo, stale, bookmarked, or guessed module URLs could look like valid DMIS feature pages while doing nothing.
- Implementation 2026-07-08: `app.routes.ts` now routes unknown authenticated `/m/:slug/:item` and `/m/:slug` paths to a real `NotFoundComponent`, and the generic `ScreenComponent` placeholder was deleted. Source grep now finds no remaining `ScreenComponent`/`screen.component` references; frontend production build passed with `npm run build -- --configuration production`.
- Verification 2026-07-09: headless Chrome against the running local stack proved unauthenticated `/m/prevention-mitigation/risk-index` redirected to `/login` with the login form, authenticated `/m/no-such-module/no-such-item` rendered `Page not found` without the INFORM screen or access-denied page, and authenticated `/m/prevention-mitigation/risk-index` rendered the real `INFORM Risk Index` screen without not-found/access-denied.
- Fix hint: Closed. Preserve `NotFoundComponent` fallbacks for unknown authenticated `/m/:slug` and `/m/:slug/:item` URLs, and keep new module screens on explicit routes so bad links cannot look like functional pages.

## F104 [FIXED s3] Centralized national map base is correct, but the current map stack still depends on external Carto raster tiles and direct bypasses
- Domain: Production readiness / GIS resilience / system integrations (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — local-first governed map base plus district/region/national scope behavior live-smoked in browser.**
- Evidence: The user's centralization concept is sound: `addTanzaniaGisBase()` is a single shared Tanzania base map helper intended to make one update reflect across most maps. Before the fix, the helper hardcoded Carto Voyager raster tiles and multiple components bypassed it with their own direct `L.tileLayer(...)` calls, including One Health dashboard, mitigation risk form/dashboard/GIS map, EW region picker, MoW flood, and DMD consolidated impact map. The repo already had local `/geojson/...` plus vendored Leaflet assets, so the system was close to a resilient national GIS base but still anchored to third-party raster availability and inconsistent call sites.
- Implementation 2026-07-08: `tz-map.ts` now exposes `addDmisBaseLayer()` as the governed base-layer registry. Production/development `environment.mapTiles.allowExternal` defaults to `false`, so the operational base is local Tanzania vector GeoJSON; optional Carto URLs live only in environment config and are used only if explicitly enabled. If external tiles are enabled and fail, the helper falls back to local Tanzania layers and displays a base-map offline notice. The seven direct feature-level `L.tileLayer(...)` call sites were changed to `addDmisBaseLayer(...)`; source scan now finds Carto URLs only in environment config and `L.tileLayer(...)` only inside `tz-map.ts`. Frontend production build passed with `npm run build -- --configuration production`.
- Additional hardening 2026-07-08: `tz-map.ts` now exposes `applyDmisMapJurisdiction()`, a shared internal-map presentation guard. Response dashboard/EOCC maps call it with the backend `my_area` payload: district users get a masked, panning-bounded own-district map; region users get a masked, panning-bounded own-region map with all districts in that region drawn; national users keep the whole-country view. This is presentation defense only; backend `JurisdictionScope` remains the security boundary. Frontend production build passed after the change.
- Verification 2026-07-09: local stack smoke ran against Angular `http://127.0.0.1:4200` and a freshly built Java 21 backend jar on `http://127.0.0.1:8080`. Direct API proof showed `dc@test.com` dashboard `my_area={scope:DISTRICT, district:Dodoma Urban, region:Dodoma}`, `ras@pmo.go.tz` dashboard/EOCC `my_area={scope:REGION, region:Dodoma}`, and `eocc@pmo.go.tz` dashboard/EOCC `my_area={scope:NATIONAL}`; DC EOCC API was 403. Headless Chrome smoke passed 12/12 cases with full JSON saved at `/tmp/f104-smoke-results.json`: district Response dashboard rendered `resp-map` 770x500 with 152 layers, local vector base, no external tiles, district mask/fill, and maxBounds set; RAS Response dashboard rendered 161 layers with region mask/fill, 7 district boundary paths, and maxBounds set; EOCC Response dashboard and EOCC board rendered national max-bounded maps; DC/RAS EOCC route denied to `/access-denied`; Mitigation dashboard, GIS Map, Risk Assessment form (Media & Maps tab), One Health dashboard, MoW flood map, and DMD consolidated map all rendered nonblank local-vector maps with `externalTiles=0`, failed requests `0`, bad responses `0`, and page errors `0`. Focused EW region-picker smoke on `/m/preparedness/early-warnings/gst` rendered `rp-0` 650x540 with 127 layers, 116 SVG paths, local vector base, maxBounds, and zero external tiles/failed/bad/page-error events. During the first smoke, `GET /v1/response/dashboard` exposed a real SQL-spacing 500 in the dashboard queue query; that backend bug was fixed before this F104 closure and is documented under still-open F23.
- Fix hint: Closed. Keep future map work on `addDmisBaseLayer()` / `applyDmisMapJurisdiction()` rather than direct `L.tileLayer(...)` calls; if a new operational map is added, smoke it for local-vector rendering, nonblank layers, no external tile dependency, and role-appropriate area focus.

## F105 [GAP s3] AI/ML readiness is not architected yet; current analytics are deterministic engines/scanners, not governed model inference
- Domain: AI/ML accommodation / international production architecture (fresh reassessment, 2026-07-06)
- Status: **OPEN**
- Evidence: The risk analytics that exist are real, but they are deterministic. `InformEngine.computeFromRaw()` standardises raw values, computes weighted means, scaled geometric means, and final risk (`InformEngine.java:47-98`); `InformService.submit()` either clamps a direct 0-10 score or standardises a raw value into a pending governance row (`InformService.java:61-96`). The early-warning scanner uses fixed keyword dictionaries, source fetchers, region extraction, and Java HTTP calls (`DisasterScannerService.java:44-90`, `:250-271`), not model inference. A static scan across runtime source/migrations found no model-inference substrate: no OpenAI/LLM/embedding/vector/model_version/model registry/feature store/inference/ONNX/PMML/TensorFlow/PyTorch references, and no tables for feature snapshots, prediction events, model version audits, confidence/explanation payloads, or human disposition of AI suggestions. This means DMIS can honestly claim deterministic risk scoring and scanning today, but not a production AI/ML-ready architecture.
- Fix hint: Add an AI/ML integration contract before shipping AI-assisted workflows: `model_registry`, `model_run`, `prediction_event`, `feature_snapshot` references, model version and checksum, confidence/explanation fields, human review/accept/reject status, RBAC around AI suggestions, retention rules, and an async integration channel that is actually wired (or fix the dead outbox first). UI copy should label model output as suggestions until a human action makes it operational.

## F106 [FIXED s3] Public Reports conversion UI cannot assign a district for untagged citizen reports
- Domain: Incident lifecycle depth / portal -> system linkage integrity (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — untagged public reports can be assigned to a valid in-area district from the triage UI, with server-side area rejection and live browser/API proof.**
- Evidence: The backend conversion contract was stronger than the UI. `PublicReportsController.convert()` accepted `district_id` / `region_id` in the request body when the public report itself was untagged, and refused conversion with "Assign the incident's district before converting" when no district could be resolved. But `public-reports.component.ts` opened a convert modal with only a severity selector and posted only `{ severity_level: this.convSeverity }`. Result: reports submitted without Region/District could be reviewed or dismissed, but not converted from the actual triage desk.
- Implementation 2026-07-08: `PublicReportsController.index()` now returns `region_id`, `district_id`, `region_name`, and `district_name` for each public report. `PublicReportsController.convert()` now validates the selected district, verifies district-region consistency, derives the authoritative region from the district, and rejects out-of-area conversion targets for district/region actors. The public reports UI now shows report area, loads Region -> District options in the convert modal for untagged reports, posts `district_id`/`region_id`, and surfaces conversion errors inline instead of using a generic alert. Backend compile and frontend production build both passed.
- Verification 2026-07-09: live API smoke inserted controlled untagged/tagged public reports. Dist DC converting an untagged report to Ilala returned HTTP 422, then converting the same report to Dodoma Urban returned HTTP 200 and SQL proved `waiting_ded`, `origin_level='district'`, `region_id=1`, `district_id=101`, `region_name='Dodoma'`, `district_name='Dodoma Urban'`, source report `converted`. RAS converting an untagged report to Ilala returned HTTP 422, then assigning Dodoma Urban succeeded. A tagged Dodoma Urban report converted without sending an area override and preserved severity/area. Headless Chrome smoke on `http://127.0.0.1:4200/m/response/public-reports` as `dc@test.com` proved the modal blocks missing district inline before any POST, surfaces the foreign-district server error inline, and then converts after selecting Dodoma Urban; SQL proved the browser-created incident rested at `waiting_ded` with the correct Dodoma names/ids. Controlled report/incident rows were deleted and follow-up SQL showed zero `SMOKE-CONVERT-*` / `SMOKE-UI-*` leftovers.
- Fix hint: Closed. Keep public-report conversion as a two-layer control: UI helps pick the district, backend remains authoritative for region/district/caller scope.

## F107 [FIXED s3] SweetAlert raw HTML strings bypass Angular escaping across operational dialogs
- Domain: Frontend security / production hardening (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — SweetAlert API-derived titles/options/bodies are escaped or text-rendered, with hostile-payload browser regression proof.**
- Evidence: Normal Angular templates escape interpolated values, but several operational dialogs build raw SweetAlert `html:` strings from database/user-controlled values. Examples: Communication details and template editing interpolate `d.alert.title`, `d.alert.message`, `t.name`, `t.title`, and `t.message` into SweetAlert title/html/input values (`communication.component.ts:231-268`); Dispatch donation/procurement dialogs interpolate `b.stakeholder_name`, `w.name`, and `w.level` into titles/options/html (`dispatch-console.component.ts:429-489`); Command Center appointment/DRF dialogs build option HTML from user/stakeholder names and role titles (`command-center.component.ts:1118-1121`, `:1282-1285`); Warehouse Ops, Stakeholder Donations, Resource Allocations, and Tasks repeat the pattern for store/resource/incident/user names and history text (`warehouse-ops.component.ts:374-486`, `stakeholder-donations.component.ts:235-301`, `resource-allocations.component.ts:311-313`, `tasks.component.ts:318-321`). Those strings are inserted outside Angular's sanitizer, so malicious HTML stored in a name/message/title can render inside authenticated operator workflows.
- Implementation 2026-07-09: SweetAlert boundaries in Communication, Dispatch Console, Command Center, Warehouse Ops, Stakeholder Donations, Resource Allocations, Tasks, Declarations, Approvals, Assessment Show, Incident Show, Response Settings, Dashboards activation, and Training Plans now use `titleText` for API-derived titles and `escapeHtml()` for API-derived HTML bodies, input values, option labels, history rows, incident/resource/user/stakeholder names, dates, and reference numbers. Numeric IDs/quantities are normalized before insertion into form HTML.
- Verification 2026-07-09: frontend `npm run build` passed and `git diff --check` passed. `node frontend/render-tests/f107-sweetalert-xss-smoke.js` inserted controlled `codex-f107-*` alert/template/user/task rows containing `<img src=x onerror="window.__F107_XSS=1">`, drove headless Chrome through Communication alert details, Communication template edit, and Task reassign SweetAlert dialogs, and passed with `fired=false`, `hostileImageCount=0`, escaped `titleHtml` / option `innerHTML`, and literal payload text preserved in title/body/input/option values. Follow-up SQL cleanup checks returned `alerts=0`, `templates=0`, `users=0`, and `tasks=0` for the controlled prefix.
- Fix hint: Closed. Keep using `titleText`, `escapeHtml()`, numeric normalization, or DOM `textContent` for SweetAlert raw-HTML boundaries; rerun `frontend/render-tests/f107-sweetalert-xss-smoke.js` if a dialog is changed.

## F108 [FIXED s3] Risk Frameworks/Publications permissions are split between Content Management and Prevention & Mitigation, with auth-only reads
- Domain: RBAC consistency / content-publications / mitigation integration (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — framework registry is canonicalized under Content Management permissions and live-smoked through V161 role probes.**
- Evidence: The frontend places Publications and Risk Frameworks under Content Management (`modules.ts:142-143`) and routes them at `/m/content-management/frameworks` (`app.routes.ts:112-117`). The backend controller is tagged as Content Management but implemented in the mitigation package (`FrameworkController.java:23-28`); reads are gated only by `isAuthenticated()` (`FrameworkController.java:32-42`), writes require `prevention_and_mitigation.manage` (`FrameworkController.java:46-65`), and `ModuleGuardFilter` has mappings for `/v1/hazards`, `/v1/mitigation-measures`, `/v1/infrastructure-items`, `/v1/past-disasters`, `/v1/inform`, and `/v1/risk-assessments` but no `/v1/frameworks` mapping (`ModuleGuardFilter.java:56-86`). Live re-check 2026-07-07: `X-Local-Roles: NoSuchRole` gets `200` plus the frameworks payload from `GET /api/v1/frameworks`, while the same no-permission persona gets `403 required:"content_management.view"` from `GET /api/v1/content/news`; `X-Local-Roles: Comms Officer` also gets `200` on frameworks read. Result: any authenticated user can read the framework registry/detail/stats through the API; a Content Management user can reach the screen but fail writes if they lack Prevention & Mitigation manage; a Prevention & Mitigation manager may satisfy backend write authority but be blocked from the Content route if they lack `content_management.view`.
- Implementation/verification 2026-07-09: `disaster_risk_frameworks` is canonicalized under Content Management/Publications. `/v1/frameworks` is mapped in `ModuleGuardFilter`, controller reads require `content_management.view`, writes require `content_management.manage`, and the frontend route/action buttons follow the same permissions. V161 smoke proved Partner/default access gets 403 on Frameworks while admin gets Frameworks 200; backend compile, frontend production build, `git diff --check`, Flyway V161, and Partner/MoH browser regression smoke passed. Closed; keep future framework/publication routes on the same `content_management.view/manage` contract unless a separate shared permission is intentionally introduced.

## F109 [FIXED s2] Public Reports frontend route permission does not match the backend incident-triage permission model
- Domain: RBAC consistency / public report triage (fresh reassessment, 2026-07-06)
- Status: **FIXED 2026-07-09 — the Public Reports route and API both require `incidents.view`, verified with incident and communications-only personas.**
- Evidence: The UI route guard maps `/m/response/public-reports` to `communication_and_alerts.view` (`access.ts:47`), but the backend module guard maps `/v1/response/public-reports` to `incidents.view` (`ModuleGuardFilter.java:81`) and the controller gates list/read on `incidents.view` plus mutations on incident update/approve authorities (`PublicReportsController.java:47-49`, `:91-128`). Result: a communications user can be allowed into the route and then receive API 403s, while an incident-triage user without Communication & Alerts can be blocked by the frontend even though the backend considers Public Reports part of incident response.
- Implementation note 2026-07-08: `frontend/src/app/core/access.ts` now maps `/m/response/public-reports` to `incidents.view`, matching `ModuleGuardFilter` and `PublicReportsController`. Frontend production build passed with `npm run build -- --configuration production`.
- Verification 2026-07-09: Dist DC browser smoke opened `/m/response/public-reports`, loaded the API, filtered controlled reports, and converted one through the modal. Because the seeded `Comms Officer` also has `incidents.view`, a temporary controlled role/user with only `communication_and_alerts.view` was created for honest negative proof: real `/api/v1/auth/login` returned exactly that permission set and no `incidents.view`; `GET /api/v1/response/public-reports` with the real JWT returned HTTP 403 with `required:"incidents.view"`; headless Chrome navigation to `/m/response/public-reports` redirected to `/access-denied`. The temporary role/user were deleted and follow-up SQL showed zero `SMOKE-F109-*` leftovers.
- Fix hint: Closed. Keep the frontend route, backend module guard, and controller method gate on the same incident-triage permission.

## F110 [FIXED s3] Budget creation lets area finance actors misstate the budget tier/scope used by approval ceilings
- Domain: Budget & Finance / public-finance controls (fresh reassessment, 2026-07-07)
- Status: **FIXED 2026-07-09 — migrated DB plus live finance role smoke proved scope/area consistency for area and national budget creators.**
- Evidence: `BudgetController.createBudget()` reads `scope_level` directly from the body (`BudgetController.java:101-119`). For REGION/DISTRICT callers it correctly overrides `district_id` / `region_id` with the caller's own area (`:108-113`), but it does not force the posted `scope_level` to match the caller's tier or the area ids being saved. The database also does not enforce the consistency: `disaster_budgets` is created with `scope_level`, `district_id`, and `region_id` but no CHECK tying `national => both ids null`, `region => region_id only`, or `district => district_id` (`V99__disaster_budget_finance.sql:19-34`); live `pg_constraint` shows only PK/FK constraints. That matters because `approveCommitment()` later reads `db.scope_level` and applies approval ceilings from `budget_approval_thresholds` (`BudgetController.java:233-249`); live thresholds are `district=50,000,000`, `region=200,000,000`, `national=NULL/unlimited`. `V99` grants `budget_and_finance.manage` to District/Regional Planning Officer roles (`V99:95-98`), and the current DB confirms those roles still hold manage after V142 trims only RAS/Reg DC/DED/DAS. The current seed has zero planning-officer users, so this is latent until those roles are assigned, but the UI already supports assigning those area roles and the finance form posts the chosen scope (`budget-finance.component.ts:333-337`).
- Implementation note 2026-07-08: `BudgetController.createBudget()` now resolves a coherent `BudgetScope`: district callers are forced to their own district tier, regional callers to their own region tier, no-area callers are rejected, and national callers can choose `national`/`region`/`district` only with matching area ids. V145 normalizes existing `disaster_budgets` rows and adds `disaster_budgets_scope_area_chk` so `national` rows have no area ids, `region` rows have only `region_id`, and `district` rows carry `district_id`. Backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`.
- Verification 2026-07-09: V145 was present in the migrated local DB and `disaster_budgets_scope_area_chk` existed. Controlled real-login smoke created temporary District Planning Officer, Regional Planning Officer, and admin sessions. A district planning user posting `scope_level='national'` plus foreign region/district ids received HTTP 200 but SQL stored the row as `scope_level='district'`, `region_id=1`, `district_id=101`; a regional planning user posting a foreign district body stored `scope_level='region'`, `region_id=1`, `district_id=null`. Admin created valid national, region, and district budget rows intentionally, and SQL showed the matching tuples. A direct invalid `national + district_id` insert was rejected by the DB check constraint. Controlled budgets/users were cleaned up and follow-up counts were zero.
- Fix hint: Closed. Keep budget creation on `resolveBudgetScope()` and preserve `disaster_budgets_scope_area_chk` in future migrations.

## F111 [FIXED s3] Direct NDMF incident disbursement skips incident area guard and simulation isolation
- Domain: Budget & Finance / NDMF cash-out controls (fresh reassessment, 2026-07-07)
- Status: **FIXED 2026-07-09 — live finance/drill smoke proved incident area guard, table-top simulation block, and full-scale exercise allowance.**
- Evidence: The normal budget commitment flow is well guarded: requesting budget funds calls `simulationGuard.assertNotSimulationIncident(...)` and scopes the budget line through its parent budget (`BudgetController.java:195-205`), approval scopes the owning budget (`:233-240`), and disbursement again blocks simulation incidents (`:286-289`). The direct NDMF cash-out path is different. `ndmfDisburse()` accepts `incident_id`, checks positive amount, fund balance, donation status, remaining amount, and tight earmarking, then inserts a paid `ndmf_disbursements` row (`BudgetController.java:444-491`), but it never calls `areaGuard.assertOwn("public.incidents", incidentId)` / `assertOwnOrShared` and never calls `simulationGuard.assertNotSimulationIncident(incidentId, ...)`. The DB only enforces that `incident_id` references an existing incident; live constraints on `ndmf_disbursements` have FK/checks but no area/simulation policy, and `incidents` carries an `is_simulation` flag. `V99` grants `budget_and_finance.disburse` to District/Regional Logistic Officer roles (`V99:102-104`); the current DB confirms those roles hold the permission, though no seed users currently hold the roles. The frontend hides most of the risk by loading incidents from scoped `/api/v1/response/incidents` before posting (`budget-finance.component.ts:343-359`), but the backend still accepts any valid incident id an authorized logistics actor can submit or guess.
- Implementation note 2026-07-08: `BudgetController.ndmfDisburse()` now rejects a missing/invalid `incident_id`, applies `areaGuard.assertOwn("public.incidents", incidentId)` before the fund lock/cash movement, and calls `simulationGuard.assertNotSimulationIncident(incidentId, "disbursing NDMF cash")`. Backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`.
- Verification 2026-07-09: controlled real-login smoke created a temporary District Logistic Officer tied to Dodoma Urban and controlled live/table-top/full-scale incidents. Own-district NDMF cash-out returned HTTP 200 and inserted a paid disbursement; a foreign live incident id returned HTTP 404; a table-top simulation incident returned HTTP 422 with the `SIMULATION drill (table-top)` guard message; a full-scale simulation with `allow_real_ops=true` returned HTTP 200 and inserted a paid disbursement, which is the current explicit policy. Before cleanup, SQL showed only the own-live and full-scale references as paid; the foreign/table-top references had no disbursement rows. Controlled disbursements, incidents, activations, and users were cleaned up and follow-up counts were zero.
- Fix hint: Closed. Preserve the strict incident area guard and `SimulationGuard.assertNotSimulationIncident()` before any NDMF cash movement; if policy later disallows full-scale real-money exercise flows, change `SimulationGuard.realOpsAllowed()` policy deliberately and re-smoke finance/logistics.

## F112 [FIXED s3] Hazard Monitor is routed as Content Management but its real actions require the separate Hazards domain permission
- Domain: RBAC consistency / content-publications / mitigation integration (fresh reassessment, 2026-07-07)
- Status: **FIXED 2026-07-09 — live API/browser RBAC smoke proved hazards.view read-only and hazards.manage write/graphic control.**
- Evidence: The frontend exposes Hazard Monitor at `/m/content-management/hazard-monitor` under Content Management (`app.routes.ts:108`, `modules.ts:145`), and the API prefix `/v1/content/threats` is module-gated as Content Management by `ModuleGuardFilter` (`ModuleGuardFilter.java:65`). Reads on the threat controller are only `isAuthenticated()` behind that module gate (`ThreatAdminController.java:51-67`), but every real operation on the same screen is guarded by `hazards.manage`: create/update threat, add/edit timeline updates, review stakeholder plans, and delete (`ThreatAdminController.java:86-180`). The component always renders the action controls (`threat-monitor.component.ts:32-35`, `:91-108`, `:127-131`, `:187-263`) and has no permission-aware disabling. Live proof against the running local backend: `X-Local-Roles: Comms Officer` returned `200` from `GET /api/v1/content/threats` but `403` from `POST /api/v1/content/threats`; `X-Local-Roles: EOCC` on the same POST reached method validation and returned `400 Threat name is required`. The DB explains why: `Comms Officer` holds `content_management.manage/view` but not `hazards.manage`; `hazards.manage` is held by EOCC/Director/Asst Director/Super Admin/ICT Admin. Result: a legitimate Content Management user can open what looks like a working content screen and then hit authorization failures on its main controls, while the domain permission name says this may actually belong to hazards/prevention rather than content.
- Implementation note 2026-07-09: Hazard Monitor is canonicalized to the Hazards permission family while preserving the existing URL. `access.ts` maps `/m/content-management/hazard-monitor` to `hazards.view`; `ModuleGuardFilter` maps `/v1/content/threats` to `hazards.view`; threat reads require `hazards.view`; create/update/timeline/plan-review/delete plus the threat-specific graphic upload endpoint require `hazards.manage`; and the Angular screen hides or disables mutation controls unless the caller has `hazards.manage`. Permission labels/hints describe that `hazards.view/manage` covers both Hazard Management and Hazard Monitor. The first live smoke exposed a separate threat-controller bug where partial update/timeline payloads with omitted enum fields could hit `List.of(...).contains(null)` and throw; `ThreatAdminController` now normalizes optional `severity`/`status` fields null-safely. Backend compile passed with `JAVA_HOME=/home/kaijage/tools/jdk /home/kaijage/tools/maven/bin/mvn -B -DskipTests compile`; `./start-all.sh` restarted the local stack from current source; and `frontend/render-tests/f112-hazard-monitor-rbac-smoke.js` passed (`codex-f112-1783607954254`) after proving Content-only denial, Hazards-view read-only API/browser access, Hazards-manage create/update/timeline/plan-review/graphic upload, and Chrome visibility/disabled-state controls.
- Fix hint: Closed. Keep Hazard Monitor route/API reads on `hazards.view`, all mutation/graphic paths on `hazards.manage`, and preserve null-safe enum normalization for partial update payloads.

## F113 [FIXED s3] Content Management still hosts Settings/User-Management submodules with different backend permission families
- Domain: RBAC consistency / content-publications / settings integration (fresh reassessment, 2026-07-07)
- Status: **FIXED 2026-07-09 — live API/browser RBAC smoke proved Translations and Agencies are controlled by their owning System Settings permissions.**
- Evidence: The Content Management module menu advertises `Translations` and `Agencies` alongside news/portal content (`modules.ts:133-146`), and both routes are guarded only by `/m/content-management -> content_management.view` (`access.ts:82`, `app.routes.ts:107`, `:110`). Their APIs teach different contracts. Translations calls `/api/v1/settings/translations` (`translations.component.ts:106-135`), while `ModuleGuardFilter` requires `translations.view` for `/v1/settings/translations` and writes require `translations.manage` (`ModuleGuardFilter.java:74`, `TranslationController.java:41-111`). Agencies lists under `/v1/content/agencies` but create/update/delete require `user_management.manage` (`AgencyAdminController.java:41-103`), even though the component renders New/Edit/Activate controls from the Content screen (`agencies.component.ts:19-22`, `:51-57`, `:138-153`). Live proof: `X-Local-Roles: EOCC` gets the Content module but `GET /api/v1/settings/translations?...` returns `403` with `required:"translations.view"`; `X-Local-Roles: Comms Officer` gets `200` on `GET /api/v1/content/agencies` but `403` on `POST /api/v1/content/agencies`; `X-Local-Roles: ICT Admin` on that same POST reaches validation and returns `400 Agency name is required`. This creates polished but partially dead content screens for roles that the menu says are allowed, and hides the real owning permission from admins configuring roles.
- Implementation note 2026-07-09: Translations and Agencies are moved out of the Content Management menu into System Settings. Guarded routes are `/m/user-management/translations` (`translations.view`) and `/m/user-management/agencies` (`user_management.view`), with the old content URLs redirecting to the owning screens. Agencies uses `/api/v1/settings/agencies`; `ModuleGuardFilter` maps it to `user_management.view`; controller reads require `user_management.view`; writes require `user_management.manage`; and the Angular agency screen hides New/Edit/Activate controls unless the caller has `user_management.manage`. Role-permission labels/hints tell admins that translations follow `translations.*` and partner agencies follow `user_management.*`. Live `frontend/render-tests/f113-settings-rbac-smoke.js` passed (`codex-f113-1783609004887`) after creating temporary roles through `/settings/roles`, proving Content-only API/old-route denial, Translations-view read-only access, Translations-manage create/update and editable cells, User-view Agencies read-only access, and User-manage Agencies create/update/delete plus row actions. The smoke cleaned controlled roles/translations/agencies through Settings APIs.
- Fix hint: Closed. Keep Translations under `translations.*`, Agencies under `user_management.*`, old Content Management URLs redirecting to the owning System Settings routes, and action visibility tied to the matching manage permission.

## F114 [GAP s3] DMD Impact Analysis lacks a real satellite/exposure geospatial layer and direct INFORM-context overlay
- Domain: Early Warning / Impact Analysis / satellite, exposure, INFORM integration (fresh reassessment, 2026-07-07)
- Status: **OPEN**
- Evidence: The DMD consolidated impact screen is real but still operator-synthesis driven. `DmdConsolidatedComponent` describes the current product as an overlay of warning entities' submissions into a 3-day district picture (`dmd-consolidated.component.ts:18-21`), then stores manual PMO impact inputs as `pmoShapes`, `pmoOverrides`, `pmoNarratives`, and `pmoDirectives` (`:172-176`). `generateImpact()` turns the consolidated warning tiers into a multirisk bulletin with affected districts, area points, and a centroid (`:204-241`), and `confirmPush()` ingests it as a pending warning and stores the PDF (`:256-275`). After the F104 cleanup, the map base is local-first/governed rather than a direct Carto bypass, but the DMD impact screen still uses GADM district polygons and operator-painted impact tiers, not a satellite/EO scene selector, WMS/WMTS/COG layer, exposure dataset, or server-computed impact footprint. INFORM is stronger than a stub: advanced Tanzania-EO indicators are seeded separately from the strategic model (`V116__inform_seed_advanced.sql:1-31`), `InformService.riskFor()` deliberately keeps strategic risk separate from operational EO signals (`InformService.java:158-177`), and `signalsFor()` returns decomposed reliability-flagged EO hazard signals (`InformService.java:179-225`). The INFORM map UI consumes risk and signals in the Prevention/Mitigation module (`inform-map.component.ts:33-34`, `:256-275`), but a targeted search found no INFORM API/service calls in the Preparedness EW agency/DMD screens or `tz.go.pmo.dmis.ew`, so DMD Impact Analysis does not directly overlay INFORM risk/signals while preparing a national impact bulletin. A source scan also found no backend contract for satellite scenes/layer catalogues/exposure datasets/hazard-footprint intersection results (`satellite`, `Sentinel`, `Landsat`, `WMS`, `WMTS`, `GeoTIFF`, `COG`, `layer_catalog`, `exposure_dataset`, `impact_analysis`, `ST_Intersects`); the satellite wording in current UI is mostly iconography, not an operational satellite layer subsystem.
- Fix hint: Build a governed `gis-impact` capability around the existing central map concept rather than adding one-off map code. Minimum target: `gis_layer_catalog` (base, satellite, hazard, exposure, INFORM, preparedness-resource layers), `satellite_scene`/`eo_product` metadata (source, acquisition time, resolution, cloud cover, confidence, URL/cache key), `exposure_dataset` registry (population, buildings, crops/livestock, roads, schools, health facilities, evacuation centers, warehouses, critical infrastructure), `hazard_footprint` geometries for warnings/scanner/DMD-drawn areas, and `impact_analysis_result` snapshots that store footprint ∩ exposure ∩ INFORM risk/signals with confidence and human disposition. DMD should be able to switch base/satellite/terrain layers, overlay the warning footprint, see exposed people/assets/resources and INFORM vulnerability/coping context, then generate the impact bulletin from that audited snapshot. If ML/AI is added, it must write prediction events against the same snapshot with model version, confidence, explanation, and human accept/reject.

## F115 [FIXED s3] Leaflet tooltips still interpolate live API/operator values outside Angular escaping in Response, DMD, and cross-agency maps
- Domain: Frontend security / production hardening / GIS UI (fresh reassessment, 2026-07-07)
- Status: **FIXED 2026-07-09 — Leaflet tooltip escaping verified with hostile-data Chrome regression.**
- Evidence: The codebase already knows the right pattern: `frontend/src/app/core/html.ts:1-11` defines `escapeHtml()` specifically for Leaflet popup HTML; public portal, INFORM, infrastructure, warehouse, and mitigation GIS popups use it. The problem is inconsistent adoption in other production maps. Response dashboard tooltips inject incident API fields directly into Leaflet HTML (`response/dashboards.component.ts:135`, `:288`: `<b>${i.title}</b><br>${i.severity_level} · ${i.status}`), and the Command Center incident situation map injects activation title/severity/region/location the same way (`command-center.component.ts:822`). Those values are not static labels: `/api/v1/response/dashboard` selects `i.title`, `i.location_description`, `i.severity_level`, and `i.status` for `recent_incidents` (`DashboardController.java:150-154`), `/api/v1/response/eocc` selects the same style of incident fields for `recent_incidents` and `map_incidents` (`DashboardController.java:256-266`), and incident create writes `title` and `location_description` straight from the submitted form (`IncidentController.java:226-242`) with validation proving only presence/enumeration, not HTML neutralization (`IncidentController.java:783-810`). The DMD consolidated impact map repeats the raw HTML pattern for agency overlays (`dmd-consolidated.component.ts:548`, `:577`) using agency/type/area strings from warning submissions, and the reusable cross-agency reference helper does the same (`cross-agency-ref.ts:78`). Leaflet binds these strings through DOM HTML, outside Angular interpolation escaping, so a stored HTML payload in an incident title/location or agency area can render in operational map tooltips even though adjacent modules have already been hardened.
- Implementation note 2026-07-09: `escapeHtml()` now guards Response dashboard and EOCC incident map tooltips, Command Post incident-situation tooltips, DMD consolidated district/overlay/hydromet tooltips, MoW flood district tooltips, EW region-picker and cross-agency reference marker tooltips, EW alert-map region tooltips, Anticipatory Plans coverage tooltips, and the shared Tanzania base-map fallback ward/district/region labels. The public landing callback path remains a deliberate pre-escaped HTML contract. Added `frontend/render-tests/f115-leaflet-xss-smoke.js`, which drives Chrome through Response dashboard, EOCC, Command Post incident map, PMO-DMD consolidated district/overlay markers, and EW region-picker/cross-agency reference markers with `<img onerror=...>` payloads from mocked API/GeoJSON data. Verification passed: `node frontend/render-tests/f115-leaflet-xss-smoke.js` (`F115 Leaflet XSS smoke passed: codex-f115-1783606062177`) and frontend `npm run build`.
- Fix hint: Make `escapeHtml()` the mandatory boundary for every Leaflet `bindTooltip`, `bindPopup`, and `L.divIcon({ html })` path that includes API/operator values; use DOM nodes plus `textContent` where the markup is simple. Add a small helper such as `leafletTooltipHtml(parts)` so Response, Preparedness EW, and cross-agency maps do not each re-invent escaping. Add regression data/tests with `<img onerror=...>` or equivalent inert HTML in incident titles, locations, agency areas, and reference-marker names; verify the literal text appears in tooltips and never executes/renders as markup. Keep vetted static icon/color strings separate from escaped dynamic text.

## F116 [PARTIAL s3] Component linkage, AI-readiness, and multiscale production capacity are not captured as executable contracts
- Domain: System architecture / component linkage / scalability / AI-readiness (fresh reassessment, 2026-07-07)
- Status: **OPEN**
- Evidence: The system is a large modular monolith with real module surfaces: the current source has 18 backend package areas under `tz.go.pmo.dmis`, 82 controller files, and 596 Spring mapping annotations; the largest concentration is Response (21 controllers), then Portal (9), Mitigation (8), Settings/Preparedness/EW (6 each), One Health (5), Reports/Recovery (4 each), Notification (3), Repository/Content (2 each), and IAM/Finance/Stakeholder (1 each). The technology foundation is viable: Spring Boot 3.3.4 / Java 21, Angular 18, PostgreSQL, Flyway, Spring Security, OpenAPI, Actuator health, and production Hikari settings with `DB_POOL_MAX` default 20 (`backend/pom.xml:12-99`, `application-prod.yml:1-8`). But the current architecture is not yet an executable linkage/capacity model. `DmisPlatformApplication` says bounded contexts communicate through outbox events (`DmisPlatformApplication.java:10-15`), while the standing F18 evidence and a fresh search show no aggregate extending/registering events outside the common event scaffolding (`AggregateRoot`, `DomainEvent`, `OutboxAppender`, `OutboxRelay` only). In practice, linkage is mostly direct module/service/JdbcTemplate reads over shared `public` tables, plus selected async notification calls. Production-scale primitives are partial: notification delivery has an in-process `@Async` executor of core 2, max 6, queue 200 (`AsyncConfig.java:16-26`); public/login/upload rate limiting is useful but in-memory/per-instance by design (`FixedWindowRateLimiter.java:7-15`); management exposes only health/info (`application.yml:79-86`); frontend freshness is polling-heavy (topbar 45s, public landing 60s, response/EOCC dashboard 30s, executive watch 30s, and now Command Post board 30s under fixed F29), but a fresh scan found no Kafka/Rabbit/Redis/shared cache/Caffeine `@Cacheable`/SSE/WebSocket runtime. This means the stack can scale as a hardened modular monolith, but not yet as a crash-resistant national surge platform or AI/GIS workload platform without explicit event contracts, read models, shared throttling/cache, worker queues, observability, and load tests.
- Progress 2026-07-15: the hybrid contract is now executable for the incident slice. Read-only `mobileHome` GraphQL composes scoped services and returns a repeatable-read cursor/scope key; REST keeps authoritative idempotent incident commands. V211 captures incident insert/update/delete in the same transaction, serializes cursor assignment through commit, emits jurisdiction-move tombstones, exposes scoped REST delta recovery, and wakes authorized web sessions through bounded content-free REST/SSE. V212 supplies a separate per-user commit-ordered inserted-notification cursor. This reduces a concrete linkage/offline risk but does not close F116: other domains have no such delta/conflict/idempotency contract, there is no general outbox/broker or native APNs/FCM implementation, and staging capacity/SLO/load evidence is still absent.
- Fix hint: Create a system-linkage control plane before microservice extraction or AI launch. Minimum: component dependency matrix (module owner, routes, APIs, tables, permissions, row scope, upstream/downstream dependencies), OpenAPI/event contracts, a real transactional outbox with consumers/replay/retry/dead-letter/correlation ids, shared rate limiting/cache where multi-node deployment is expected, async workers for notification fan-out/PDF/GIS/AI jobs, read-model projections for dashboards and public counters, SLOs and load tests for district/region/national/surge profiles, and AI feature-snapshot/model-run/prediction-event/human-disposition tables. Keep incident command, stock movement, finance, and approval workflows in the monolith until idempotency and audit timelines are proven; extract notification/integration and analytics read models first.

## 2026-07-10 — Economics of Disaster v2 (Budget & Finance)

- Service `EconomicsOfDisasterService` + `GET /v1/finance/economics` (deterministic, not AI).
- Feeds: budgets/NDMF, allocations×unit_cost, stock, relief, recovery, mitigation/training/AAP/contingency budgets, warehouses/ECs, EW + incidents.
- Horizons: annual, climate bands (Masika/Vuli/Dry), per-incident, influence board.
- Fixed TX-abort isolation (no outer @Transactional); dual-proved non-zero threats/seasonal/forecast.
- FE: `/m/budget-finance/economics`.

## 2026-07-10 — residual close-out (F102 / F59 / 404 hygiene)

- **F102 FIXED (platform, reverified 2026-07-15):** Testcontainers 1.21.4 + `docker-java.properties` api.version=1.44 (Docker Engine 29); hermetic Postgres loads `db/baseline/baseline.sql`. Isolated Docker-backed verification executes **186/186 with no failures, errors, or skips**. Flyway validates 197 schema-history entries (the baseline marker plus 196 versioned SQL files through V212), with scheduled external jobs disabled by the hermetic base.
- **F59 FIXED (platform dual-proof):** DLR maps SMPP `DELIVRD`; POST `/v1/webhooks/mgov/dlr` flipped sms_logs to delivered (MGV-PROOF-F59-001/003). Ops residual only: production must set `DMIS_MGOV_DLR_SECRET` and register gateway callback URL.
- **Unknown API paths → 404** (was 500) via GlobalExceptionHandler NoResourceFoundException.

## 2026-07-10 — Security reassessment (basic gaps closed)

- Local god-mode OFF by default (`dmis.security.local-god-mode=false`); unauth protected APIs → 401 under local.
- JWT jti + POST /v1/auth/logout denylist (proved 200→401).
- Limited tokens for password_change / mfa_enroll (`RestrictedTokenUseFilter`).
- JWT TTL default 30m; limited 15m; FE idle 30m + server logout.
- Authenticated write rate limit; prod springdoc disabled; actuator health-only in prod.
- Dual-proof: unauth 401 matrix, storage 401, logout denylist, weak password 400, CORS 403.

## F117 [PARTIAL s3] V198 cutover password revoke uses one fixed literal bcrypt hash for every revoked account
- Domain: security hygiene / cutover (found by the 2026-07-18 deep reassessment)
- Status: **OPEN**
- Evidence: V198__revoke_local_test_password_on_cutover.sql sets every demo/seeded account to the single literal hash `$2a$10$N9qo8uLOickgx2ZMRZoMye…` — the salt is the canonical jBCrypt textbook example, and the "generated once; not a known secret" comment is unverifiable. 15 common candidate passwords were checked offline and all fail, so it is not trivially usable — but one shared literal across all revoked accounts means a single preimage discovery opens every one of them. V198 is already applied, so the FILE must not be edited (Flyway checksum).
- Fix hint: new migration (counter line — next free V215+) setting `password = NULL` (login already fails on null hash) or a per-row random value for every account still carrying the V198 literal; run before any public cutover.

## F118 [PARTIAL s3] V178 orphan-stock repair broader than its own comment — unscoped first UPDATE re-attaches ALL orphan stock to one arbitrary store
- Domain: data migration honesty (found by the 2026-07-18 deep reassessment)
- Status: **OPEN**
- Evidence: V178__orphan_stock_repair_and_role_clarity.sql's comment scopes the repair to "inventory_items id 18 / movement 12", but the first UPDATE carries no id filter — every orphan stock line (null warehouse ids, qty>0) is re-attached to the lowest-id active temporary warehouse; only the movement UPDATE is scoped (sm.id=12). Idempotent but over-broad; on a DB with other orphans it silently assigns unrelated stock to one store. Applied — do not edit the file.
- Fix hint: document the actual behavior here (done); on prod cutover, verify no unexpected orphan rows existed pre-V178 (compare stock_movements journal); scope any future data repair by id.

## F119 [PARTIAL s4] Disaster Repository events registry READ requires the authoring permission — `disaster_repository.view` holders (Secretary) lose all read access
- Domain: RBAC semantics (found by the 2026-07-18 deep reassessment)
- Status: **OPEN**
- Evidence: ModuleGuardFilter maps /v1/repository/events → disaster_repository.enter and access.ts gates /m/reports-analytics/repository on .enter too (coherent, but on the wrong permission for the read path); Secretary holds only disaster_repository.view (SQL-verified) and cannot open the registry while holding a permission literally named "view".
- Fix hint: product decision — either the read path accepts .view OR .enter, or Secretary is granted .enter; today ".view" is a permission that grants nothing on this module.

## F120 [PARTIAL s4] RecoveryLocalSeeder still uses the max(id)+1 code-generation race F98 condemned
- Domain: recurring failure pattern (local-only; found by the 2026-07-18 deep reassessment)
- Status: **OPEN**
- Evidence: RecoveryLocalSeeder.java:115 generates codes with max(id)+1 against the same unique column whose production path was fixed in F98 (StrategicProjectServiceImpl.store() now id-derived). Local profile only, but it is the exact recurring pattern the campaign tracks.
- Fix hint: same id-derived pattern as the F98 fix, one line.
